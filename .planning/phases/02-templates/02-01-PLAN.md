---
phase: 02-templates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/templates/STATE-TEMPLATE.md
  - core/templates/SUMMARY-TEMPLATE.md
autonomous: true

must_haves:
  truths:
    - "STATE-TEMPLATE.md matches the format defined in state-machine.md lines 83-113 and can initialize design state for any workflow"
    - "SUMMARY-TEMPLATE.md matches the format defined in compose-screen.md section E (lines 106-127) and provides the structure for screen composition summaries"
    - "Both templates use {PLACEHOLDER} syntax for agent-filled values and [description] syntax for agent-written content"
  artifacts:
    - path: "core/templates/STATE-TEMPLATE.md"
      provides: "Reusable state initialization template"
      contains: "Design Forge State"
    - path: "core/templates/SUMMARY-TEMPLATE.md"
      provides: "Reusable screen composition summary template"
      contains: "Screen:"
  key_links:
    - from: "core/templates/STATE-TEMPLATE.md"
      to: "core/references/state-machine.md"
      via: "format extraction"
      pattern: "Phase|Vertical|Stack|Screens|Decisions Log|Context Budget"
    - from: "core/templates/SUMMARY-TEMPLATE.md"
      to: "core/workflows/compose-screen.md"
      via: "format extraction"
      pattern: "Components Used|Key Tokens Referenced|Vertical Patterns Applied|States|Files Created"
---

<objective>
Create the STATE-TEMPLATE.md and SUMMARY-TEMPLATE.md output templates by extracting and formalizing the formats already defined in their source-of-truth files.

Purpose: Workflows and agents need standalone, reusable template files to produce consistent output. Currently these formats exist inline in reference/workflow files but have no dedicated template. This plan extracts them into `core/templates/` alongside the existing `VERTICAL-TEMPLATE.md`.

Output: Two markdown template files in `core/templates/`.
</objective>

<execution_context>
@/Users/austineogbuanya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austineogbuanya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@core/references/state-machine.md
@core/workflows/compose-screen.md
@core/templates/VERTICAL-TEMPLATE.md
@.planning/phases/02-templates/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create STATE-TEMPLATE.md</name>
  <files>core/templates/STATE-TEMPLATE.md</files>
  <action>
Extract the STATE.md format from `core/references/state-machine.md` lines 83-113 into a standalone template file at `core/templates/STATE-TEMPLATE.md`.

The template must:

1. Start with a source-of-truth comment: `<!-- Source of truth: core/references/state-machine.md -- keep in sync -->`
2. Use `{PLACEHOLDER}` syntax for values the orchestrator fills in at runtime:
   - `{VERTICAL}` for the detected vertical
   - `{STACK}` for the technical stack
   - `{ISO_DATE}` for the initialization date
3. Set the initial Phase to `INITIALIZED` (not the enum list from the reference) because STATE.md is created by `/forge:init` which transitions from UNINITIALIZED to INITIALIZED
4. Start the Screens table with the header row but no data rows (screens are added later by `/forge:compose`)
5. Start the Decisions Log with `- {ISO_DATE} Project initialized`
6. Include the Context Budget table exactly as defined in state-machine.md (matching the token budgets: PROJECT.md <=1,000, DESIGN-BRIEF.md <=1,000, DESIGN-RESEARCH.md <=3,000, tokens.css <=3,000, COMPONENT-SPECS.md <=5,000)
7. Use `~` prefix for approximate token counts in the initial budget (e.g., `~800`) and leave generated-file counts as `--` since they don't exist at initialization
8. Match the exact section headers from state-machine.md: `# Design Forge State`, `## Phase`, `## Vertical`, `## Stack`, `## Screens`, `## Decisions Log`, `## Context Budget`

Do NOT add any sections beyond what state-machine.md defines. Do NOT include process documentation or usage instructions beyond the source-of-truth comment. The template is an output format blueprint, not a guide.
  </action>
  <verify>
1. File exists at `core/templates/STATE-TEMPLATE.md`
2. Contains all 6 section headers from state-machine.md: Phase, Vertical, Stack, Screens, Decisions Log, Context Budget
3. Contains `{VERTICAL}`, `{STACK}`, `{ISO_DATE}` placeholders
4. Phase is set to `INITIALIZED` (not the enum list)
5. Context Budget table has 5 rows matching state-machine.md budgets
6. Source-of-truth comment present at top
  </verify>
  <done>STATE-TEMPLATE.md exists in core/templates/, matches the state-machine.md format exactly, and uses placeholder syntax for runtime values.</done>
</task>

<task type="auto">
  <name>Task 2: Create SUMMARY-TEMPLATE.md</name>
  <files>core/templates/SUMMARY-TEMPLATE.md</files>
  <action>
Extract the screen summary format from `core/workflows/compose-screen.md` section E "Create Summary" (lines 106-127) into a standalone template file at `core/templates/SUMMARY-TEMPLATE.md`.

The template must:

1. Start with a source-of-truth comment: `<!-- Source of truth: core/workflows/compose-screen.md section E -- keep in sync -->`
2. Use `{SCREEN_NAME}` placeholder for the screen name in the heading: `# Screen: {SCREEN_NAME}`
3. Use `[description]` syntax for sections where the agent writes original content:
   - `[List all design system components used in this screen]` under `## Components Used`
   - `[List the primary tokens this screen depends on]` under `## Key Tokens Referenced`
   - `[Which LOCKED decisions from DESIGN-RESEARCH.md were implemented]` under `## Vertical Patterns Applied`
   - `[List all files created/modified for this screen]` under `## Files Created`
4. Include the States section with the exact four states from compose-screen.md: Default, Loading, Empty, Error -- using a placeholder checkmark pattern (keep it as the compose workflow shows it: `Default ✓/✗` etc.)
5. Match these exact section headers from compose-screen.md: `## Components Used`, `## Key Tokens Referenced`, `## Vertical Patterns Applied`, `## States`, `## Files Created`

Cross-reference check: the reviewer agent (`runtimes/claude-code/agents/forge-design-reviewer.md`) loads `{screen}-SUMMARY.md` to compare "intent vs. implementation." Ensure the template includes all fields the reviewer would need: components, tokens, vertical patterns, states, and files. The compose-screen.md format already covers all of these.

Do NOT add sections beyond what compose-screen.md section E defines. Do NOT include process documentation.
  </action>
  <verify>
1. File exists at `core/templates/SUMMARY-TEMPLATE.md`
2. Contains all 5 section headers: Components Used, Key Tokens Referenced, Vertical Patterns Applied, States, Files Created
3. Contains `{SCREEN_NAME}` placeholder
4. States section lists all four: Default, Loading, Empty, Error
5. Source-of-truth comment present at top
  </verify>
  <done>SUMMARY-TEMPLATE.md exists in core/templates/, matches the compose-screen.md section E format exactly, and uses placeholder syntax for runtime values.</done>
</task>

</tasks>

<verification>
1. Both files exist in `core/templates/` alongside existing `VERTICAL-TEMPLATE.md`
2. STATE-TEMPLATE.md sections match state-machine.md lines 83-113 one-to-one
3. SUMMARY-TEMPLATE.md sections match compose-screen.md lines 106-127 one-to-one
4. No sections were added beyond what source-of-truth files define
5. Placeholder conventions are consistent: `{UPPER_SNAKE}` for orchestrator values, `[description]` for agent-written content
</verification>

<success_criteria>
- STATE-TEMPLATE.md can be used by `/forge:init` to create a valid STATE.md by replacing {VERTICAL}, {STACK}, and {ISO_DATE}
- SUMMARY-TEMPLATE.md can be used by the screen composer agent to create a valid {screen}-SUMMARY.md by replacing {SCREEN_NAME} and filling [description] sections
- Both templates are pure output format blueprints with no process documentation
</success_criteria>

<output>
After completion, create `.planning/phases/02-templates/02-01-SUMMARY.md`
</output>

---
phase: 27-nextjs-scaffold-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/get-motif/scripts/scaffold-project.js
  - .claude/get-motif/references/framework-registry.json
  - .claude/get-motif/workflows/generate-system.md
  - .claude/get-motif/references/composer-nextjs.md
  - .claude/get-motif/workflows/compose-screen.md
  - .claude/get-motif/fixtures/phase27-nextjs/
autonomous: true
requirements:
  - SCAF-02
  - COMP-01
---

<objective>
Finish the Next.js scaffold path so that `SCAF-02` and the Next.js integration gap are no longer flagged: Motif must run `create-next-app`/composition wiring and hand the compositor a real App Router project configured with Tailwind + shadcn, then feed that runnable project into the flow that already exists for API/Web composition.
Purpose: The audit shows only the registry contract exists (`SCAF-02` partial) and the integration between scaffold → compose → auto-run is only documented, not executable. Closing those gaps requires running the actual scaffold command, ensuring the generated project is consistent with `composer-nextjs.md`, and verifying the flow on disk before auto-run takes over.
</objective>

<execution_context>
@/Users/austineogbuanya/.codex/get-shit-done/workflows/execute-plan.md
@/Users/austineogbuanya/.codex/get-shit-done/workflows/plan-phase.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.4-MILESTONE-AUDIT.md
@.claude/get-motif/scripts/scaffold-project.js
@.claude/get-motif/references/framework-registry.json
@.claude/get-motif/references/composer-nextjs.md
@.claude/get-motif/workflows/generate-system.md
@.claude/get-motif/workflows/compose-screen.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" id="27-01-01" wave="1">
  <name>Register a Next.js scaffold runner for `create-next-app`</name>
  <files>.claude/get-motif/scripts/scaffold-project.js, .claude/get-motif/references/framework-registry.json</files>
  <action>
    Expand the registry entry for `web-nextjs` to declare an explicit scaffold contract (command, args, env, post-install, template overrides, output paths). Update `scaffold-project.js` so the Next.js path switches from stub detection to spawning `npx create-next-app --typescript --experimental-app` (or registry-provided command) inside the new project directory, copies `composer-nextjs.md` overlay files, wires Tailwind/shadcn install steps, and writes a clear verification marker so later tasks can confirm the project is runnable.
    Ensure scaffolding is idempotent (recreates files when a project already exists) and captures stdout/stderr to help diagnosis.
  </action>
  <verify>
    <automated>
      <check command="node .claude/get-motif/scripts/scaffold-project.js --platform web-nextjs --dry-run">Confirms the new command exists and reports the registry-driven scaffold step without actually mutating disk.</check>
      <check command="node .claude/get-motif/scripts/scaffold-project.js --platform web-nextjs --target ./tmp-phase27 && cat tmp-phase27/package.json">Ensures the scaffold runner can create a Next.js project and that the generated `package.json` includes `app` dir and Tailwind/shadcn dependencies.</check>
    </automated>
  </verify>
  <done>The registry declares an executable Next.js scaffold command, and `scaffold-project.js` spawns that workflow to produce a runnable project folder along with the overlay files expected by `composer-nextjs.md`.</done>
</task>

<task type="auto" id="27-01-02" wave="1" depends_on="27-01-01">
  <name>Wire the runnable Next.js project into the composition and auto-run flow</name>
  <files>.claude/get-motif/workflows/generate-system.md, .claude/get-motif/workflows/compose-screen.md</files>
  <action>
    Update `generate-system.md` so the Next.js case does more than write registry metadata: after tokens, it must invoke the scaffold runner, refresh STATE.md with the new project path, and flag the output as ready for composition. Adjust `compose-screen.md` to detect the real Next.js project (created by Step 1), resolve the overlay via `composer-nextjs.md`, and prevent regressions to prior stub behavior. Document the new hand-off inside the workflow so downstream phases (e.g., auto-run) know a runnable scaffold exists when they inspect STATE/project artifacts.
  </action>
  <verify>
    <automated>
      <check command="node .claude/get-motif/workflows/generate-system.md --platform web-nextjs --dry-run">Verifies the workflow now invokes the scaffold runner and reports the generated project path without performing the full run.</check>
      <check command="node .claude/get-motif/workflows/compose-screen.md --platform web-nextjs --dry-run">Ensures the composer can resolve the project produced by Step 1 and references `composer-nextjs.md` overlays.</check>
    </automated>
  </verify>
  <done>Composing a Next.js project now consumes the actual scaffold output instead of stubs, so the path `init → scaffold → compose → auto-run` is executable and closes the integration gap.</done>
</task>

</tasks>

<verification>
1. Running `/motif:init` with the Next.js option creates a real App Router project folder that includes `package.json`, `app` directory, Tailwind/shadcn deps, and the registries overlay metadata.
2. `scaffold-project.js` reads the registry entry, spawns `create-next-app`, copies overlay files, and leaves a marker (e.g., `.motif-nextjs-scaffolded`) so the compose flow can detect it.
3. `compose-screen` and `generate-system` detect the real project, use `composer-nextjs.md`, and log the same project path that auto-run will later inspect.
4. No gaps remain for SCAF-02 or the Next.js integration flow (audit update will show satisfied once validation is rerun).
</verification>

<success_criteria>
- Running the Next.js path results in a runnable project (App Router + Tailwind/shadcn + `create-next-app` output).
- Compose now consumes that project instead of simulacra, closing the gap noted in `SCAF-02` and the - Compose now consumes the actual scaffold output instead of simulacra, closing the gap noted in `SCAF-02` and the related Next.js integration flow.
- Automation markers/logs confirm the audit evidence (registry contract + runnable project) without misreporting partial completion.
</success_criteria>

<output>
After implementation, update `.planning/phases/27-nextjs-scaffold-execution/27-01-SUMMARY.md` with validation notes and any follow-on work.
</output>

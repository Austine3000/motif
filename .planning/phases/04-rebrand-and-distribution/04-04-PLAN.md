---
phase: 04-rebrand-and-distribution
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - core/references/runtime-adapters.md
  - core/workflows/generate-system.md
  - docs/BUILD-SPEC.md
  - test/e2e-installer.js
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Zero old-brand references in shipped files -- grep for forge/Design Forge/FORGE_ROOT across core/, runtimes/, bin/ returns zero results"
    - "E2e test suite passes with correct motif-*.md agent filename expectations"
    - "docs/BUILD-SPEC.md reflects current Motif-branded filenames"
  artifacts:
    - path: "core/references/runtime-adapters.md"
      provides: "Correct motif-*.md agent filenames in adapter table"
      contains: "motif-researcher"
    - path: "core/workflows/generate-system.md"
      provides: "Motif-branded CSS comment example in token template"
      contains: "MOTIF"
    - path: "docs/BUILD-SPEC.md"
      provides: "Updated build spec reflecting Motif branding"
      contains: "Motif"
    - path: "test/e2e-installer.js"
      provides: "Correct motif-researcher.md expectation and MOTIF_ROOT variable names"
      contains: "motif-researcher.md"
  key_links:
    - from: "test/e2e-installer.js"
      to: "runtimes/claude-code/agents/motif-researcher.md"
      via: "expected filename in test assertion"
      pattern: "motif-researcher\\.md"
    - from: "core/references/runtime-adapters.md"
      to: "runtimes/claude-code/agents/motif-*.md"
      via: "agent filename references in adapter table"
      pattern: "motif-\\*\\.md"
---

<objective>
Close the single UAT gap: stale forge references survive in 2 shipped files (runtime-adapters.md, generate-system.md) and 2 non-shipped files (docs/BUILD-SPEC.md, test/e2e-installer.js).

Purpose: UAT Test 3 ("Zero old-brand references in shipped files") failed because the Phase 4 rebrand missed isolated single-line survivors in a table row and a CSS comment example. The test file also has a broken assertion (expects forge-researcher.md but the actual file is motif-researcher.md) that will cause e2e test failures.

Output: All four files corrected. Shipped files pass the zero-forge-references grep sweep. E2e test assertions match actual motif-branded filenames.
</objective>

<execution_context>
@/Users/austineogbuanya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austineogbuanya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-rebrand-and-distribution/04-01-SUMMARY.md
@.planning/phases/04-rebrand-and-distribution/04-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix stale forge references in shipped files (runtime-adapters.md, generate-system.md)</name>
  <files>core/references/runtime-adapters.md, core/workflows/generate-system.md</files>
  <action>
    **runtime-adapters.md (line 23):**
    Replace the agent defs row in the adapter table. Current line:
    ```
    | **Agent defs** | `.claude/agents/forge-*.md` | `.opencode/agents/forge-*.md` | `.gemini/agents/forge-*.md` | N/A |
    ```
    Replace with:
    ```
    | **Agent defs** | `.claude/agents/motif-*.md` | `.opencode/agents/motif-*.md` | `.gemini/agents/motif-*.md` | N/A |
    ```
    This is a factual correction -- the actual files on disk are motif-*.md (renamed in 04-01).

    **generate-system.md (line 153):**
    Replace the CSS comment example header. Current line:
    ```
       DESIGN FORGE — [Product] Design Tokens
    ```
    Replace with:
    ```
       MOTIF — [Product] Design Tokens
    ```
    This prevents AI agents from writing old branding into generated tokens.css files.

    After edits, run a comprehensive grep sweep to confirm zero forge/Design Forge/FORGE_ROOT references remain in core/ and runtimes/:
    ```bash
    grep -rni "design forge\|/forge:\|design-forge\|FORGE_ROOT\|forgeRoot\|commands/forge\|forge-researcher\|forge-system\|forge-screen\|forge-design\|forge-fix\|forge-token\|forge-font\|forge-aria\|forge-context" core/ runtimes/ bin/
    ```
    Must return zero results.
  </action>
  <verify>
    Run: `grep -rni "forge" core/references/runtime-adapters.md core/workflows/generate-system.md` -- must return zero results.
    Run: `grep -rni "design forge\|/forge:\|FORGE_ROOT\|forgeRoot\|forge-" core/ runtimes/ bin/` -- must return zero results.
  </verify>
  <done>runtime-adapters.md line 23 shows motif-*.md agent filenames. generate-system.md line 153 shows MOTIF instead of DESIGN FORGE. Full grep sweep of shipped directories returns zero forge references.</done>
</task>

<task type="auto">
  <name>Task 2: Fix stale forge references in non-shipped files (docs/BUILD-SPEC.md, test/e2e-installer.js)</name>
  <files>docs/BUILD-SPEC.md, test/e2e-installer.js</files>
  <action>
    **docs/BUILD-SPEC.md -- full rebrand (14+ references):**
    Apply the same replacement map from 04-RESEARCH.md to this file. Specific changes:
    - Line 1: "Design Forge" -> "Motif"
    - Line 3: "Design Forge" -> "Motif"
    - Line 43: "commands/forge/init.md" -> "commands/motif/init.md"
    - Line 44: "commands/forge/*.md" -> "commands/motif/*.md"
    - Lines 46-50: "forge-researcher" -> "motif-researcher", "forge-system-architect" -> "motif-system-architect", "forge-screen-composer" -> "motif-screen-composer", "forge-design-reviewer" -> "motif-design-reviewer", "forge-fix-agent" -> "motif-fix-agent"
    - Lines 51-54: "forge-token-check" -> "motif-token-check", "forge-font-check" -> "motif-font-check", "forge-aria-check" -> "motif-aria-check", "forge-context-monitor" -> "motif-context-monitor"
    - Line 82: "/forge:" commands -> "/motif:" commands
    - Also update status markers where applicable (agents are now BUILT, installer is BUILT, etc.) but ONLY if you can confirm the file exists on disk. The build spec is a docs file -- accuracy matters but shipping does not depend on it.

    **test/e2e-installer.js -- 3 targeted fixes:**

    1. **Line 112:** Change the expected agent filename from stale `forge-researcher.md` to correct `motif-researcher.md`:
       ```javascript
       // OLD:
       '.claude/get-motif/agents/forge-researcher.md',
       // NEW:
       '.claude/get-motif/agents/motif-researcher.md',
       ```

    2. **Lines 136-143:** Rename stale variable names and string references. The variables `forgeRootMatches1`, `forgeRootMatches2`, `allForgeRootMatches` should be renamed to `motifRootMatches1`, `motifRootMatches2`, `allMotifRootMatches`. The grep search string `'{FORGE_ROOT}'` should become `'{MOTIF_ROOT}'` since the actual template variable was renamed in 04-01. Update the corresponding assert message and console.log accordingly.

    3. **Lines 146-151:** Similarly rename `oldPathMatches1`, `oldPathMatches2`, `allOldPathMatches` -- these search for `.claude/get-design-forge` which is fine to keep as a regression check (it SHOULD still find zero results). Variable names are technically fine here, but update the comment from "Grep for .claude/get-design-forge" to clarify this is a regression check for old paths. No functional change needed for the path matchers themselves.

    4. **Lines 198-201:** Update comments to reference MOTIF_ROOT instead of FORGE_ROOT. The `.replaceAll('{FORGE_ROOT}', ...)` line should become `.replaceAll('{MOTIF_ROOT}', ...)` since the actual snippet uses {MOTIF_ROOT} now. The `.replaceAll('.claude/get-design-forge', ...)` line can stay as a safety fallback or be removed -- use your judgment.
  </action>
  <verify>
    Run: `grep -n "forge-researcher" test/e2e-installer.js` -- must return zero results.
    Run: `grep -ni "design forge" docs/BUILD-SPEC.md` -- must return zero results.
    Run: `grep -ni "/forge:" docs/BUILD-SPEC.md` -- must return zero results.
    Run: `node test/e2e-installer.js` -- all 7 tests should pass (if installer is working correctly).
  </verify>
  <done>test/e2e-installer.js expects motif-researcher.md (matching actual file on disk), uses MOTIF_ROOT variable names consistently. docs/BUILD-SPEC.md uses Motif branding throughout. E2e test suite passes all 7 tests.</done>
</task>

</tasks>

<verification>
After both tasks complete, run the full verification:

1. **Shipped files sweep:**
   ```bash
   grep -rni "design forge\|/forge:\|design-forge\|FORGE_ROOT\|forgeRoot\|commands/forge\|forge-researcher\|forge-system\|forge-screen\|forge-design\|forge-fix" core/ runtimes/ bin/
   ```
   Must return zero results.

2. **Non-shipped files check:**
   ```bash
   grep -ni "forge-researcher\|forge-system-architect\|forge-screen-composer\|forge-design-reviewer\|forge-fix-agent" docs/BUILD-SPEC.md test/e2e-installer.js
   ```
   Must return zero results.

3. **E2e test run:**
   ```bash
   node test/e2e-installer.js
   ```
   All 7 tests pass (7/7).

4. **Installer dry-run still works:**
   ```bash
   node bin/install.js --dry-run
   ```
   Completes without error.
</verification>

<success_criteria>
- UAT Test 3 now passes: zero old-brand references in shipped files
- E2e test suite passes 7/7 (no broken forge-researcher.md assertion)
- docs/BUILD-SPEC.md is consistent with current Motif branding
- Installer dry-run still works (no regressions from edits)
</success_criteria>

<output>
After completion, create `.planning/phases/04-rebrand-and-distribution/04-04-SUMMARY.md`
</output>

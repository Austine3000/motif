---
phase: 18-new-verticals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/get-motif/references/verticals/social.md
  - .claude/get-motif/references/verticals/education.md
autonomous: true

must_haves:
  truths:
    - "User can select 'social' as their vertical and receive Social-specific palettes, typography, components, spacing, and interaction patterns"
    - "User can select 'education' as their vertical and receive Education-specific palettes, typography, components, spacing, and interaction patterns"
    - "Social vertical includes icon vocabulary mapped across all 4 icon libraries"
    - "Education vertical includes icon vocabulary mapped across all 4 icon libraries"
    - "Social vertical includes domain-specific empty, error, and loading state patterns"
    - "Education vertical includes domain-specific empty, error, and loading state patterns"
  artifacts:
    - path: ".claude/get-motif/references/verticals/social.md"
      provides: "Social vertical design intelligence"
      contains: "# Social Design Intelligence"
    - path: ".claude/get-motif/references/verticals/education.md"
      provides: "Education vertical design intelligence"
      contains: "# Education Design Intelligence"
  key_links:
    - from: ".claude/get-motif/references/verticals/social.md"
      to: "generate-system.md"
      via: "file path convention loading"
      pattern: "## Icon Vocabulary"
    - from: ".claude/get-motif/references/verticals/education.md"
      to: "generate-system.md"
      via: "file path convention loading"
      pattern: "## Icon Vocabulary"
---

<objective>
Author Social and Education vertical reference files with full design intelligence.

Purpose: Enable users to generate domain-intelligent designs for Social and Education projects with the same quality as existing verticals (fintech, health, saas, ecommerce).
Output: Two complete vertical markdown files following the exact template structure of existing verticals.
</objective>

<execution_context>
@/Users/austineogbuanya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austineogbuanya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-new-verticals/18-RESEARCH.md
@.claude/get-motif/references/verticals/fintech.md (template exemplar -- match this structure exactly)
@.claude/get-motif/references/verticals/saas.md (second exemplar for cross-reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Social vertical reference file</name>
  <files>.claude/get-motif/references/verticals/social.md</files>
  <action>
Create `.claude/get-motif/references/verticals/social.md` following the EXACT section heading structure of fintech.md. Use 18-RESEARCH.md "Social Vertical" section for domain-specific content.

Required sections (in this exact order with these exact headings):
1. `# Social Design Intelligence`
2. `## Core Design Principle` -- "Connection is the product." Bold one-liner + 2-3 sentence expansion
3. `## Navigation Patterns` with `### Standard Models` and `### Vertical-Specific Rules`
4. `## Color System` with:
   - `### Palette A: Vibrant Blue-Violet (Community)` -- full primary-50 through primary-900 + surface + text tokens, BOTH light and dark mode hex values, contrast ratios annotated
   - `### Palette B: Warm Coral (Expressive)` -- same completeness
   - `### Semantic Colors` -- Success=posted, Error=failed to send, Warning=reported content, Info=new follower
   - `### Color Anti-Patterns` -- 3-4 anti-patterns (e.g., "Never use red for likes/hearts")
5. `## Typography` with:
   - `### Pairing A: General Sans + DM Sans` -- Display 700/Body 400 with specimen table
   - `### Pairing B: Plus Jakarta Sans` -- Display 700/Body 400
   - No Data & Mono subsection (social doesn't need it per research)
   - `### Type Scale` -- 7 tokens (text-xs through text-3xl) with social-specific usage descriptions
   - `### Typography Rules` -- 3-4 rules
   - `### Typography Anti-Patterns` -- 3-4 anti-patterns
6. `## Spacing & Density` with `### Recommended Density: Comfortable` and `### Concrete Values`
7. `## Component Specifications` -- 3 components in XML format:
   - FeedPost (content card with author avatar, text/media, engagement actions)
   - StoryBubble (circular avatar with gradient ring)
   - MessageBubble (chat message with sender alignment, timestamps, read receipts)
   Each uses `<component>`, `<description>`, `<structure>`, `<dimensions>`, `<states>` XML tags
8. `## Interaction Patterns` with `### Core Flows`, `### States` (Loading/Empty/Error with social-specific copy), `### Motion`
9. `## Accessibility Specifics` -- social-specific (e.g., alt text for user-uploaded images, screen reader for engagement counts)
10. `## Border Radius` -- rounded, social-friendly (12-16px cards, full-round avatars)
11. `## Shadow Style` -- subtle, elevated cards
12. `## Icon Vocabulary` with HTML verification comment, then 4 category tables:
    - Navigation (5 icons), Social & Communication (6-8 icons), Status & Feedback (4-5 icons), Actions (5-6 icons)
    - Each table has 4 columns: Concept, Lucide, Phosphor, Material Symbols, Tabler
    - Primary library: Phosphor Icons. Use accurate icon names per each library's conventions.
    - Add HTML comment: `<!-- Icon names verified against: Lucide v0.400+, Phosphor Icons v2.1+, Material Symbols, Tabler Icons v3.0+ -->`
13. `## Social-Specific Additions` -- 4-7 bullet points for domain patterns

Target length: ~270-300 lines (matching existing verticals). Do NOT pad with filler. Every line must provide actionable design intelligence.

CRITICAL: Icon names MUST use each library's actual naming convention:
- Lucide: kebab-case (e.g., `message-circle`, `heart`, `share-2`)
- Phosphor: PascalCase with prefix concept (e.g., `ChatCircle`, `Heart`, `ShareNetwork`)
- Material Symbols: snake_case (e.g., `chat_bubble`, `favorite`, `share`)
- Tabler: kebab-case with `icon-` prefix convention (e.g., `message-circle`, `heart`, `share`)
  </action>
  <verify>
Run: `grep "^##" .claude/get-motif/references/verticals/social.md` and compare against `grep "^##" .claude/get-motif/references/verticals/fintech.md` -- all required section headings must be present in the same order. Run: `wc -l .claude/get-motif/references/verticals/social.md` -- should be 250-320 lines.
  </verify>
  <done>social.md exists with all 14 required sections, both palettes have light+dark mode values, 3 XML component specs, icon vocabulary with 4 categories mapped across 4 libraries, and domain-specific empty/error/loading states.</done>
</task>

<task type="auto">
  <name>Task 2: Author Education vertical reference file</name>
  <files>.claude/get-motif/references/verticals/education.md</files>
  <action>
Create `.claude/get-motif/references/verticals/education.md` following the EXACT section heading structure of fintech.md. Use 18-RESEARCH.md "Education Vertical" section for domain-specific content.

Required sections (in this exact order with these exact headings):
1. `# Education Design Intelligence`
2. `## Core Design Principle` -- "Clarity enables learning." Bold one-liner + 2-3 sentence expansion
3. `## Navigation Patterns` with `### Standard Models` and `### Vertical-Specific Rules`
4. `## Color System` with:
   - `### Palette A: Deep Teal (Knowledge)` -- full primary-50 through primary-900 + surface + text tokens, BOTH light and dark mode hex values, contrast ratios annotated
   - `### Palette B: Warm Amber (Achievement)` -- same completeness
   - `### Semantic Colors` -- Success=completed/passed, Error=failed/overdue, Warning=due soon, Info=new content
   - `### Color Anti-Patterns`
5. `## Typography` with:
   - `### Pairing A: Outfit + Source Sans 3` -- Display 700/Body 400
   - `### Pairing B: Fraunces + Nunito` -- warm authority + friendly body
   - `### Data & Mono` -- for code snippets in technical courses (JetBrains Mono or Fira Code)
   - `### Type Scale`, `### Typography Rules`, `### Typography Anti-Patterns`
6. `## Spacing & Density` with `### Recommended Density: Comfortable` and `### Concrete Values`
7. `## Component Specifications` -- 3 components in XML format:
   - CourseCard (course image, title, instructor, progress bar, enrollment count)
   - LessonProgress (vertical stepper/timeline for module completion)
   - QuizQuestion (question text, answer options, submit, result feedback)
8. `## Interaction Patterns` with `### Core Flows`, `### States` (education-specific empty/error/loading), `### Motion`
9. `## Accessibility Specifics` -- education-specific (e.g., captions for video content, keyboard nav for quizzes)
10. `## Border Radius` -- friendly but structured (8-12px)
11. `## Shadow Style`
12. `## Icon Vocabulary` with HTML verification comment, then 4 category tables:
    - Navigation (5 icons), Education & Learning (6-8 icons), Status & Feedback (4-5 icons), Actions (5-6 icons)
    - Primary library: Material Symbols (Rounded). Same 4-column table format.
    - Add HTML comment: `<!-- Icon names verified against: Lucide v0.400+, Phosphor Icons v2.1+, Material Symbols, Tabler Icons v3.0+ -->`
13. `## Education-Specific Additions` -- 4-7 bullet points

Same conventions as Task 1 for icon names, target length, XML format, palette completeness.
  </action>
  <verify>
Run: `grep "^##" .claude/get-motif/references/verticals/education.md` and compare against `grep "^##" .claude/get-motif/references/verticals/fintech.md` -- all required section headings must be present. Run: `wc -l .claude/get-motif/references/verticals/education.md` -- should be 250-320 lines.
  </verify>
  <done>education.md exists with all 14 required sections, both palettes have light+dark mode values, 3 XML component specs, icon vocabulary with 4 categories mapped across 4 libraries, and domain-specific empty/error/loading states.</done>
</task>

</tasks>

<verification>
1. Both files exist at correct paths under `.claude/get-motif/references/verticals/`
2. Section heading diff shows structural parity with existing verticals:
   ```bash
   diff <(grep "^## " .claude/get-motif/references/verticals/fintech.md | sed 's/:.*//' ) <(grep "^## " .claude/get-motif/references/verticals/social.md | sed 's/:.*//')
   ```
3. Each file contains `## Icon Vocabulary` with an HTML verification comment
4. Each file contains exactly 3 `<component>` XML blocks
5. Each file contains `### States` subsection under `## Interaction Patterns` with empty/error/loading patterns
</verification>

<success_criteria>
- social.md and education.md exist with correct template structure
- Both have complete color palettes (light + dark mode) with contrast ratios
- Both have 3 XML component specifications
- Both have icon vocabulary tables with 4 categories across 4 icon libraries
- Both have domain-specific empty/error/loading state patterns
- Line counts are 250-320 lines (matching existing vertical depth)
</success_criteria>

<output>
After completion, create `.planning/phases/18-new-verticals/18-01-SUMMARY.md`
</output>

---
phase: 08.1-pre-publish-integration-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/workflows/research.md
  - core/workflows/generate-system.md
  - runtimes/claude-code/agents/motif-researcher.md
  - runtimes/claude-code/agents/motif-system-architect.md
  - .github/workflows/publish.yml
  - bin/install.js
  - core/references/runtime-adapters.md
  - runtimes/claude-code/commands/motif/init.md
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Vertical intelligence loads successfully when research workflow checks for vertical reference file"
    - "CI publish workflow runs without action version errors on GitHub Actions"
    - "All user-facing help text and documentation references the correct package name motif-design"
    - "init.md does not instruct agents to read a file that does not exist in the installed file tree"
  artifacts:
    - path: "core/workflows/research.md"
      provides: "Research orchestrator with correct vertical paths"
      contains: "{MOTIF_ROOT}/references/verticals/"
    - path: "core/workflows/generate-system.md"
      provides: "System generator orchestrator with correct vertical paths"
      contains: "{MOTIF_ROOT}/references/verticals/"
    - path: "runtimes/claude-code/agents/motif-researcher.md"
      provides: "Researcher agent with correct vertical path in context profile"
      contains: ".claude/get-motif/references/verticals/"
    - path: "runtimes/claude-code/agents/motif-system-architect.md"
      provides: "System architect agent with correct vertical path in context profile"
      contains: ".claude/get-motif/references/verticals/"
    - path: ".github/workflows/publish.yml"
      provides: "CI workflow with valid GitHub Action versions"
      contains: "@v4"
    - path: "bin/install.js"
      provides: "Installer with correct npx motif-design@latest help text"
      contains: "npx motif-design@latest"
    - path: "core/references/runtime-adapters.md"
      provides: "Runtime adapters doc with correct package name"
      contains: "npx motif-design@latest"
    - path: "runtimes/claude-code/commands/motif/init.md"
      provides: "Init command without reference to missing CLAUDE-MD-SNIPPET.md file"
  key_links:
    - from: "core/workflows/research.md"
      to: "core/references/verticals/{vertical}.md"
      via: "{MOTIF_ROOT}/references/verticals/ path resolution"
      pattern: "MOTIF_ROOT./references/verticals/"
    - from: "core/workflows/generate-system.md"
      to: "core/references/verticals/{vertical}.md"
      via: "{MOTIF_ROOT}/references/verticals/ path resolution"
      pattern: "MOTIF_ROOT./references/verticals/"
    - from: ".github/workflows/publish.yml"
      to: "GitHub Actions marketplace"
      via: "actions/checkout@v4 and actions/setup-node@v4"
      pattern: "@v4"
---

<objective>
Fix all cross-phase integration gaps found by the v1.0 milestone audit so that vertical intelligence loads correctly, CI workflow runs, and package references are consistent.

Purpose: The milestone audit found 4 integration gaps (1 critical, 1 high, 1 medium, 1 low) that would cause silent failures in production: broken vertical paths make the core design intelligence feature non-functional, broken CI action versions prevent automated publishing, stale package name references confuse users, and a missing file reference in init.md sends agents looking for a non-existent file.

Output: 8 files corrected with verified path, version, and name consistency.
</objective>

<execution_context>
@/Users/austineogbuanya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austineogbuanya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix vertical path references and CI action versions (Critical + High gaps)</name>
  <files>
    core/workflows/research.md
    core/workflows/generate-system.md
    runtimes/claude-code/agents/motif-researcher.md
    runtimes/claude-code/agents/motif-system-architect.md
    .github/workflows/publish.yml
  </files>
  <action>
Fix the vertical path mismatch in 4 files. Verticals are installed at `.claude/get-motif/references/verticals/` but workflows and agents look for them without the `references/` segment.

**core/workflows/research.md** — 3 replacements:
- Line 36: `{MOTIF_ROOT}/verticals/{VERTICAL}.md` → `{MOTIF_ROOT}/references/verticals/{VERTICAL}.md`
- Line 57: `{MOTIF_ROOT}/verticals/{VERTICAL}.md` → `{MOTIF_ROOT}/references/verticals/{VERTICAL}.md`
- Line 89: `{MOTIF_ROOT}/verticals/{VERTICAL}.md` → `{MOTIF_ROOT}/references/verticals/{VERTICAL}.md`

**core/workflows/generate-system.md** — 2 replacements:
- Line 35: `{MOTIF_ROOT}/verticals/{VERTICAL}.md` → `{MOTIF_ROOT}/references/verticals/{VERTICAL}.md`
- Line 54: `{MOTIF_ROOT}/verticals/{VERTICAL}.md` → `{MOTIF_ROOT}/references/verticals/{VERTICAL}.md`

**runtimes/claude-code/agents/motif-researcher.md** — 1 replacement:
- Line 50: `.claude/get-motif/verticals/{vertical}.md` → `.claude/get-motif/references/verticals/{vertical}.md`

**runtimes/claude-code/agents/motif-system-architect.md** — 1 replacement:
- Line 52: `.claude/get-motif/verticals/{vertical}.md` → `.claude/get-motif/references/verticals/{vertical}.md`

Fix the CI workflow action versions in 1 file:

**.github/workflows/publish.yml** — 2 replacements:
- Line 20: `actions/checkout@v6` → `actions/checkout@v4`
- Line 23: `actions/setup-node@v6` → `actions/setup-node@v4`

Use Edit tool for each file. Do NOT change any other content in these files.
  </action>
  <verify>
Run these checks:

```bash
# Verify zero remaining wrong vertical paths in workflows and agents
grep -rn 'MOTIF_ROOT}/verticals/' core/workflows/ runtimes/claude-code/agents/ | grep -v 'references/verticals/' && echo "FAIL: stale vertical paths remain" || echo "PASS: all vertical paths corrected"

# Verify correct paths exist
grep -c 'references/verticals/' core/workflows/research.md  # expect 3
grep -c 'references/verticals/' core/workflows/generate-system.md  # expect 2
grep -c 'references/verticals/' runtimes/claude-code/agents/motif-researcher.md  # expect 1
grep -c 'references/verticals/' runtimes/claude-code/agents/motif-system-architect.md  # expect 1

# Verify CI action versions
grep -n '@v6' .github/workflows/publish.yml && echo "FAIL: @v6 still present" || echo "PASS: no @v6 references"
grep -c '@v4' .github/workflows/publish.yml  # expect 2
```
  </verify>
  <done>
All 7 vertical path references across 4 files point to `references/verticals/` (matching the installer copy mapping). Both GitHub Action versions in publish.yml are `@v4`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix stale package name references and init.md missing snippet reference (Medium + Low gaps)</name>
  <files>
    bin/install.js
    core/references/runtime-adapters.md
    runtimes/claude-code/commands/motif/init.md
  </files>
  <action>
Fix stale `npx motif@latest` references in 2 files:

**bin/install.js** — 5 replacements in the `printHelp()` function (lines 31, 41, 42, 43, 44):
- All instances of `npx motif@latest` → `npx motif-design@latest`
- Specifically: the USAGE line, and the 4 EXAMPLES lines (including `--force` example on line 44)

**core/references/runtime-adapters.md** — 5 replacements in the Installer Behavior section (lines 90-94):
- All instances of `npx motif@latest` → `npx motif-design@latest`

Fix the init.md reference to a non-existent file:

**runtimes/claude-code/commands/motif/init.md** — Replace lines 209-213 (the "### Inject CLAUDE.md Rules" subsection under Post-Generation). Preserve the `## Post-Generation` header at line 207. The installer already handles CLAUDE.md injection via sentinel markers during `npx motif-design@latest`, so this subsection should not instruct agents to do it again (and especially not by reading a file that does not exist at the expected path). Replace the subsection with:

```markdown
### CLAUDE.md Rules

The installer handles CLAUDE.md injection automatically via sentinel markers during `npx motif-design@latest`. No manual injection is needed here. If CLAUDE.md is missing Motif rules, re-run the installer.
```

Use Edit tool for each file. Do NOT change any other content in these files.
  </action>
  <verify>
Run these checks:

```bash
# Verify zero remaining stale package name in shipped files
grep -rn 'npx motif@latest' bin/ core/ runtimes/ && echo "FAIL: stale npx motif@latest remains" || echo "PASS: all references updated to motif-design"

# Verify correct name exists
grep -c 'npx motif-design@latest' bin/install.js  # expect 5
grep -c 'npx motif-design@latest' core/references/runtime-adapters.md  # expect 5

# Verify init.md no longer references CLAUDE-MD-SNIPPET.md
grep -n 'CLAUDE-MD-SNIPPET' runtimes/claude-code/commands/motif/init.md && echo "FAIL: snippet reference remains" || echo "PASS: snippet reference removed"

# Verify init.md still has a CLAUDE.md section (replacement, not deletion)
grep -n 'CLAUDE.md' runtimes/claude-code/commands/motif/init.md | head -5
```
  </verify>
  <done>
Zero `npx motif@latest` references remain in any shipped file (bin/, core/, runtimes/). init.md Post-Generation section explains that the installer handles CLAUDE.md injection and does not reference `templates/CLAUDE-MD-SNIPPET.md`.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run the full integration check that mirrors the milestone audit gaps:

```bash
echo "=== Gap 1: Vertical Path Mismatch ==="
# Should find ZERO lines without 'references/' in the path
grep -rn '{MOTIF_ROOT}/verticals/' core/workflows/ runtimes/claude-code/agents/ | grep -v 'references/verticals/' | wc -l
# Should find ZERO lines with wrong agent path
grep -rn 'get-motif/verticals/' runtimes/claude-code/agents/ | grep -v 'references/verticals/' | wc -l

echo "=== Gap 2: CI Action Versions ==="
grep '@v' .github/workflows/publish.yml

echo "=== Gap 3: Stale Package Name ==="
grep -rn 'npx motif@latest' bin/ core/ runtimes/ | wc -l  # expect 0

echo "=== Gap 4: Missing Snippet Reference ==="
grep -c 'CLAUDE-MD-SNIPPET' runtimes/claude-code/commands/motif/init.md  # expect 0
```

All four gap checks must return zero/clean results.
</verification>

<success_criteria>
1. `grep -rn '{MOTIF_ROOT}/verticals/' core/ runtimes/ | grep -v 'references/verticals/'` returns zero matches
2. `.github/workflows/publish.yml` contains only `@v4` action version tags
3. `grep -rn 'npx motif@latest' bin/ core/ runtimes/` returns zero matches
4. `grep 'CLAUDE-MD-SNIPPET' runtimes/claude-code/commands/motif/init.md` returns zero matches
5. No other content in any file has been modified beyond the targeted fixes
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-pre-publish-integration-fixes/08.1-01-SUMMARY.md`
</output>

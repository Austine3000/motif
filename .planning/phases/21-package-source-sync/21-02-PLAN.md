---
phase: 21-package-source-sync
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - test/e2e-installer.js
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "E2E test verifies motif-state.js is installed to .claude/get-motif/scripts/"
    - "E2E test verifies motif-session-start.js is installed to .claude/get-motif/hooks/"
    - "E2E test verifies motif-context-monitor.js has 100+ lines (rich display version)"
    - "E2E test verifies CLAUDE.md contains State Awareness section"
    - "E2E test verifies settings.json has SessionStart hook registration"
    - "E2E test verifies icon-libraries.md contains all 8 verticals"
  artifacts:
    - path: "test/e2e-installer.js"
      provides: "Fresh install verification covering all v1.3 artifacts"
      contains: "SessionStart"
  key_links:
    - from: "test/e2e-installer.js"
      to: "bin/commands/init.js"
      via: "Runs init in temp dir and asserts output"
      pattern: "motif-state\\.js|motif-session-start\\.js|State Awareness|SessionStart"
---

<objective>
Add E2E test assertions that verify a fresh install gets all v1.3 artifacts.

Purpose: Prevent future regressions where package source falls out of sync with installed artifacts. The existing E2E test covers file existence and manifest integrity but does not check Phase 17/18 artifacts (SessionStart hooks, state recovery, rich status display, State Awareness CLAUDE.md rule, 8-vertical icon library).

Output: Updated test/e2e-installer.js with 6 new assertions covering all gap closure items.
</objective>

<execution_context>
@/Users/austineogbuanya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austineogbuanya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-package-source-sync/21-RESEARCH.md
@.planning/phases/21-package-source-sync/21-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add v1.3 artifact assertions to E2E installer test</name>
  <files>test/e2e-installer.js</files>
  <action>
Read the existing `test/e2e-installer.js` (~619 lines) to understand the test structure and find the right insertion point for new assertions.

Add a new test section (following the existing naming pattern) titled "Phase 21: Package Source Sync Verification" or similar. Insert it after the existing file-existence and hook-registration assertions. Add these 6 assertions:

1. **motif-state.js installed:** Assert `.claude/get-motif/scripts/motif-state.js` exists in the temp install directory. Verify it contains the `cmdRecover` function (proving it is the full version, not a stub).

2. **motif-session-start.js installed:** Assert `.claude/get-motif/hooks/motif-session-start.js` exists. Verify it contains `hookEventName` (proving it is the real hook).

3. **motif-context-monitor.js is rich version:** Assert `.claude/get-motif/hooks/motif-context-monitor.js` exists and has 100+ lines (the 40-line stale version would fail this).

4. **CLAUDE.md has State Awareness:** Read the installed CLAUDE.md and assert it contains the string "State Awareness" between the MOTIF-START/MOTIF-END markers.

5. **settings.json has SessionStart hooks:** Parse `.claude/settings.json` as JSON and assert `settings.hooks.SessionStart` exists, contains an entry with matcher `startup|resume|clear|compact`, and that entry has a hook command containing `motif-session-start`.

6. **icon-libraries.md has all 8 verticals:** Read `.claude/get-motif/references/icon-libraries.md` and assert it contains all 8 vertical names: Fintech, Health, SaaS, E-commerce, Social, Education, Marketplace, DevTools.

Follow the existing assertion style in the file (likely uses `assert()` or a custom assertion helper). Match the console.log formatting for test output.

IMPORTANT: Do not modify or break any existing assertions. These are additive only.
  </action>
  <verify>
Run the full E2E test suite:
```bash
npm test 2>&1 | tail -30
```

Verify the new assertions appear in test output and pass. If the E2E test runs the full install flow, all 6 new assertions should pass given Plan 21-01 synced the source files.
  </verify>
  <done>E2E test includes 6 new assertions covering motif-state.js, motif-session-start.js, motif-context-monitor.js line count, CLAUDE.md State Awareness, settings.json SessionStart hooks, and icon-libraries.md 8 verticals. All tests pass including new and existing assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite and verify doctor passes</name>
  <files></files>
  <action>
Run the complete test suite to verify no regressions:

```bash
npm test
```

Then run `motif doctor` locally to verify it passes all checks including SessionStart hook verification:

```bash
node bin/cli.js doctor
```

If doctor reports any failures related to the gap closure items (SessionStart hooks, file integrity), diagnose and fix. The fix would be in 21-01 artifacts (source files) not in the test file.

If all checks pass, the phase is complete.
  </action>
  <verify>
```bash
npm test 2>&1 | grep -E "(pass|fail|error)" -i
node bin/cli.js doctor 2>&1
```

Both commands should report all-pass with no failures.
  </verify>
  <done>Full test suite passes. motif doctor passes all checks. No regressions from gap closure changes.</done>
</task>

</tasks>

<verification>
1. `npm test` passes including all new v1.3 assertions
2. `node bin/cli.js doctor` passes all checks
3. No existing test regressions
</verification>

<success_criteria>
- test/e2e-installer.js contains assertions for all 6 gap closure items
- npm test passes with 0 failures
- motif doctor passes all checks including SessionStart
</success_criteria>

<output>
After completion, create `.planning/phases/21-package-source-sync/21-02-SUMMARY.md`
</output>

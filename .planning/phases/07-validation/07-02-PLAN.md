---
phase: 07-validation
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - test/validation/fixtures/controlled/setup.sh
autonomous: false

must_haves:
  truths:
    - "Full Motif workflow (init through fix) completes without errors on a controlled test project"
    - "All expected artifacts exist after workflow completion: PROJECT.md, DESIGN-BRIEF.md, STATE.md, DESIGN-RESEARCH.md, research files, tokens.css, COMPONENT-SPECS.md, DESIGN-SYSTEM.md, token-showcase.html, screen SUMMARYs, and REVIEW files"
    - "Generated tokens.css passes quality checks: no banned fonts, primary-500 exists, WCAG annotations present"
  artifacts:
    - path: "test/validation/fixtures/controlled/setup.sh"
      provides: "Automated setup script for controlled test project"
      min_lines: 20
  key_links:
    - from: "test/validation/validate-workflow.js"
      to: "controlled test project"
      via: "CLI invocation with project dir"
      pattern: "node test/validation/validate-workflow.js"
    - from: "test/validation/validate-tokens.js"
      to: "controlled test tokens.css"
      via: "CLI invocation with tokens.css path"
      pattern: "node test/validation/validate-tokens.js"
---

<objective>
Validate that the complete Motif workflow (init, research, system, compose, review, fix) works end-to-end on a controlled test project with known inputs and automated verification (VALD-01).

Purpose: Proves the integrated system works -- all commands chain correctly, subagents receive proper context, gate checks pass, and output artifacts meet quality standards.
Output: Confirmed VALD-01 pass with validation script results.
</objective>

<execution_context>
@/Users/austineogbuanya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austineogbuanya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-validation/07-RESEARCH.md
@.planning/phases/07-validation/07-01-SUMMARY.md
@runtimes/claude-code/commands/motif/init.md
@core/references/state-machine.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create controlled test project setup script</name>
  <files>test/validation/fixtures/controlled/setup.sh</files>
  <action>
Create `test/validation/fixtures/controlled/setup.sh` -- a bash script that prepares an isolated controlled test project directory in /tmp for Motif validation.

The script must:
1. Set -euo pipefail.
2. Create a temp directory: `TESTDIR=$(mktemp -d /tmp/motif-validation-controlled-XXXXXX)`.
3. Print the directory path.
4. Create `.claude/` directory inside it (simulates Claude Code project).
5. Run the Motif installer: `node "$(dirname "$0")/../../../../bin/install.js"` from within the test directory (installs Motif files).
6. Verify installation succeeded by checking `.motif-manifest.json` exists.
7. Print instructions for the next step:
   ```
   Controlled test project ready at: $TESTDIR

   Next: cd into the project and run the full Motif workflow:

   1. /motif:init --auto --vertical fintech --stack react --theme light --density comfortable
   2. /motif:research
   3. /motif:system
   4. /motif:compose login
   5. /motif:compose dashboard
   6. /motif:compose settings
   7. /motif:review login
   8. /motif:review dashboard
   9. /motif:review settings
   10. /motif:fix (if any review finds critical issues)

   After workflow completes, run verification:
     node test/validation/validate-workflow.js $TESTDIR --screens login,dashboard,settings
     node test/validation/validate-tokens.js $TESTDIR/.planning/design/system/tokens.css
   ```

Make the script executable. This is a setup helper -- the actual workflow execution is manual.
  </action>
  <verify>Run `bash test/validation/fixtures/controlled/setup.sh` -- should create temp directory, install Motif, print instructions. Check that `.motif-manifest.json` exists in the created directory.</verify>
  <done>Setup script creates an isolated controlled test project with Motif installed and prints clear instructions for manual workflow execution (including review of ALL three screens) and automated verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify controlled test workflow completion (VALD-01)</name>
  <files>none (verification only)</files>
  <action>
Human runs the full Motif workflow in a NEW Claude Code session on the controlled test project, then runs automated verification scripts.

What was built: Controlled test project setup script and validation tooling (Plan 01 scripts + Task 1 setup script).

Steps for verification:
1. Run the setup script:
   bash test/validation/fixtures/controlled/setup.sh
   Note the TESTDIR path printed.

2. Open a NEW Claude Code session in the TESTDIR and run the full workflow in sequence:
   /motif:init --auto --vertical fintech --stack react --theme light --density comfortable
   /motif:research
   /motif:system
   /motif:compose login
   /motif:compose dashboard
   /motif:compose settings
   /motif:review login
   /motif:review dashboard
   /motif:review settings
   /motif:fix  (if any review finds critical issues)

3. After all commands complete, return to THIS session and run verification from the Motif project root:
   node test/validation/validate-workflow.js $TESTDIR --screens login,dashboard,settings
   node test/validation/validate-tokens.js $TESTDIR/.planning/design/system/tokens.css

4. Expected: Both scripts exit 0 with all checks passing.
   - validate-workflow.js: All init/research/system/compose/review artifacts exist for ALL three screens (login, dashboard, settings)
   - validate-tokens.js: No banned fonts, primary-500 exists, WCAG annotations present, 20+ tokens

5. If any checks fail, report the specific failures -- they indicate workflow gaps to fix.
  </action>
  <verify>validate-workflow.js exits 0 (all artifacts present including reviews/login-REVIEW.md, reviews/dashboard-REVIEW.md, reviews/settings-REVIEW.md). validate-tokens.js exits 0 (token quality passes).</verify>
  <done>VALD-01 confirmed: Full Motif workflow completes without errors on controlled test project, all artifacts exist (including review artifacts for all three screens), tokens pass quality checks.</done>
</task>

</tasks>

<verification>
VALD-01 requirement:
- Full workflow (init, research, system, compose, review, fix) completes without errors on a controlled test project
- All expected artifacts exist for all screens (checked by validate-workflow.js with --screens login,dashboard,settings)
- Review artifacts exist for ALL three screens (login, dashboard, settings) -- not just one
- Token quality passes (checked by validate-tokens.js)
</verification>

<success_criteria>
VALD-01 passes: validate-workflow.js and validate-tokens.js both exit 0 when run against the controlled test project after full workflow execution. Specifically, reviews/{screen}-REVIEW.md must exist for login, dashboard, AND settings.
</success_criteria>

<output>
After completion, create `.planning/phases/07-validation/07-02-SUMMARY.md`
</output>

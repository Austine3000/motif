---
phase: 07-validation
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - test/validation/fixtures/cryptopay/setup.sh
  - test/validation/run-all-validations.sh
autonomous: false

must_haves:
  truths:
    - "Full Motif workflow completes on CryptoPay (real fintech project) producing usable design artifacts"
    - "Running the same vertical with two different differentiation seeds produces visibly distinct color palettes, typography choices, and component styling"
    - "User-specified brand colors appear in generated tokens without being overridden or replaced by the system's palette generation"
    - "Screen quality (token compliance, design coherence) remains consistent across 5+ screens composed in sequence"
  artifacts:
    - path: "test/validation/fixtures/cryptopay/setup.sh"
      provides: "CryptoPay test project setup with brand color constraint"
      min_lines: 25
    - path: "test/validation/run-all-validations.sh"
      provides: "Master script to run all verification checks against completed test projects"
      min_lines: 30
  key_links:
    - from: "test/validation/validate-diff.js"
      to: "two tokens.css files from different seeds"
      via: "CLI invocation comparing tokensA and tokensB"
      pattern: "node test/validation/validate-diff.js"
    - from: "test/validation/validate-tokens.js"
      to: "CryptoPay tokens.css with --brand-color flag"
      via: "brand color preservation check"
      pattern: "--brand-color"
---

<objective>
Validate CryptoPay battle test (VALD-02), differentiation (VALD-03), brand color preservation (VALD-04), and screen consistency across 5+ screens (VALD-05).

Purpose: Proves that Motif works on a real project, produces genuinely differentiated designs, preserves user brand constraints, and maintains quality across many screens via fresh context isolation.
Output: Confirmed VALD-02 through VALD-05 pass with validation script results.
</objective>

<execution_context>
@/Users/austineogbuanya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austineogbuanya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-validation/07-RESEARCH.md
@.planning/phases/07-validation/07-01-SUMMARY.md
@core/references/design-inputs.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CryptoPay setup script and master validation runner</name>
  <files>
test/validation/fixtures/cryptopay/setup.sh
test/validation/run-all-validations.sh
  </files>
  <action>
Create two scripts:

**1. `test/validation/fixtures/cryptopay/setup.sh`** -- prepares a CryptoPay test project:
- Set -euo pipefail.
- Create temp dir: `TESTDIR=$(mktemp -d /tmp/motif-validation-cryptopay-XXXXXX)`.
- Create `.claude/` inside it.
- Run Motif installer from the project root (same pattern as controlled setup.sh).
- Print instructions for CryptoPay workflow. Per research, CryptoPay specifics:
  - Vertical: fintech
  - Description: "Crypto payments platform for Nigerian users, mobile-first, React"
  - Differentiation seed: personality 7 (rebellious/crypto), temperature 6 (warm/Nigerian market), era 8 (cutting-edge/web3)
  - Brand color: `#7C3AED` (violet -- represents crypto/web3 identity)
- Instructions must include:
  1. `/motif:init --auto --vertical fintech --stack react --theme dark --density comfortable`
     NOTE: After init, manually edit DESIGN-BRIEF.md to add brand color `#7C3AED` as LOCKED primary and set the differentiation seed values (personality:7, temperature:6, density:5, formality:5, era:8). This simulates a real user providing brand constraints.
  2. `/motif:research`
  3. `/motif:system`
  4. Compose 5+ screens for VALD-05 consistency test: `/motif:compose wallet`, `/motif:compose send`, `/motif:compose receive`, `/motif:compose history`, `/motif:compose profile`
  5. `/motif:review wallet`
  6. `/motif:fix` if needed
- After workflow, run verification commands (printed by script).

**2. `test/validation/run-all-validations.sh`** -- master validation runner that accepts test project directories and runs all checks:
- Accepts arguments: `--controlled DIR`, `--cryptopay DIR`, `--diff-a TOKENS_A --diff-b TOKENS_B`.
- Validation checks (each prints PASS/FAIL):

  **VALD-01 (if --controlled):**
  ```bash
  node test/validation/validate-workflow.js "$CONTROLLED_DIR" --screens login,dashboard,settings
  node test/validation/validate-tokens.js "$CONTROLLED_DIR/.planning/design/system/tokens.css"
  ```

  **VALD-02 (if --cryptopay):**
  ```bash
  node test/validation/validate-workflow.js "$CRYPTOPAY_DIR" --screens wallet,send,receive,history,profile
  node test/validation/validate-tokens.js "$CRYPTOPAY_DIR/.planning/design/system/tokens.css" --brand-color "#7C3AED"
  ```

  **VALD-03 (if --diff-a and --diff-b):**
  ```bash
  node test/validation/validate-diff.js "$TOKENS_A" "$TOKENS_B"
  ```

  **VALD-04 (embedded in VALD-02):**
  The `--brand-color` flag in validate-tokens.js handles this. The run script explicitly prints "VALD-04: Brand color preservation" header before that check.

  **VALD-05 (if --cryptopay with 5+ screens):**
  Run a loop checking each screen SUMMARY exists and grep each screen's source directory for hardcoded values (use the same patterns as motif-token-check.js: hex colors, px values outside var()). Print compliance percentage.
  For the consistency check: verify all screen SUMMARYs reference the same tokens.css token names (extract var(-- references from each, compare sets). If any screen introduces tokens not in tokens.css, flag it.

- Print final summary: `VALD-01: PASS/FAIL/SKIP`, `VALD-02: PASS/FAIL/SKIP`, etc.

Make both scripts executable.
  </action>
  <verify>
Run `bash test/validation/fixtures/cryptopay/setup.sh` -- should create temp dir, install Motif, print CryptoPay-specific instructions.
Run `bash test/validation/run-all-validations.sh` with no args -- should print usage showing available flags.
Run `bash -n test/validation/run-all-validations.sh` -- should parse without syntax errors.
  </verify>
  <done>CryptoPay setup creates an isolated test project with clear instructions for CryptoPay-specific workflow. Master validation runner accepts project directories and runs all VALD checks with clear PASS/FAIL output.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify CryptoPay, differentiation, brand color, and consistency (VALD-02 through VALD-05)</name>
  <files>none (verification only)</files>
  <action>
Human runs the full CryptoPay workflow and differentiation tests, then runs automated verification scripts.

What was built: CryptoPay setup script, master validation runner, and all validation tooling from Plan 01.

Steps for verification:

VALD-02: CryptoPay Battle Test
1. Run the setup script:
   bash test/validation/fixtures/cryptopay/setup.sh
   Note the TESTDIR path.

2. Open a NEW Claude Code session in the TESTDIR. Run:
   /motif:init --auto --vertical fintech --stack react --theme dark --density comfortable
3. Edit .planning/design/DESIGN-BRIEF.md:
   - Under Brand Constraints, add: Primary color: #7C3AED (user-provided, LOCKED)
   - Set differentiation seed: personality:7, temperature:6, density:5, formality:5, era:8
4. Continue workflow:
   /motif:research
   /motif:system
   /motif:compose wallet
   /motif:compose send
   /motif:compose receive
   /motif:compose history
   /motif:compose profile
   /motif:review wallet
   /motif:fix   (if review finds critical issues)

VALD-03: Differentiation Test
5. SAVE a copy of the CryptoPay tokens.css:
   cp $TESTDIR/.planning/design/system/tokens.css /tmp/tokens-cryptopay.css
6. The controlled test project from Plan 02 should already have a tokens.css. Copy it:
   cp $CONTROLLED_DIR/.planning/design/system/tokens.css /tmp/tokens-controlled.css

Run All Validations
7. Return to the Motif project root and run:
   bash test/validation/run-all-validations.sh --controlled $CONTROLLED_DIR --cryptopay $TESTDIR --diff-a /tmp/tokens-controlled.css --diff-b /tmp/tokens-cryptopay.css

8. Expected results:
   - VALD-02: All artifacts exist, tokens pass quality checks
   - VALD-03: Hue difference >= 30 degrees, display fonts differ
   - VALD-04: --color-primary-500 exactly matches #7C3AED
   - VALD-05: All 5 screen SUMMARYs exist, token references consistent

9. If any VALD fails, report specific failure output.
  </action>
  <verify>run-all-validations.sh prints PASS for VALD-02, VALD-03, VALD-04, and VALD-05.</verify>
  <done>VALD-02 through VALD-05 confirmed: CryptoPay workflow completes, designs are visibly distinct, brand color #7C3AED preserved as --color-primary-500, 5 screens maintain token consistency.</done>
</task>

</tasks>

<verification>
VALD-02: CryptoPay full workflow completes, all artifacts verified by validate-workflow.js
VALD-03: validate-diff.js reports hue >= 30 degrees and different fonts between two test projects
VALD-04: validate-tokens.js with --brand-color confirms #7C3AED preserved as --color-primary-500
VALD-05: 5+ screen SUMMARYs exist and reference tokens consistently (no drift, no hardcoded values)
</verification>

<success_criteria>
All four requirements (VALD-02 through VALD-05) pass their respective automated verification checks. Master validation script prints PASS for all VALD requirements.
</success_criteria>

<output>
After completion, create `.planning/phases/07-validation/07-03-SUMMARY.md`
</output>

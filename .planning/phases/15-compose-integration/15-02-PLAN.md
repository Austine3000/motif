---
phase: 15-compose-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - core/workflows/compose-screen.md
  - .claude/get-motif/workflows/compose-screen.md
autonomous: true

must_haves:
  truths:
    - "User can receive screen output decomposed into one component per file with proper imports"
    - "User can have composed files written to the project's actual source directories when scan data exists"
    - "User can have existing project components imported and reused instead of recreated"
    - "Composed output follows the project's detected naming conventions, file structure, and framework patterns"
    - "Greenfield projects still get decomposed output but in .planning/design/screens/{name}/ not project directories"
  artifacts:
    - path: "core/workflows/compose-screen.md"
      provides: "Canonical compose workflow with decomposition, placement, and reuse"
      contains: "Decomposition Rules"
    - path: ".claude/get-motif/workflows/compose-screen.md"
      provides: "Live copy of compose workflow (must match core/)"
      contains: "Decomposition Rules"
  key_links:
    - from: "core/workflows/compose-screen.md"
      to: ".planning/design/CONVENTIONS.md"
      via: "Context assembly optional files and agent instructions"
      pattern: "CONVENTIONS\\.md"
    - from: "core/workflows/compose-screen.md"
      to: ".planning/design/PROJECT-SCAN.md"
      via: "Brownfield detection flag and agent placement instructions"
      pattern: "PROJECT-SCAN\\.md"
    - from: "core/workflows/compose-screen.md"
      to: ".planning/design/COMPONENT-GAP.md"
      via: "Agent reuse instructions for existing components"
      pattern: "COMPONENT-GAP\\.md"
---

<objective>
Add decomposition, project-directory placement, and existing component reuse to the compose-screen workflow.

Purpose: This is the core deliverable of Phase 15. The compose workflow's agent prompt gains three new instruction sections that transform the composer from a monolithic single-file generator into a decomposition engine that writes one component per file, places files in the user's actual project directories (brownfield), and imports existing components instead of recreating them.
Output: Updated compose-screen.md (both core/ canonical and .claude/get-motif/ live copy, kept identical)
</objective>

<execution_context>
@/Users/austineogbuanya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austineogbuanya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-compose-integration/15-RESEARCH.md
@core/workflows/compose-screen.md
@.claude/get-motif/workflows/compose-screen.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add brownfield context assembly and decomposition agent instructions to compose-screen.md</name>
  <files>core/workflows/compose-screen.md</files>
  <action>
Modify core/workflows/compose-screen.md with these changes. All additions are existence-gated so greenfield behavior is unchanged.

**Step 2 (Assemble Context Profile) -- add brownfield optional files:**

Add to the OPTIONAL_FILES block:
```
  - .planning/design/PROJECT-SCAN.md        (brownfield: directory structure, framework)
  - .planning/design/CONVENTIONS.md         (brownfield: code conventions)
  - .planning/design/COMPONENT-GAP.md       (brownfield: existing component reuse map)
```

After checking PROJECT.md for the stack, add brownfield detection:
```
Check if `.planning/design/PROJECT-SCAN.md` exists → set BROWNFIELD=true
If BROWNFIELD: also check for CONVENTIONS.md and COMPONENT-GAP.md.
```

**Step 3 (Agent Prompt) -- add context files 6-8 for brownfield:**

After the existing context files list (items 1-5 plus optional previous summaries), add:
```
{IF .planning/design/PROJECT-SCAN.md exists (brownfield project):}
7. `.planning/design/PROJECT-SCAN.md` — project directory structure, framework, existing components
8. `.planning/design/CONVENTIONS.md` — file naming, export style, import paths, CSS approach
9. `.planning/design/COMPONENT-GAP.md` — which design system components exist in the project vs need generating
```

**Step 3, after section B (Implementation Rules) -- add new section B2: Decomposition Rules:**

```markdown
### B2. Decomposition Rules

You MUST decompose every screen into individual component files. No monolithic single-file output.

#### File Decomposition
1. Identify all components in your screen design
2. Create one file per component:
   - Page/route component (top level)
   - Section components (major visual areas)
   - New UI primitives (only if not already existing per COMPONENT-GAP.md)
3. Each file must:
   - Have its own imports
   - Export exactly one component
   - Be under 150 lines (decompose further if needed)

#### Import Hierarchy (STRICT -- no circular imports)
Page -> Sections -> Primitives
- A page imports sections and primitives
- A section imports primitives only
- A primitive imports nothing from this screen (only external deps and tokens)

#### File Placement

{IF PROJECT-SCAN.md and CONVENTIONS.md exist (brownfield):}

Read CONVENTIONS.md for:
- Naming convention (PascalCase/kebab-case)
- Export style (named/default)
- Import paths (aliases like @/, relative paths)
- CSS approach (Tailwind/CSS Modules/styled-components)

Read PROJECT-SCAN.md for:
- Component directories (where existing components live)
- Route/page directories (where pages live)
- Framework-specific patterns (App Router vs Pages Router, etc.)

Place files:
- Page component -> framework route directory (e.g., src/app/dashboard/page.tsx)
- New reusable components -> project's component directory (e.g., src/components/ui/StatCard.tsx)
- Screen-specific sections -> co-located with page (e.g., src/app/dashboard/_components/DashboardHeader.tsx) OR in a features directory, depending on project convention

NEVER modify existing project files. Only create new files and import existing ones.
NEVER write to node_modules, .next, dist, build, or any build output directory.
Before writing any file, check if a file already exists at that path. If it does, use a different name (e.g., DashboardCard.tsx instead of Card.tsx).

{IF no PROJECT-SCAN.md (greenfield):}

Place all files in: `.planning/design/screens/{SCREEN_NAME}/`
Create a barrel export (index.ts or index.js) for the directory.
Use sensible defaults: TypeScript (.tsx), named exports, single quotes, semicolons, inline styles with CSS custom properties from tokens.css.
```

**Step 3, after B2 -- add new section B3: Existing Component Reuse:**

```markdown
### B3. Existing Component Reuse

{IF .planning/design/COMPONENT-GAP.md exists:}

Before generating ANY component, check COMPONENT-GAP.md:

1. **"Existing" table:** Component exists in the project.
   -> Import it. Do NOT recreate it.
   -> Use the file path from the table.
   -> Apply project import conventions from CONVENTIONS.md (path aliases, barrel files, etc.).

2. **"Partial Match" table:** Similar component exists.
   -> Import it. Do NOT recreate it.
   -> Add a comment: // Note: using existing [FoundName] as [RequiredName]
   -> If it needs props it doesn't have, pass them anyway -- the user will enhance later.

3. **Not in COMPONENT-GAP.md:** Component does not exist.
   -> Generate it as a new file following COMPONENT-SPECS.md.
   -> Place per File Placement rules above.

Example:
```typescript
// COMPONENT-GAP.md says Button exists at src/components/ui/Button.tsx
// CONVENTIONS.md says project uses @/ alias
import { Button } from '@/components/ui/Button';

// COMPONENT-GAP.md says no StatCard exists
// Generate new: src/components/dashboard/StatCard.tsx
import { StatCard } from '@/components/dashboard/StatCard';
```

{IF COMPONENT-GAP.md does not exist:}
Generate all components as new files. No reuse checks needed.
```

**Step 3, section C (Anti-Slop Check) -- add decomposition checks:**

Add these items to the anti-slop checklist:
```
- Am I writing everything into one file? -> STOP. Decompose into one component per file.
- Am I recreating a component that exists in COMPONENT-GAP.md? -> STOP. Import it instead.
- Am I using a different import path style than CONVENTIONS.md specifies? -> STOP. Match the project's convention.
```

**Step 3, section D (Self-Review Checklist) -- add decomposition checks:**

Add these items:
```
- [ ] Each component in its own file (no monolithic output)
- [ ] Import hierarchy: page -> sections -> primitives (no circular imports)
- [ ] IF brownfield: existing components imported, not recreated
- [ ] IF brownfield: file paths match project conventions (naming, directories)
- [ ] IF brownfield: import paths use project's style (@/ alias, relative, barrel)
```

**Step 3, section E (Create Summary) -- update to decomposed format:**

Replace the summary template with:
```markdown
# Screen: {SCREEN_NAME}

## Components Used
### Existing (imported from project)
[List components imported from the user's project with file paths -- or "N/A (greenfield)" if no scan data]

### New (generated by Motif)
[List all newly generated components with their file paths]

## Key Tokens Referenced
[List the primary tokens this screen depends on]

## Vertical Patterns Applied
[Which LOCKED decisions were implemented]

## States
- Default Y/N
- Loading Y/N
- Empty Y/N
- Error Y/N

## Files Created
[List ALL files created with full paths]

## Files Imported (not created)
[List all existing project files that were imported -- or "N/A (greenfield)" if no scan data]
```

**Keep the frontmatter, gate_check, Step 1, Step 4, Step 5, Step 6, and Parallel Composition sections unchanged.** Only modify Step 2 (context assembly) and Step 3 (agent prompt) as described above.
  </action>
  <verify>Read core/workflows/compose-screen.md. Confirm it contains: (1) "Decomposition Rules" section, (2) "Existing Component Reuse" section, (3) PROJECT-SCAN.md in optional files, (4) CONVENTIONS.md in optional files, (5) COMPONENT-GAP.md in optional files, (6) greenfield fallback to .planning/design/screens/{name}/, (7) updated summary format with "Files Imported".</verify>
  <done>Compose workflow agent prompt includes decomposition rules, brownfield file placement, existing component reuse, and updated summary format. All additions are existence-gated.</done>
</task>

<task type="auto">
  <name>Task 2: Sync live copy of compose-screen.md</name>
  <files>.claude/get-motif/workflows/compose-screen.md</files>
  <action>
Copy the contents of core/workflows/compose-screen.md to .claude/get-motif/workflows/compose-screen.md so both files are identical.

Read core/workflows/compose-screen.md (the canonical version updated in Task 1) and write its exact contents to .claude/get-motif/workflows/compose-screen.md.

These two files MUST be byte-identical. The core/ version is the canonical source; the .claude/get-motif/ version is the live copy that Claude Code actually executes.
  </action>
  <verify>Diff the two files: `diff core/workflows/compose-screen.md .claude/get-motif/workflows/compose-screen.md` should show no differences.</verify>
  <done>Both copies of compose-screen.md are identical with all decomposition, placement, and reuse additions.</done>
</task>

</tasks>

<verification>
1. `grep "Decomposition Rules" core/workflows/compose-screen.md` returns a match
2. `grep "Existing Component Reuse" core/workflows/compose-screen.md` returns a match
3. `grep "COMPONENT-GAP.md" core/workflows/compose-screen.md` returns at least one match
4. `grep "PROJECT-SCAN.md" core/workflows/compose-screen.md` returns at least one match
5. `grep "CONVENTIONS.md" core/workflows/compose-screen.md` returns at least one match
6. `grep "greenfield" core/workflows/compose-screen.md` returns at least one match
7. `grep "Files Imported" core/workflows/compose-screen.md` returns a match
8. `diff core/workflows/compose-screen.md .claude/get-motif/workflows/compose-screen.md` shows no differences
</verification>

<success_criteria>
- Compose workflow decomposes screens into one component per file (COMP-01)
- Brownfield projects get files written to actual project directories (COMP-02)
- Existing components are imported from COMPONENT-GAP.md instead of recreated (COMP-03)
- Generated code follows project conventions from CONVENTIONS.md (naming, imports, exports)
- Greenfield projects get decomposed output in .planning/design/screens/{name}/ (no breakage)
- Both copies of compose-screen.md are identical
</success_criteria>

<output>
After completion, create `.planning/phases/15-compose-integration/15-02-SUMMARY.md`
</output>

---
phase: 15-compose-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/get-motif/references/context-engine.md
  - .claude/get-motif/templates/SUMMARY-TEMPLATE.md
  - .claude/get-motif/references/state-machine.md
autonomous: true

must_haves:
  truths:
    - "Composer context profile includes brownfield scan artifacts (PROJECT-SCAN.md, CONVENTIONS.md, COMPONENT-GAP.md)"
    - "Summary template includes sections for existing vs new components, files created, and files imported"
    - "Context budget table includes entries for brownfield artifacts in the composer profile"
  artifacts:
    - path: ".claude/get-motif/references/context-engine.md"
      provides: "Updated composer profile with brownfield artifact loading"
      contains: "PROJECT-SCAN.md"
    - path: ".claude/get-motif/templates/SUMMARY-TEMPLATE.md"
      provides: "Updated summary format with decomposition tracking"
      contains: "Files Imported"
    - path: ".claude/get-motif/references/state-machine.md"
      provides: "Updated COMPOSING artifacts to reflect decomposed output"
      contains: "decomposed"
  key_links:
    - from: ".claude/get-motif/references/context-engine.md"
      to: ".planning/design/PROJECT-SCAN.md"
      via: "load_if_exists entry in composer profile"
      pattern: "PROJECT-SCAN\\.md"
    - from: ".claude/get-motif/references/context-engine.md"
      to: ".planning/design/CONVENTIONS.md"
      via: "load_if_exists entry in composer profile"
      pattern: "CONVENTIONS\\.md"
    - from: ".claude/get-motif/references/context-engine.md"
      to: ".planning/design/COMPONENT-GAP.md"
      via: "load_if_exists entry in composer profile"
      pattern: "COMPONENT-GAP\\.md"
---

<objective>
Update context engine, summary template, and state machine to support decomposed composition output.

Purpose: The composer profile must know about brownfield artifacts (PROJECT-SCAN.md, CONVENTIONS.md, COMPONENT-GAP.md) so the compose workflow can pass them to subagents. The summary template must track which components were reused vs newly generated. The state machine must reflect that COMPOSING now produces decomposed multi-file output.
Output: Updated context-engine.md, SUMMARY-TEMPLATE.md, and state-machine.md
</objective>

<execution_context>
@/Users/austineogbuanya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austineogbuanya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-compose-integration/15-RESEARCH.md
@.claude/get-motif/references/context-engine.md
@.claude/get-motif/templates/SUMMARY-TEMPLATE.md
@.claude/get-motif/references/state-machine.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update composer context profile and budget table</name>
  <files>.claude/get-motif/references/context-engine.md</files>
  <action>
Update the Screen Composer profile (the `<context_profile name="composer">` block) to add brownfield artifacts to `load_if_exists`:

```xml
<load_if_exists>
    .planning/design/DESIGN-RESEARCH.md
    .planning/design/screens/{previous-screen}-SUMMARY.md (for consistency)
    .planning/design/PROJECT-SCAN.md
    .planning/design/CONVENTIONS.md
    .planning/design/COMPONENT-GAP.md
</load_if_exists>
```

Also add TOKEN-INVENTORY.md to the `never_load` list for the composer profile with comment: "Composer doesn't need token inventory -- tokens.css has the final tokens".

Update the Context Budgets table to add a note that the fully-loaded brownfield composer budget is ~15,000 tokens (within budget). No new rows needed since PROJECT-SCAN.md, CONVENTIONS.md, and COMPONENT-GAP.md already have entries in the budget table from 14-03.

All additions must be purely additive -- do not remove or reorder existing entries.
  </action>
  <verify>Read .claude/get-motif/references/context-engine.md and confirm the composer profile's load_if_exists section contains PROJECT-SCAN.md, CONVENTIONS.md, and COMPONENT-GAP.md. Confirm TOKEN-INVENTORY.md is in never_load.</verify>
  <done>Composer context profile includes all three brownfield scan artifacts in load_if_exists and TOKEN-INVENTORY.md in never_load</done>
</task>

<task type="auto">
  <name>Task 2: Update summary template and state machine for decomposed output</name>
  <files>.claude/get-motif/templates/SUMMARY-TEMPLATE.md, .claude/get-motif/references/state-machine.md</files>
  <action>
**SUMMARY-TEMPLATE.md:** Replace the current template with the updated format that supports decomposition tracking. The new template must include:

```markdown
# Screen: {SCREEN_NAME}

## Components Used
### Existing (imported from project)
[List components imported from the user's project with file paths -- or "N/A (greenfield)" if no scan data]

### New (generated by Motif)
[List all newly generated components with their file paths]

## Key Tokens Referenced
[List the primary tokens this screen depends on]

## Vertical Patterns Applied
[Which LOCKED decisions from DESIGN-RESEARCH.md were implemented]

## States
- Default Y/N
- Loading Y/N
- Empty Y/N
- Error Y/N

## Files Created
[List ALL files created with full paths]

## Files Imported (not created)
[List all existing project files that were imported -- or "N/A (greenfield)" if no scan data]
```

Keep the source-of-truth comment at the top: `<!-- Source of truth: core/workflows/compose-screen.md section E -- keep in sync -->`

**state-machine.md:** Update the COMPOSING row in the Phase Definitions table. Change the Artifacts Created column from `screens/[name].*, screens/[name]-SUMMARY.md` to `screens/[name]/ (decomposed components), screens/[name]-SUMMARY.md, OR project-directory files (brownfield)` to reflect that compose now produces multiple files.

Do NOT change any other rows or gate checks.
  </action>
  <verify>Read both files. Confirm SUMMARY-TEMPLATE.md has "Existing (imported from project)" and "Files Imported (not created)" sections. Confirm state-machine.md COMPOSING row mentions decomposed output.</verify>
  <done>Summary template tracks reused vs new components and lists files created/imported. State machine reflects decomposed output pattern.</done>
</task>

</tasks>

<verification>
1. `grep -c "PROJECT-SCAN.md" .claude/get-motif/references/context-engine.md` returns at least 2 (one in system-generator, one in composer)
2. `grep "Files Imported" .claude/get-motif/templates/SUMMARY-TEMPLATE.md` returns a match
3. `grep "decomposed" .claude/get-motif/references/state-machine.md` returns a match
4. Greenfield profiles unchanged -- no existing always_load or never_load entries removed
</verification>

<success_criteria>
- Context engine composer profile loads brownfield artifacts when they exist
- Summary template distinguishes between existing (imported) and new (generated) components
- State machine accurately describes decomposed COMPOSING output
- All changes are additive -- no existing behavior altered
</success_criteria>

<output>
After completion, create `.planning/phases/15-compose-integration/15-01-SUMMARY.md`
</output>

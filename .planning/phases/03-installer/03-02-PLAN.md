---
phase: 03-installer
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - bin/install.js
autonomous: true

must_haves:
  truths:
    - "Re-installing on an existing installation backs up user-modified files to .motif-backup/ before overwriting"
    - "Re-installing does NOT back up files that have not been modified since the last install (hash matches manifest)"
    - "Running with --uninstall removes all installed files, cleans up empty directories, removes CLAUDE.md snippet, and deletes the manifest"
    - "Running --uninstall when no manifest exists prints an error and exits 1"
    - "--force flag skips backup checks and overwrites all files unconditionally"
  artifacts:
    - path: "bin/install.js"
      provides: "Upgrade tracking with manifest-based hash diffing, backup logic, and uninstall functionality"
      contains: "shouldBackup"
  key_links:
    - from: "bin/install.js shouldBackup()"
      to: ".motif-manifest.json"
      via: "reads existing manifest and compares file hashes"
      pattern: "shouldBackup"
    - from: "bin/install.js uninstall()"
      to: ".motif-manifest.json"
      via: "reads manifest to find installed files, then removes them"
      pattern: "manifest\\.files"
---

<objective>
Enhance bin/install.js with manifest-based upgrade tracking (backup user-modified files on re-install), uninstall functionality (--uninstall removes all installed files and config), and the --force flag behavior (skip backups, overwrite all). This completes the installer's re-install and uninstall flows.

Purpose: Users need to safely upgrade Motif without losing their customizations, and cleanly remove it when no longer needed. Without this, re-installs silently destroy user changes.

Output: Complete installer with full lifecycle support -- install, upgrade, and uninstall.
</objective>

<execution_context>
@/Users/austineogbuanya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austineogbuanya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-installer/03-RESEARCH.md
@.planning/phases/03-installer/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add manifest-based upgrade tracking and backup logic</name>
  <files>bin/install.js</files>
  <action>
Modify the existing bin/install.js (created in Plan 01) to add re-install awareness. Three changes needed:

**Change 1: Load existing manifest at startup (in main flow, after resolveMapping)**

After `resolveMapping()` returns, check if `.motif-manifest.json` exists at project root. If it does, parse it as `existingManifest`. If not, set to `null`. Pass `existingManifest` into `copyFiles()`.

**Change 2: Add shouldBackup() function**

```javascript
function shouldBackup(destPath, existingManifest) {
  if (!fs.existsSync(destPath)) return false;
  if (!existingManifest) return true;  // No manifest = unknown state, back up to be safe

  const relPath = path.relative(process.cwd(), destPath);
  const entry = existingManifest.files[relPath];
  if (!entry) return true;  // File not in manifest = unknown, back up

  const currentHash = hashFile(destPath);
  // If current hash matches what we installed, user hasn't modified it -- safe to overwrite
  if (currentHash === entry.hash) return false;
  // User modified this file -- back up before overwriting
  return true;
}
```

**Change 3: Integrate backup into walkAndCopy()**

In the file-copy loop of walkAndCopy(), before writing each file:
1. If `flags.force` is true, skip backup check entirely
2. Otherwise, call `shouldBackup(destPath, existingManifest)`
3. If backup needed:
   - Create `.motif-backup/` directory at project root if not exists
   - Generate timestamped backup filename: `{original-name}.{ISO-timestamp-safe}` (replace `:` and `.` with `-`)
   - Copy the existing file to `.motif-backup/{timestamped-name}`
   - Increment `results.backedUp` counter
   - Print: `  Backed up: {relPath} -> .motif-backup/{backupName}`
4. Proceed with the overwrite (copy new version)

If `--dry-run`, print "Would back up: {relPath}" instead of actually backing up.

Update `printSummary` to include the backedUp count: `  Files backed up: {N}`.
  </action>
  <verify>
Test re-install scenario:
1. Run `node bin/install.js` in a test directory with `.claude/` present (fresh install)
2. Modify one installed .md file (add a comment)
3. Run `node bin/install.js --dry-run` again and confirm it reports "Would back up" for the modified file but NOT for unmodified files
4. Run `node bin/install.js` again and confirm the modified file is backed up to `.motif-backup/` and then overwritten
  </verify>
  <done>Re-install detects user-modified files via manifest hash comparison, backs them up to .motif-backup/ before overwriting, and skips backup for unmodified files. --force skips all backup checks.</done>
</task>

<task type="auto">
  <name>Task 2: Implement --uninstall flag functionality</name>
  <files>bin/install.js</files>
  <action>
Replace the placeholder "Uninstall not yet implemented" message (from Plan 01) with a working `uninstall()` function. This function runs when `--uninstall` flag is present.

**uninstall() implementation:**

1. Read `.motif-manifest.json` from project root. If not found, print red error: "No Motif installation found (missing .motif-manifest.json)" and exit 1.

2. Parse the manifest JSON. Iterate over `Object.keys(manifest.files)` and for each relative file path:
   - Resolve to absolute path with `path.join(process.cwd(), filePath)`
   - Validate path is within project root (path traversal check)
   - If file exists, delete with `fs.unlinkSync()`
   - Track count of removed files

3. Clean up empty directories using a `cleanEmptyDirs(dir)` helper:
   - Walk directory tree bottom-up
   - If a directory is empty after file removal, remove it with `fs.rmdirSync()`
   - Run on: `.claude/get-motif/`, `.claude/commands/motif/`
   - Do NOT remove `.claude/` itself (user may have other content there)
   - Do NOT remove `.claude/commands/` itself (user may have other commands)

4. Remove CLAUDE.md sentinel block:
   - Read CLAUDE.md (or `.claude/CLAUDE.md` -- check both)
   - Find `<!-- MOTIF-START -->` and `<!-- MOTIF-END -->` markers
   - Remove everything from START marker through END marker (inclusive)
   - Clean up extra blank lines left behind (replace 3+ consecutive newlines with 2)
   - If CLAUDE.md is now empty or whitespace-only after removal, delete the file
   - Otherwise write the cleaned content back

5. Remove `.motif-manifest.json` itself.

6. Remove `.motif-backup/` directory if it exists (use `fs.rmSync(dir, { recursive: true })`).

7. Print green success message: "Motif uninstalled successfully. Removed {N} files."

If `--dry-run` is also passed with `--uninstall`, print what would be removed without actually deleting.
  </action>
  <verify>
Test uninstall flow:
1. Run `node bin/install.js` in a test directory to install
2. Confirm files exist at `.claude/get-motif/` and `.claude/commands/motif/`
3. Run `node bin/install.js --uninstall --dry-run` and confirm it prints what would be removed
4. Run `node bin/install.js --uninstall` and confirm all installed files are removed, CLAUDE.md snippet is removed, manifest is deleted
5. Run `node bin/install.js --uninstall` again and confirm it prints "No Motif installation found" error
  </verify>
  <done>--uninstall removes all manifest-tracked files, cleans empty directories, strips CLAUDE.md sentinel block, and deletes the manifest. Repeated uninstall shows error. --dry-run with --uninstall prints without deleting.</done>
</task>

</tasks>

<verification>
1. Fresh install + re-install test: install, modify a file, re-install -- modified file backed up, unmodified files overwritten cleanly
2. --force re-install test: install, modify a file, re-install with --force -- no backup created, all files overwritten
3. --uninstall test: install, uninstall -- all files removed, CLAUDE.md cleaned, manifest deleted
4. --uninstall without installation: prints error and exits 1
5. Full round-trip: install -> modify -> re-install (backup created) -> uninstall (everything cleaned up including backups)
</verification>

<success_criteria>
- shouldBackup() correctly identifies user-modified files via SHA-256 hash comparison against manifest
- User-modified files are copied to .motif-backup/ with timestamps before being overwritten
- Unmodified files are overwritten without backup (hash matches manifest)
- --force bypasses all backup logic
- --uninstall removes all installed files, sentinel block from CLAUDE.md, and manifest
- --uninstall + --dry-run prints planned removals without deleting
- Empty directories are cleaned up during uninstall but parent dirs (.claude/, .claude/commands/) preserved
</success_criteria>

<output>
After completion, create `.planning/phases/03-installer/03-02-SUMMARY.md`
</output>

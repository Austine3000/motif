---
phase: 03-installer
plan: 04
type: execute
wave: 1
depends_on: ["03-03"]
files_modified:
  - core/references/runtime-adapters.md
  - core/references/design-inputs.md
  - runtimes/claude-code/commands/motif/help.md
  - runtimes/claude-code/CLAUDE-MD-SNIPPET.md
  - runtimes/claude-code/agents/
  - runtimes/opencode/
  - runtimes/gemini/
  - runtimes/cursor/
  - bin/commands/init.js
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Zero old branding remains in shipped sources (no Design Forge / /forge: / get-design-forge / .design-forge / forge-* in core/, runtimes/, bin/)."
    - "All runtime command/agent paths are Motif-branded (commands/motif and motif-*.md)."
    - "Installer mapping and runtime snippets reference Motif paths and /motif: commands only."
    - "A fresh install produces zero old-brand references in installed .md files."
  artifacts:
    - path: "core/references/runtime-adapters.md"
      provides: "Runtime mapping table and install paths use Motif naming"
      contains: ".opencode/get-motif"
    - path: "core/references/design-inputs.md"
      provides: "Motif command references in design input documentation"
      contains: "/motif:init"
    - path: "runtimes/claude-code/commands/motif/help.md"
      provides: "Help text lists /motif:* commands"
      contains: "/motif:help"
    - path: "runtimes/claude-code/CLAUDE-MD-SNIPPET.md"
      provides: "Config snippet uses Motif branding and /motif:* commands"
      contains: "Motif Rules"
    - path: "bin/commands/init.js"
      provides: "Installer mapping points to commands/motif and motif-*.md agents"
      contains: "commands/motif"
  key_links:
    - from: "bin/commands/init.js"
      to: "runtimes/claude-code/commands/motif/"
      via: "Installer mapping for command files"
      pattern: "commands/motif"
    - from: "bin/commands/init.js"
      to: "runtimes/claude-code/agents/"
      via: "Installer mapping for agent definitions"
      pattern: "motif-"
---

<objective>
Close the Phase 3 UAT branding gap by removing any remaining Design Forge / forge references from shipped sources and ensuring runtime command/agent paths consistently use Motif naming.

Purpose: UAT Test 2 failed due to stale branding in runtime references and help/command docs. These inconsistencies leak old product naming into installed files and confuse users.

Output: Fully Motif-branded source files, Motif-branded runtime paths, and a verified clean install with zero old-brand references.
</objective>

<execution_context>
@/Users/austineogbuanya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austineogbuanya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-installer/03-UAT.md
@.planning/phases/03-installer/03-VERIFICATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Brand sweep and fix stale forge references in source files</name>
  <files>
    core/references/runtime-adapters.md
    core/references/design-inputs.md
    runtimes/claude-code/commands/motif/help.md
    runtimes/claude-code/CLAUDE-MD-SNIPPET.md
    runtimes/claude-code/agents/
    runtimes/opencode/
    runtimes/gemini/
    runtimes/cursor/
    bin/commands/init.js
  </files>
  <action>
Run a targeted branding sweep and fix all remaining old-brand references. Replace only branding strings (do NOT remove functional placeholders like `{FORGE_ROOT}` unless they are user-facing examples).

1) Search for old branding across shipped sources:
```bash
rg -n "Design Forge|design forge|/forge:|get-design-forge|\.design-forge|commands/forge|forge-" core/ runtimes/ bin/
```
2) Apply these replacements where found:
- "Design Forge"/"design forge" → "Motif"
- "/forge:" → "/motif:"
- "get-design-forge" → "get-motif"
- ".design-forge" → ".motif"
- "commands/forge" → "commands/motif"
- "forge-*.md" agent/command filenames → "motif-*.md" (rename files and update references)

3) Verify runtime paths and commands in the known UAT files:
- `core/references/runtime-adapters.md` should show `.opencode/get-motif`, `.gemini/get-motif`, `.motif/`, and `.claude/commands/motif/*` paths.
- `core/references/design-inputs.md` should reference `/motif:*` commands only.
- `runtimes/claude-code/commands/motif/help.md` should list `/motif:*` commands only.
- `runtimes/claude-code/CLAUDE-MD-SNIPPET.md` should read "Motif Rules" and show `/motif:*` commands.

4) If any `runtimes/**/commands/forge/` or `runtimes/**/agents/forge-*.md` remain:
- Rename to `commands/motif/` and `motif-*.md`.
- Update all references in command/help docs and installer mapping.

5) Ensure installer mapping uses the Motif paths:
- In `bin/commands/init.js`, confirm source mapping points to `runtimes/claude-code/commands/motif/` and motif agent filenames.
- Update any lingering forge path segments.
  </action>
  <verify>
Run:
```bash
rg -n "Design Forge|design forge|/forge:|get-design-forge|\.design-forge|commands/forge|forge-" core/ runtimes/ bin/
```
Expect zero matches.
  </verify>
  <done>All shipped sources reference Motif only. Any forge-named directories or files are renamed to Motif and all references updated.</done>
</task>

<task type="auto">
  <name>Task 2: Re-verify installer output has zero old-brand references</name>
  <files>test/e2e-installer.js</files>
  <action>
Re-run the installer verification to ensure installed files are clean:

Option A (full e2e):
```bash
node test/e2e-installer.js
```

Option B (targeted):
1. Create a temp directory with `.claude/`
2. Run `node bin/install.js` from that directory
3. Grep installed files:
```bash
rg -n "Design Forge|design forge|/forge:|get-design-forge|\.design-forge|commands/forge|forge-" .claude/get-motif/ .claude/commands/motif/
```
4. Expect zero matches.

If Option A is used, ensure the test still asserts zero `.claude/get-design-forge` occurrences and passes.
  </action>
  <verify>All e2e tests pass (or targeted grep returns zero matches). No old-brand references appear in installed .md files.</verify>
  <done>Installer output is fully Motif-branded and UAT Test 2 passes.</done>
</task>

</tasks>

<verification>
- `rg` across core/, runtimes/, bin/ returns zero old-brand matches.
- Fresh install output contains zero old-brand references in installed .md files.
</verification>

<success_criteria>
- No old branding in shipped sources or runtime help/snippet files
- Runtime command/agent directories and filenames use Motif naming consistently
- Installer output is clean (UAT Test 2 passes)
</success_criteria>

<output>
After completion, create `.planning/phases/03-installer/03-04-SUMMARY.md`
</output>

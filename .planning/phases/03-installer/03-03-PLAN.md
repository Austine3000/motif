---
phase: 03-installer
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Fresh install in a clean project with .claude/ creates all expected directories and files"
    - "All installed .md files contain zero occurrences of {FORGE_ROOT} or .claude/get-design-forge"
    - "CLAUDE.md has Motif config between sentinel markers"
    - ".motif-manifest.json records every installed file with valid SHA-256 hashes"
    - "Re-install backs up modified files and overwrites unmodified files"
    - "Uninstall removes everything cleanly"
  artifacts: []
  key_links: []
---

<objective>
End-to-end verification of the complete installer lifecycle: fresh install, path resolution validation, re-install with backup, and uninstall cleanup. This is the human-verify checkpoint confirming the installer works correctly before Phase 3 is marked complete.

Purpose: The installer is the delivery mechanism for the entire product. A broken installer means no one can use Motif. This checkpoint catches integration issues that unit-level checks in Plans 01 and 02 might miss.

Output: Verified installer that handles the full install/upgrade/uninstall lifecycle.
</objective>

<execution_context>
@/Users/austineogbuanya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austineogbuanya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-installer/03-01-SUMMARY.md
@.planning/phases/03-installer/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated end-to-end test suite</name>
  <files></files>
  <action>
Create a temporary test directory and run the full installer lifecycle. All tests run from the temp directory using `node {project-root}/bin/install.js`.

**Test 1: Fresh install**
1. Create temp dir, create `.claude/` subdirectory inside it (simulates Claude Code project)
2. Run `node {project}/bin/install.js` from temp dir
3. Assert these directories exist: `.claude/get-motif/references/`, `.claude/get-motif/workflows/`, `.claude/get-motif/templates/`, `.claude/get-motif/agents/`, `.claude/commands/motif/`
4. Assert these specific files exist:
   - `.claude/get-motif/references/state-machine.md`
   - `.claude/get-motif/references/verticals/fintech.md`
   - `.claude/get-motif/workflows/research.md`
   - `.claude/get-motif/agents/forge-researcher.md`
   - `.claude/commands/motif/init.md`
   - `.claude/get-motif/templates/STATE-TEMPLATE.md`
   - `.claude/get-motif/templates/token-showcase-template.html`
   - `CLAUDE.md`
   - `.motif-manifest.json`
5. Print pass/fail for each assertion

**Test 2: Path resolution verification**
1. Grep ALL `.md` files under `.claude/get-motif/` and `.claude/commands/motif/` for the literal string `{FORGE_ROOT}`
2. Grep ALL `.md` files for the literal string `.claude/get-design-forge`
3. Assert zero matches for both patterns
4. Spot-check `.claude/get-motif/workflows/research.md` contains `.claude/get-motif` where `{FORGE_ROOT}` was
5. Spot-check `.claude/commands/motif/init.md` contains `.claude/get-motif` where `.claude/get-design-forge` was
6. Print pass/fail for each assertion

**Test 3: CLAUDE.md sentinel markers**
1. Read CLAUDE.md content
2. Assert it contains `<!-- MOTIF-START -->`
3. Assert it contains `<!-- MOTIF-END -->`
4. Assert START appears before END
5. Assert the snippet content (from CLAUDE-MD-SNIPPET.md) is between the markers
6. Print pass/fail

**Test 4: Manifest integrity**
1. Parse `.motif-manifest.json`
2. Assert it has `version`, `runtime`, `installedAt`, `files` fields
3. Assert `files` object has entries for each installed file
4. Pick 3 random files from manifest, compute their SHA-256 hash, assert it matches the manifest hash
5. Print pass/fail

**Test 5: Re-install with backup**
1. Modify one installed file (e.g., add `<!-- user edit -->` to `.claude/get-motif/workflows/research.md`)
2. Run `node {project}/bin/install.js` again
3. Assert `.motif-backup/` directory was created
4. Assert the backup directory contains a timestamped copy of the modified file
5. Assert the installed file has been overwritten with the fresh version (no `<!-- user edit -->`)
6. Print pass/fail

**Test 6: Uninstall**
1. Run `node {project}/bin/install.js --uninstall`
2. Assert `.claude/get-motif/` directory does not exist
3. Assert `.claude/commands/motif/` directory does not exist
4. Assert CLAUDE.md either does not exist or does not contain `<!-- MOTIF-START -->`
5. Assert `.motif-manifest.json` does not exist
6. Assert `.claude/` directory still exists (not deleted -- user may have other content)
7. Print pass/fail

**Test 7: --dry-run**
1. Run `node {project}/bin/install.js --dry-run` (after uninstall, so starting fresh)
2. Assert NO new files were created (the directories from the install should not reappear)
3. Assert output contains "Would copy:" messages
4. Print pass/fail

Print a final summary: "X/7 tests passed". Clean up the temp directory.

If any test fails, print the specific failure details and leave the temp directory intact for debugging (print its path).
  </action>
  <verify>All 7 tests print "PASS". Final summary shows "7/7 tests passed".</verify>
  <done>All automated lifecycle tests pass: fresh install, path resolution, sentinel markers, manifest integrity, re-install backup, uninstall cleanup, and dry-run</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of installer lifecycle</name>
  <files></files>
  <action>
Present the automated test results to the user for review. The checkpoint verifies that the complete Motif installer (bin/install.js) works correctly for fresh install, upgrade with backup, and uninstall flows.

What was built: Complete Motif installer (bin/install.js) with fresh install, upgrade with backup, and uninstall support. The automated test suite in Task 1 validated the full lifecycle.

How to verify:
1. Review the automated test output from Task 1 -- all 7 tests should pass
2. Optionally run a manual test: create a new temp directory, add a `.claude/` folder, then run `node {project}/bin/install.js` from that directory
3. Check the installed files look correct: `ls -R .claude/get-motif/ .claude/commands/motif/`
4. Check CLAUDE.md has the config snippet: `cat CLAUDE.md`
5. Check path resolution worked: `grep -r 'FORGE_ROOT' .claude/get-motif/` should return nothing
6. Run `node {project}/bin/install.js --uninstall` and verify cleanup

Resume signal: Type "approved" or describe issues to fix.
  </action>
  <verify>User approves the installer after reviewing automated test results and optional manual testing</verify>
  <done>Human confirms the installer works correctly for fresh install, upgrade, and uninstall scenarios</done>
</task>

</tasks>

<verification>
All 7 automated tests pass, confirming:
1. Fresh install creates correct directory structure and files
2. Zero unresolved {FORGE_ROOT} or .claude/get-design-forge references in installed files
3. CLAUDE.md has properly formed sentinel markers with config snippet
4. Manifest tracks all files with valid SHA-256 hashes
5. Re-install backs up user-modified files, overwrites unmodified ones
6. Uninstall cleanly removes all installed artifacts
7. --dry-run reports planned actions without side effects
</verification>

<success_criteria>
- 7/7 automated lifecycle tests pass
- Human confirms installer works as expected
- Phase 3 requirements INST-01 through INST-08 are all satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/03-installer/03-03-SUMMARY.md`
</output>

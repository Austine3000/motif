---
phase: 19-global-cli-core
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - test/e2e-installer.js
autonomous: true

must_haves:
  truths:
    - "E2E tests pass when invoked via the new cli.js entry point"
    - "E2E tests verify that legacy install.js shim path still works"
    - "E2E tests verify project root detection (subdirectory install and non-project rejection)"
    - "npm pack --dry-run shows all new files included in the published package"
  artifacts:
    - path: "test/e2e-installer.js"
      provides: "Updated e2e tests covering CLI router, backward compat, and root detection"
      contains: "cli.js"
  key_links:
    - from: "test/e2e-installer.js"
      to: "bin/cli.js"
      via: "execSync invocation"
      pattern: "bin.*cli\\.js"
    - from: "test/e2e-installer.js"
      to: "bin/lib/find-root.js"
      via: "test assertions on root detection behavior"
      pattern: "find-root|findProjectRoot|project root"
---

<objective>
Update e2e tests to cover the new CLI router, backward compatibility, and project root detection. Verify package publishing readiness.

Purpose: Ensure both install paths work correctly and the refactoring has not broken any existing behavior. Confirm npm package includes all new files.
Output: Updated test/e2e-installer.js with new test cases, verified npm pack output.
</objective>

<execution_context>
@/Users/austineogbuanya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austineogbuanya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-global-cli-core/19-01-SUMMARY.md
@test/e2e-installer.js
@bin/cli.js
@bin/commands/init.js
@bin/lib/find-root.js
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update e2e tests for CLI router and root detection</name>
  <files>test/e2e-installer.js</files>
  <action>
  Update the existing e2e test file to cover Phase 19 changes. Keep all existing tests working but update paths and add new tests.

  1. **Update the INSTALLER constant** at line 10:
     - Change from `path.join(PROJECT_ROOT, 'bin', 'install.js')` to `path.join(PROJECT_ROOT, 'bin', 'cli.js')`
     - Add a LEGACY_INSTALLER constant pointing to `path.join(PROJECT_ROOT, 'bin', 'install.js')` for backward compat tests

  2. **Update runInstaller helper** (line 28):
     - The existing helper runs `node ${INSTALLER} ${args}` — this now runs cli.js
     - Add a `runLegacyInstaller(tmpDir, args)` helper that uses LEGACY_INSTALLER

  3. **Add new test section: CLI Router Tests** (before or after existing tests):
     - **Test: --help flag** — run `node ${INSTALLER} --help`, assert output contains "motif" and "init" and does not error
     - **Test: --version flag** — run `node ${INSTALLER} --version`, assert output matches a semver pattern (e.g., contains "0.")
     - **Test: explicit init subcommand** — run `node ${INSTALLER} init --dry-run --runtime claude-code` from a temp dir with `.claude/` created, assert it succeeds (exit code 0) and output contains "DRY RUN"
     - **Test: legacy no-subcommand mode** — run `node ${INSTALLER} --dry-run --runtime claude-code` from same temp dir, assert same success output. This confirms backward compatibility.

  4. **Add new test section: Project Root Detection Tests**:
     - **Test: non-project directory rejection** — run `node ${INSTALLER} init --runtime claude-code` from an empty temp dir (no .git, no package.json), assert it exits with error and output contains "Not inside a project directory"
     - **Test: subdirectory install** — create a temp dir with `.git/` at root and a `src/` subdirectory, run `node ${INSTALLER} init --dry-run --runtime claude-code` from the `src/` subdirectory, assert output contains "Installing to project root" and the parent dir path

  5. **Add new test section: Legacy Shim Test**:
     - **Test: install.js shim** — run `node ${LEGACY_INSTALLER} --dry-run --runtime claude-code` from a temp dir with `.claude/`, assert it succeeds. This confirms the shim works.

  6. **Keep all existing tests** (fresh install, upgrade, uninstall, etc.) — they should continue to work since the CLI router falls through to init for unknown-subcommand args. Update any assertions that check for specific paths if needed.

  Use try/catch with `execSync` for tests that expect failures (non-project directory), catching the error and checking `error.stderr` or `error.stdout` for expected messages.
  </action>
  <verify>
  - `node test/e2e-installer.js` passes all tests (0 failures)
  - Output shows new test sections: CLI Router, Project Root Detection, Legacy Shim
  </verify>
  <done>All e2e tests pass including new CLI router tests, root detection tests, and legacy shim test</done>
</task>

<task type="auto">
  <name>Task 2: Verify package publishing readiness</name>
  <files></files>
  <action>
  Run verification commands (no file modifications):

  1. **npm pack --dry-run** — verify output includes:
     - `bin/cli.js` (new entry point)
     - `bin/commands/init.js` (extracted init command)
     - `bin/lib/find-root.js` (root detection utility)
     - `bin/install.js` (backward compat shim)
     - All existing files (core/, runtimes/, scripts/)

  2. **Verify no dependencies** — `node -e "const p=require('./package.json'); if(p.dependencies) { console.error('FAIL: dependencies still present'); process.exit(1) } else { console.log('PASS: no dependencies') }"`

  3. **Quick smoke test of both paths:**
     - `node bin/cli.js --version` exits 0
     - `node bin/cli.js init --dry-run --runtime claude-code` from project root exits 0
     - `node bin/install.js --dry-run --runtime claude-code` from project root exits 0

  If any check fails, fix the issue in the relevant file before completing.
  </action>
  <verify>
  - `npm pack --dry-run` lists all expected files
  - All smoke tests exit 0
  - No dependencies in package.json
  </verify>
  <done>Package is ready for publishing: all files included, no self-referencing dependency, both install paths verified</done>
</task>

</tasks>

<verification>
- `node test/e2e-installer.js` — all tests pass (0 failures)
- `npm pack --dry-run` — includes bin/cli.js, bin/commands/init.js, bin/lib/find-root.js
- `node bin/cli.js init --dry-run --runtime claude-code` — succeeds from project root
- `node bin/cli.js --dry-run --runtime claude-code` — succeeds (backward compat, no subcommand)
- `cd /tmp && node {project}/bin/cli.js init` — fails with clear error about non-project directory
</verification>

<success_criteria>
1. All e2e tests pass, including new tests for CLI router, root detection, and legacy shim
2. npm pack --dry-run confirms all new files are included in published package
3. No dependencies field in package.json
4. Both `motif init` and legacy npx paths verified working
</success_criteria>

<output>
After completion, create `.planning/phases/19-global-cli-core/19-02-SUMMARY.md`
</output>

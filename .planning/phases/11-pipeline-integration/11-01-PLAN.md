---
phase: 11-pipeline-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/workflows/generate-system.md
  - core/references/context-engine.md
  - runtimes/claude-code/agents/motif-system-architect.md
autonomous: true

must_haves:
  truths:
    - "generate-system.md instructs the system architect agent to produce ICON-CATALOG.md as Output 5"
    - "generate-system.md Output 3 (DESIGN-SYSTEM.md) instructions specify a structured Iconography section instead of generic 'icon style recommendation'"
    - "generate-system.md Output 4 (token-showcase.html) instructions include icon CDN link and iconography section requirements"
    - "generate-system.md Step 3 validation list includes ICON-CATALOG.md"
    - "generate-system.md agent_spawn context includes icon-libraries.md as a numbered item"
    - "context-engine.md composer profile always_load includes ICON-CATALOG.md"
    - "context-engine.md system-generator profile load_if_exists includes icon-libraries.md"
    - "context-engine.md reviewer profile load_if_exists includes ICON-CATALOG.md"
    - "context-engine.md budget table includes ICON-CATALOG.md with 1000 token budget"
    - "motif-system-architect.md has Iconography subsection in Domain Expertise"
    - "motif-system-architect.md quality checklist includes icon-related verification items"
  artifacts:
    - path: "core/workflows/generate-system.md"
      provides: "Output 5 ICON-CATALOG.md generation instructions, expanded Output 3/4 icon requirements, icon-libraries.md in agent context"
      contains: "Output 5: ICON-CATALOG.md"
    - path: "core/references/context-engine.md"
      provides: "Updated context loading profiles with ICON-CATALOG.md for composer and reviewer"
      contains: "ICON-CATALOG.md"
    - path: "runtimes/claude-code/agents/motif-system-architect.md"
      provides: "Icon domain expertise and quality checklist for system architect"
      contains: "Iconography"
  key_links:
    - from: "core/workflows/generate-system.md"
      to: "core/references/icon-libraries.md"
      via: "agent_spawn context item 7"
      pattern: "icon-libraries\\.md"
    - from: "core/workflows/generate-system.md"
      to: ".planning/design/system/ICON-CATALOG.md"
      via: "Output 5 generation instructions"
      pattern: "ICON-CATALOG\\.md"
    - from: "core/references/context-engine.md"
      to: ".planning/design/system/ICON-CATALOG.md"
      via: "composer always_load profile entry"
      pattern: "ICON-CATALOG\\.md"
---

<objective>
Wire the system generation pipeline to produce icon-aware outputs: ICON-CATALOG.md (new Output 5), structured Iconography in DESIGN-SYSTEM.md, icon CDN in token showcase, and proper context loading profiles.

Purpose: The system architect agent currently runs the Icon Library Decision Algorithm (Phase 9) but has no instructions to generate ICON-CATALOG.md, no expanded DESIGN-SYSTEM.md iconography section, and no icon showcase requirements. The context engine doesn't know about ICON-CATALOG.md for downstream agents. This plan adds all the "generation side" of icon integration.

Output: Modified generate-system.md, context-engine.md, and motif-system-architect.md with icon-aware instructions.
</objective>

<execution_context>
@/Users/austineogbuanya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austineogbuanya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-pipeline-integration/11-RESEARCH.md
@.planning/phases/09-foundation/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Output 5 (ICON-CATALOG.md) and expand icon instructions in generate-system.md</name>
  <files>core/workflows/generate-system.md</files>
  <action>
Modify `core/workflows/generate-system.md` with these 5 additive changes:

**A. Agent spawn context (Step 2, around line 54):** After the existing item 6 (vertical reference), add:
```
7. `{MOTIF_ROOT}/references/icon-libraries.md` -- icon library metadata, selection algorithm, CDN URLs
```

**B. Output 3 (DESIGN-SYSTEM.md) instructions (line ~327):** Replace the text `icon style recommendation` with `iconography (library name, CDN link, usage syntax, size scale, color rules)`. The full line should read:
```
Include: color palette with contrast table, typography scale with usage, spacing guidelines, motion principles, iconography (library name, CDN link, usage syntax, size scale, color rules).
```

**C. Output 4 (token-showcase.html) instructions (around lines 330-336):** After the existing bullet `- Component previews (one per core component)`, add:
```
- Iconography: CDN link in <head>, size scale preview (one icon at each --icon-* token), domain icon samples (8-10 key icons from vocabulary)
- IF Lucide selected: include both CDN script AND `lucide.createIcons()` initialization
- IF Material Symbols selected: include CSS font-variation-settings for proper rendering
```

**D. New Output 5 section:** After the Output 4 section (after line ~341 `Save to ... token-showcase.html`) and before `</agent_spawn>`, add:

```markdown

## Output 5: ICON-CATALOG.md (budget: <=1000 tokens)

Generate a project-specific icon catalog by:
1. Run the Icon Library Decision Algorithm (above) to determine: library, weight, CDN URL, usage syntax
2. Read the vertical reference file's `## Icon Vocabulary` section (`{MOTIF_ROOT}/references/verticals/{VERTICAL}.md`)
3. Extract ONLY the column for the selected library
4. For each icon, construct the full class/element string using the library's usage syntax
5. Organize by the same semantic categories as the vocabulary (Navigation, Domain, Status, Actions)

Save to `.planning/design/system/ICON-CATALOG.md`

Format:
```
# Icon Catalog

Library: [name]
CDN: [url]
Weight: [default] (emphasis: [emphasis])
Usage: [syntax pattern]
{IF Lucide: Requires JS: YES}

## [Category]
| Role | Icon Name | Class |
|------|-----------|-------|
| [role] | [name] | [full element/class string] |
```

Include ALL icons from the vertical vocabulary (typically 20-25 icons).
Do NOT add icons not in the vocabulary. The vocabulary is the curated, validated set.
```

**E. Step 3 validation list (around line 351):** After `- .planning/design/system/token-showcase.html`, add:
```
- `.planning/design/system/ICON-CATALOG.md`
```

Important: All changes are ADDITIVE. Do not remove or restructure existing content. The Icon Library Decision Algorithm already exists in the file (added in Phase 9) -- do not duplicate it.
  </action>
  <verify>
Run these greps to confirm all 5 additions:
1. `grep -n 'icon-libraries.md' core/workflows/generate-system.md` -- should find the agent_spawn context entry
2. `grep -n 'iconography' core/workflows/generate-system.md` -- should find Output 3 expansion
3. `grep -n 'ICON-CATALOG' core/workflows/generate-system.md` -- should find Output 5 section AND Step 3 validation entry (at least 2 matches)
4. `grep -n 'Lucide.*createIcons\|createIcons.*Lucide' core/workflows/generate-system.md` -- should find Lucide showcase warning
5. `grep -c 'Output 5' core/workflows/generate-system.md` -- should return >= 1
  </verify>
  <done>
generate-system.md has: (1) icon-libraries.md in agent_spawn context, (2) "iconography" in Output 3 instructions, (3) icon requirements in Output 4 instructions, (4) complete Output 5 ICON-CATALOG.md section, (5) ICON-CATALOG.md in Step 3 validation list.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update context-engine.md profiles and budget table for ICON-CATALOG.md</name>
  <files>core/references/context-engine.md</files>
  <action>
Modify `core/references/context-engine.md` with these 4 additive changes:

**A. Context Budgets table (around line 35):** After the `[screen]-REVIEW.md | 1,000 each | Per-screen review` row, add:
```
| ICON-CATALOG.md | 1,000 | Per-project icon name lookup for composer |
```

**B. System Generator profile (around line 73, `<load_if_exists>`):** After the existing entry for `.claude/get-motif/verticals/{vertical}.md`, add:
```
    .claude/get-motif/references/icon-libraries.md
```

**C. Composer profile (around line 88, `<always_load>`):** After `.planning/design/system/COMPONENT-SPECS.md`, add:
```
    .planning/design/system/ICON-CATALOG.md
```
Also in the composer `<never_load>` section (around line 98), add:
```
    icon-libraries.md (already distilled into ICON-CATALOG.md)
```

**D. Reviewer profile (around line 113, `<load_if_exists>`):** After `.planning/design/screens/{screen}-SUMMARY.md`, add:
```
    .planning/design/system/ICON-CATALOG.md
```

Important: Use the INSTALLED paths (`.claude/get-motif/`) for the system-generator profile entries, matching the existing pattern in that profile. Use `.planning/design/system/` paths for ICON-CATALOG.md since that's a runtime-generated artifact, matching the existing composer profile pattern.
  </action>
  <verify>
Run these greps:
1. `grep -c 'ICON-CATALOG' core/references/context-engine.md` -- should return >= 3 (budget table, composer always_load, reviewer load_if_exists)
2. `grep -c 'icon-libraries' core/references/context-engine.md` -- should return >= 1 (system-generator load_if_exists)
3. `grep -A2 'always_load' core/references/context-engine.md | grep 'ICON-CATALOG'` -- should match (composer profile)
  </verify>
  <done>
context-engine.md has: (1) ICON-CATALOG.md in budget table at 1000 tokens, (2) icon-libraries.md in system-generator load_if_exists, (3) ICON-CATALOG.md in composer always_load, (4) icon-libraries.md in composer never_load, (5) ICON-CATALOG.md in reviewer load_if_exists.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add icon domain expertise and quality checks to system architect agent</name>
  <files>runtimes/claude-code/agents/motif-system-architect.md</files>
  <action>
Modify `runtimes/claude-code/agents/motif-system-architect.md` with these 4 additive changes:

**A. Context Loading Profile (around line 52, Load If Exists):** After the existing entry for `.claude/get-motif/references/verticals/{vertical}.md`, add:
```
- `.claude/get-motif/references/icon-libraries.md` -- icon library metadata, CDN URLs, and selection algorithm
```

**B. Domain Expertise section (around line 65):** After the existing `### Component Architecture` subsection (ends around line 81), add a new subsection:

```markdown
### Iconography
- **Icon library selection:** Deterministic algorithm from icon-libraries.md maps vertical + personality seed to library + weight. User overrides in DESIGN-BRIEF.md take priority.
- **ICON-CATALOG.md generation:** Extract the selected library's column from the vertical's Icon Vocabulary tables. Build full class strings using the library's usage syntax. Organize by semantic category (Navigation, Domain, Status, Actions).
- **Icon sizing:** Fixed 8px-multiple scale via --icon-sm through --icon-2xl tokens. Icons inherit color via CSS currentColor -- no dedicated icon color tokens.
- **Library-specific requirements:** Lucide requires JS initialization (`lucide.createIcons()`). Material Symbols requires explicit font-variation-settings CSS for proper optical sizing.
```

**C. Output Format Expectations (around line 88):** After the existing `token-showcase.html` entry, add:
```
- **`ICON-CATALOG.md`** (max 1000 tokens) -- Per-project icon name lookup table generated from vertical vocabulary. Contains library name, CDN URL, weight, usage syntax, and all icon mappings with full class strings.
```

**D. Quality Checklist (around line 106):** After the existing checklist items, add these new items:
```
- [ ] Icon Library Decision Algorithm executed -- selected library and weight documented in tokens.css comment
- [ ] DESIGN-SYSTEM.md includes Iconography section with library name, CDN link, usage syntax, size scale, color rules
- [ ] ICON-CATALOG.md generated with all icons from vertical vocabulary, correct library column, full class strings
- [ ] Token showcase HTML includes CDN link for selected icon library in <head>
- [ ] Token showcase HTML includes Iconography section with size scale preview and domain icon samples
- [ ] IF Lucide selected: showcase includes both CDN script AND lucide.createIcons() initialization
- [ ] IF Material Symbols selected: showcase includes CSS font-variation-settings for proper rendering
```

Important: Do not modify existing content. All additions go after existing sections/entries.
  </action>
  <verify>
Run these greps:
1. `grep -n 'Iconography' runtimes/claude-code/agents/motif-system-architect.md` -- should find Domain Expertise subsection header
2. `grep -n 'ICON-CATALOG' runtimes/claude-code/agents/motif-system-architect.md` -- should find Output Format and Quality Checklist entries (>= 2 matches)
3. `grep -n 'icon-libraries.md' runtimes/claude-code/agents/motif-system-architect.md` -- should find Context Loading Profile entry
4. `grep -n 'lucide.createIcons' runtimes/claude-code/agents/motif-system-architect.md` -- should find quality checklist entry
  </verify>
  <done>
motif-system-architect.md has: (1) icon-libraries.md in Load If Exists, (2) Iconography subsection under Domain Expertise, (3) ICON-CATALOG.md in Output Format Expectations, (4) 7 icon-related Quality Checklist items including Lucide and Material Symbols conditionals.
  </done>
</task>

</tasks>

<verification>
After all 3 tasks complete, verify the full chain:
1. `grep -rn 'ICON-CATALOG' core/workflows/generate-system.md core/references/context-engine.md runtimes/claude-code/agents/motif-system-architect.md` -- ICON-CATALOG.md referenced across all 3 files
2. `grep -rn 'icon-libraries.md' core/workflows/generate-system.md core/references/context-engine.md runtimes/claude-code/agents/motif-system-architect.md` -- icon-libraries.md referenced in generate workflow, context engine, and architect agent
3. `grep -c 'Output 5' core/workflows/generate-system.md` -- Output 5 section exists
4. `grep -n 'iconography' core/workflows/generate-system.md` -- Output 3 expanded with iconography
</verification>

<success_criteria>
- generate-system.md has Output 5 (ICON-CATALOG.md), expanded Output 3 and 4 with icon requirements, icon-libraries.md in agent context, and ICON-CATALOG.md in Step 3 validation
- context-engine.md budget table includes ICON-CATALOG.md; composer always_load includes ICON-CATALOG.md; system-generator load_if_exists includes icon-libraries.md; reviewer load_if_exists includes ICON-CATALOG.md
- motif-system-architect.md has Iconography domain expertise, ICON-CATALOG.md output format, and 7 icon quality checklist items
</success_criteria>

<output>
After completion, create `.planning/phases/11-pipeline-integration/11-01-SUMMARY.md`
</output>

---
phase: 11-pipeline-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - core/workflows/compose-screen.md
  - runtimes/claude-code/agents/motif-screen-composer.md
  - core/templates/token-showcase-template.html
autonomous: true

must_haves:
  truths:
    - "compose-screen.md REQUIRED_FILES list includes ICON-CATALOG.md"
    - "compose-screen.md agent_spawn context files list includes ICON-CATALOG.md with icon usage instruction"
    - "compose-screen.md has an icon compliance rule (B.9) in Implementation Rules"
    - "compose-screen.md Anti-Slop Check has icon-related entries"
    - "compose-screen.md Self-Review Checklist has icon verification items"
    - "motif-screen-composer.md always_load includes ICON-CATALOG.md"
    - "motif-screen-composer.md anti-slop checklist has icon name and icon size checks"
    - "motif-screen-composer.md never_load includes icon-libraries.md"
    - "token-showcase-template.html has an {ICON_CDN_LINK} placeholder in head"
    - "token-showcase-template.html has an iconography section with icon-size-grid and icon-preview-grid"
    - "token-showcase-template.html has CSS classes for icon-meta, icon-size-grid, icon-size-item, icon-preview-grid, icon-preview-item"
  artifacts:
    - path: "core/workflows/compose-screen.md"
      provides: "Icon compliance rules and ICON-CATALOG.md context loading for screen composition"
      contains: "ICON-CATALOG.md"
    - path: "runtimes/claude-code/agents/motif-screen-composer.md"
      provides: "Icon anti-slop rules and context profile update for composer agent"
      contains: "ICON-CATALOG.md"
    - path: "core/templates/token-showcase-template.html"
      provides: "Iconography section template with CDN placeholder, size scale grid, and domain icon grid"
      contains: "iconography"
  key_links:
    - from: "core/workflows/compose-screen.md"
      to: ".planning/design/system/ICON-CATALOG.md"
      via: "REQUIRED_FILES and agent_spawn context item 5"
      pattern: "ICON-CATALOG\\.md"
    - from: "runtimes/claude-code/agents/motif-screen-composer.md"
      to: ".planning/design/system/ICON-CATALOG.md"
      via: "always_load context profile entry"
      pattern: "ICON-CATALOG\\.md"
    - from: "core/templates/token-showcase-template.html"
      to: "icon CDN"
      via: "{ICON_CDN_LINK} placeholder in head"
      pattern: "ICON_CDN_LINK"
---

<objective>
Wire the screen composition pipeline and token showcase to use concrete icons: the composer reads ICON-CATALOG.md for icon names, and the token showcase template includes an iconography preview section.

Purpose: The compose workflow and composer agent currently have no awareness of ICON-CATALOG.md. Composed screens still risk bracket placeholders or hallucinated icon names. The token showcase has no iconography section. This plan adds all the "consumption side" of icon integration.

Output: Modified compose-screen.md, motif-screen-composer.md, and token-showcase-template.html with icon-aware instructions and template structure.
</objective>

<execution_context>
@/Users/austineogbuanya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austineogbuanya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-pipeline-integration/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add icon compliance to compose-screen.md workflow and motif-screen-composer.md agent</name>
  <files>core/workflows/compose-screen.md, runtimes/claude-code/agents/motif-screen-composer.md</files>
  <action>
**Part A: Modify `core/workflows/compose-screen.md`**

5 additive changes:

**A1. REQUIRED_FILES (Step 2, around line 34):** After `- .planning/design/DESIGN-RESEARCH.md`, add:
```
  - .planning/design/system/ICON-CATALOG.md
```

**A2. OPTIONAL_FILES note:** After the existing `OPTIONAL_FILES` block (around line 37-38), add a comment:
```
Note: If ICON-CATALOG.md does not exist, warn the user:
"No icon catalog found -- icon names may be inconsistent. Re-run /motif:system to generate."
```

**A3. Agent spawn context files (Step 3, around line 63):** After item 4 (DESIGN-RESEARCH.md), add:
```
5. `.planning/design/system/ICON-CATALOG.md` -- icon name lookup. Use ONLY these icon names.
```
Also renumber the existing conditional item 5 (previous summaries) to item 6.

**A4. Implementation Rules (Section B, around line 87):** After rule 8 (Vertical patterns), add:
```
9. **Icon compliance:** Use ONLY icon names from ICON-CATALOG.md. Every icon must use:
   - The exact class/element syntax from the catalog's "Class" column
   - An `--icon-{scale}` token for sizing (`--icon-sm` through `--icon-2xl`)
   - `currentColor` for color (set via parent element's `color` property)
   - NEVER invent icon names. If a needed icon isn't in the catalog, use the closest semantic match and note it in SUMMARY.md.
   - NEVER use bracket placeholders like `[icon]` or `[MerchantIcon]`.
```

**A5. Anti-Slop Check (Section C, around line 94):** After the existing checks, add:
```
- Am I using an icon name not in ICON-CATALOG.md? -> STOP. Look up the semantic role in the catalog. Use the exact Class string.
- Am I hardcoding an icon size in px? -> STOP. Use var(--icon-sm) through var(--icon-2xl).
- Am I using a bracket placeholder like [icon: ...]? -> STOP. Replace with the actual icon element from the catalog.
```

**A6. Self-Review Checklist (Section D, around line 103):** After the existing checklist items, add:
```
- [ ] All icon names exist in ICON-CATALOG.md (no invented names)
- [ ] All icon sizes use --icon-* tokens (no hardcoded font-size for icons)
- [ ] Icon CDN link present in <head> (or inherited from framework)
- [ ] IF Lucide: createIcons() called after DOM load
```

---

**Part B: Modify `runtimes/claude-code/agents/motif-screen-composer.md`**

4 additive changes:

**B1. Always Load (around line 49):** After `.planning/design/system/COMPONENT-SPECS.md`, add:
```
- `.planning/design/system/ICON-CATALOG.md` -- icon name lookup table; use ONLY these icon names and class strings
```

**B2. Never Load (around line 58):** After `DESIGN-SYSTEM.md` entry, add:
```
- `icon-libraries.md` -- already distilled into ICON-CATALOG.md; composer does not need library metadata
```

**B3. Anti-Slop Checklist (around line 98):** After item 8 (generic icon set check), add:
```
9. **Am I using an icon name not in ICON-CATALOG.md?** STOP. Look up the semantic role in the catalog. Use the exact Class string from the catalog.
10. **Am I hardcoding an icon size in px?** STOP. Use `var(--icon-sm)` through `var(--icon-2xl)`.
11. **Am I using a bracket placeholder like [icon: ...]?** STOP. Replace with the actual icon element from ICON-CATALOG.md.
```

**B4. Self-Review Checklist (around line 119):** After the existing checklist items, add:
```
- [ ] All icon names exist in ICON-CATALOG.md -- no invented or hallucinated icon names
- [ ] All icon sizes use `--icon-*` tokens -- no hardcoded `font-size` values for icons
- [ ] Icon CDN `<link>` or `<script>` present in `<head>` (or inherited from framework layout)
- [ ] IF Lucide project: `lucide.createIcons()` called after DOM load
```

Important: All changes are ADDITIVE. Do not restructure or remove existing content.
  </action>
  <verify>
Run these greps:
1. `grep -c 'ICON-CATALOG' core/workflows/compose-screen.md` -- should return >= 4 (REQUIRED_FILES, context list, implementation rule, self-review)
2. `grep -c 'ICON-CATALOG' runtimes/claude-code/agents/motif-screen-composer.md` -- should return >= 4 (always_load, anti-slop items, self-review)
3. `grep -n 'icon-libraries.md' runtimes/claude-code/agents/motif-screen-composer.md` -- should find never_load entry
4. `grep -n 'Icon compliance' core/workflows/compose-screen.md` -- should find implementation rule B.9
5. `grep -n 'bracket placeholder' core/workflows/compose-screen.md` -- should find anti-slop check
  </verify>
  <done>
compose-screen.md has ICON-CATALOG.md in REQUIRED_FILES, agent context, implementation rules (B.9), anti-slop checks, and self-review checklist. motif-screen-composer.md has ICON-CATALOG.md in always_load, icon-libraries.md in never_load, 3 icon anti-slop items, and 4 icon self-review checks.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add iconography section and CDN placeholder to token-showcase-template.html</name>
  <files>core/templates/token-showcase-template.html</files>
  <action>
Modify `core/templates/token-showcase-template.html` with these 3 additive changes:

**A. Head section (around line 8):** After the Google Fonts `<link>`, add:
```html
  <!-- Icon library CDN -- agent fills based on selection algorithm -->
  {ICON_CDN_LINK}
  {ICON_INIT_SCRIPT}
```

**B. Style section:** After the existing `.vertical-placeholder` CSS block (around line 473) and before the `@media` responsive rule, add these new CSS rules:

```css
    /* --- Icon Preview --- */
    .icon-meta {
      font-family: var(--font-mono);
      font-size: var(--text-sm);
      color: var(--text-secondary);
      margin-bottom: var(--space-4);
    }
    .icon-size-grid {
      display: flex;
      align-items: flex-end;
      gap: var(--space-8);
      margin-bottom: var(--space-6);
    }
    .icon-size-item {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: var(--space-2);
    }
    .icon-preview-grid {
      display: flex;
      flex-wrap: wrap;
      gap: var(--space-6);
    }
    .icon-preview-item {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: var(--space-2);
      min-width: 80px;
    }
```

**C. Body section:** BEFORE the existing `<!-- SECTION: VERTICAL-SPECIFIC -->` comment (around line 931), add a new section:

```html
    <!-- ================================================================
         SECTION: ICONOGRAPHY
         ================================================================ -->
    <section id="iconography">
      <h2>Iconography</h2>

      <div class="subsection">
        <p class="subsection-title">Icon Library</p>
        <!-- Agent fills: library name, weight, icon count -->
        <p class="icon-meta">{LIBRARY_NAME} | {WEIGHT} | {ICON_COUNT} icons</p>
      </div>

      <div class="subsection">
        <p class="subsection-title">Size Scale</p>
        <div class="icon-size-grid">
          <!-- Agent fills: same reference icon at each size token -->
          <div class="icon-size-item">
            {ICON_ELEMENT_SM}
            <span class="swatch-label">--icon-sm<br>16px</span>
          </div>
          <div class="icon-size-item">
            {ICON_ELEMENT_MD}
            <span class="swatch-label">--icon-md<br>20px</span>
          </div>
          <div class="icon-size-item">
            {ICON_ELEMENT_LG}
            <span class="swatch-label">--icon-lg<br>24px</span>
          </div>
          <div class="icon-size-item">
            {ICON_ELEMENT_XL}
            <span class="swatch-label">--icon-xl<br>32px</span>
          </div>
          <div class="icon-size-item">
            {ICON_ELEMENT_2XL}
            <span class="swatch-label">--icon-2xl<br>40px</span>
          </div>
        </div>
      </div>

      <div class="subsection">
        <p class="subsection-title">Domain Icons</p>
        <div class="icon-preview-grid">
          <!-- Agent fills: 8-10 key icons from the vertical vocabulary -->
          {ICON_PREVIEW_ITEMS}
        </div>
      </div>
    </section>

```

Important notes:
- The `{ICON_CDN_LINK}` placeholder follows the same pattern as `{FONT_FAMILIES}` -- the system architect agent replaces it during generation
- The `{ICON_INIT_SCRIPT}` placeholder is for Lucide's `<script>lucide.createIcons()</script>` -- empty for other libraries
- The `{ICON_ELEMENT_SM}` through `{ICON_ELEMENT_2XL}` placeholders are for the same icon at different sizes -- agent fills with library-specific element (e.g., `<i class="ph ph-house" style="font-size: var(--icon-sm);"></i>`)
- The `{ICON_PREVIEW_ITEMS}` placeholder is for 8-10 domain icon preview items, each as: `<div class="icon-preview-item">{ICON_ELEMENT}<span class="swatch-label">{role}</span></div>`
- Use HTML comment separators matching the existing style (double line equals signs)
  </action>
  <verify>
Run these checks:
1. `grep -n 'ICON_CDN_LINK' core/templates/token-showcase-template.html` -- should find head placeholder
2. `grep -n 'ICON_INIT_SCRIPT' core/templates/token-showcase-template.html` -- should find head placeholder
3. `grep -n 'iconography' core/templates/token-showcase-template.html` -- should find section id
4. `grep -n 'icon-size-grid' core/templates/token-showcase-template.html` -- should find CSS class and HTML element
5. `grep -n 'icon-preview-grid' core/templates/token-showcase-template.html` -- should find CSS class and HTML element
6. `grep -n 'icon-meta' core/templates/token-showcase-template.html` -- should find CSS class and HTML element
7. Verify the iconography section appears BEFORE the vertical-specific section in the HTML body
  </verify>
  <done>
token-showcase-template.html has: (1) {ICON_CDN_LINK} and {ICON_INIT_SCRIPT} placeholders in head, (2) icon-meta, icon-size-grid, icon-size-item, icon-preview-grid, icon-preview-item CSS classes, (3) Iconography section with library info, size scale grid, and domain icon grid, positioned before the Vertical-Specific section.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full chain:
1. `grep -rn 'ICON-CATALOG' core/workflows/compose-screen.md runtimes/claude-code/agents/motif-screen-composer.md` -- referenced in both workflow and agent
2. `grep -n 'iconography' core/templates/token-showcase-template.html` -- section exists in template
3. `grep -n 'ICON_CDN_LINK' core/templates/token-showcase-template.html` -- CDN placeholder in head
4. The composer workflow REQUIRED_FILES and the composer agent always_load both list ICON-CATALOG.md
5. The icon anti-slop checks exist in both compose-screen.md and motif-screen-composer.md
</verification>

<success_criteria>
- compose-screen.md has ICON-CATALOG.md in REQUIRED_FILES, context files, implementation rules, anti-slop checks, and self-review checklist
- motif-screen-composer.md has ICON-CATALOG.md in always_load, icon-libraries.md in never_load, icon anti-slop items, and icon self-review checks
- token-showcase-template.html has CDN placeholder in head, iconography CSS, and iconography section with size grid and domain icon grid
</success_criteria>

<output>
After completion, create `.planning/phases/11-pipeline-integration/11-02-SUMMARY.md`
</output>

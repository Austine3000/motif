---
phase: 28-auto-run-cleanup-and-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/get-motif/scripts/runtime-launcher.js
  - .claude/get-motif/scripts/auto-run-session-store.js
  - .claude/get-motif/scripts/phase28-runtime-cleanup-check.js
  - .claude/get-motif/references/framework-registry.json
  - .claude/get-motif/workflows/compose-screen.md
  - .planning/STATE.md
autonomous: true
requirements:
  - ARUN-03
---

<objective>
Make the auto-run cleanup logic deterministic so that launched servers never leak ports: track PIDs, persist session metadata, and ensure SIGINT/term/unclean exits kill everything that the launcher spawned. Closing `ARUN-03` also finishes the proof-of-life for the Next.js flow because the audit flow gap names the same cleanup behavior.
Purpose: The audit says session tracking is implemented but PID cleanup is unverified, meaning Phase 25 works only when a human babysits the terminal. This phase creates the cleanup guardrails, ties them into STATE or a session store, and provides a harness to demonstrate that each tracked PID is killed on exit before the next run.
</objective>

<execution_context>
@/Users/austineogbuanya/.codex/get-shit-done/workflows/execute-plan.md
@/Users/austineogbuanya/.codex/get-shit-done/workflows/phase-validation.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.4-MILESTONE-AUDIT.md
@.claude/get-motif/scripts/runtime-launcher.js
@.claude/get-motif/references/framework-registry.json
@CLAUDE.md
</context>

<tasks>

<task type="auto" id="28-01-01" wave="1">
  <name>Persist launched session metadata</name>
  <files>.claude/get-motif/scripts/runtime-launcher.js, .claude/get-motif/scripts/auto-run-session-store.js</files>
  <action>
    Create `auto-run-session-store.js` to manage tracked PIDs (spawned dev servers, browser opener tokens) and persist them near `.planning/STATE.md`. Update `runtime-launcher.js` to record each spawned PID, start time, platform, and preview URL, and to write the serialized session to the store before handing control back to the user. Add an API to query the store so cleanup logic can enumerate live sessions.
  </action>
  <verify>
    <automated>
      <check command="node .claude/get-motif/scripts/auto-run-session-store.js --report">Shows stored sessions after a dry-run launch.</check>
      <check command="node .claude/get-motif/scripts/runtime-launcher.js --platform web-nextjs --dry-run">Ensures the launcher writes metadata for its daemon/preview sessions without blocking the terminal.</check>
    </automated>
  </verify>
  <done>The launcher now records PID metadata into a persistent store so `ARUN-03` can reason over tracked sessions when it performs cleanup.</done>
</task>

<task type="auto" id="28-01-02" wave="1" depends_on="28-01-01">
  <name>Kill tracked PIDs on exit and verify port cleanup</name>
  <files>.claude/get-motif/scripts/runtime-launcher.js, .claude/get-motif/scripts/phase28-runtime-cleanup-check.js, .claude/get-motif/references/framework-registry.json</files>
  <action>
    Extend `runtime-launcher.js` with signal handlers (SIGINT, SIGTERM, exit) that read the session store and terminate tracked PIDs in reverse order, respecting Windows and Unix semantics. Create `phase28-runtime-cleanup-check.js` as the Wave 0 harness that simulates servers (spawning sleep loops) so the cleanup logic can be exercised without real runtimes. Ensure the launcher also cleans up stale sessions before launching new ones. Document the cleanup expectations inside the registry metadata so downstream flows know the contract (e.g., `cleanupTriggers` field).
  </action>
  <verify>
    <automated>
      <check command="node .claude/get-motif/scripts/phase28-runtime-cleanup-check.js">Confirms the cleanup harness can launch stub servers, send SIGINT/SIGTERM, and verify the ports are freed.</check>
      <check command="node .claude/get-motif/scripts/runtime-launcher.js --platform web-nextjs --dry-run --signal SIGINT">Ensures the launcher runs, records a session, and then cleans up tracked PIDs in response to SIGINT before exiting.</check>
    </automated>
  </verify>
  <done>PID cleanup runs reliably, keeping ports free and letting `auto-run` restart without orphaning daemons, which closes `ARUN-03`.</done>
</task>

</tasks>

<verification>
1. Auto-run session store captures every process the launcher spawns and persists the record near `.planning/STATE.md` so other tooling can query it.
2. Sending signals/emulating crashes kills tracked PIDs: the cleanup harness proves the launcher releases ports and windows `cmd start` processes even when the user aborts.
3. No processes remain in `ps` after forcing a cleanup run, satisfying the audit's requirement that `ARUN-03` handles zombie servers.
</verification>

<success_criteria>
- PIDs for dev servers and preview openers are tracked, persisted, and queryable before and after auto-run flows.
- Cleanup handlers kill the same PIDs on SIGINT/SIGTERM/exit, freeing ports so the next run can start without conflicts.
- The manual validation notes from Phase 25 now state that cleanup is automated, resolving the integration/flow gap described in the audit.
</success_criteria>

<output>
After implementation, update `.planning/phases/28-auto-run-cleanup-and-validation/28-01-SUMMARY.md` with verification results and follow-up actions.
</output>

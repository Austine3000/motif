---
phase: 32-reliability-enhancements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/get-motif/workflows/compose-screen.md
autonomous: true

must_haves:
  truths:
    - "After /clear mid-batch, running /motif:compose --all prints which screens are being skipped and which are being composed"
    - "Already-composed screens are not re-composed on resume"
    - "Stale files from an interrupted wave are warned about but not deleted"
  artifacts:
    - path: ".claude/get-motif/workflows/compose-screen.md"
      provides: "Resume detection and messaging in Step 1b"
      contains: "Resuming batch"
  key_links:
    - from: "compose-screen.md Step 1b"
      to: "STATE.md screen statuses"
      via: "motif-state.js read"
      pattern: "composed.*reviewed.*skipped"
---

<objective>
Add batch resume detection and user messaging to compose-screen.md Step 1b so that `/motif:compose --all` after a `/clear` explicitly reports which screens are being skipped (already composed) and which will be composed.

Purpose: REL-02 requires resilient batch resume. The underlying mechanism already works (Phase 29 filters by status), but the user gets no feedback about what is happening. This plan adds explicit resume detection, skip reporting, and stale file warnings.
Output: Updated compose-screen.md with Step 1b steps 4-6 for resume detection
</objective>

<execution_context>
@/Users/austineogbuanya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austineogbuanya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-reliability-enhancements/32-RESEARCH.md
@.planning/phases/29-core-batch-orchestration/29-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add resume detection and stale file warning to Step 1b</name>
  <files>.claude/get-motif/workflows/compose-screen.md</files>
  <action>
In compose-screen.md Step 1b (starts at "## Step 1b: Batch Validation (BATCH MODE only)"), add three new numbered steps between the existing step 3 ("Calculate waves") and step 4 ("Display batch plan"). The existing step 4 becomes step 7. Insert:

**New step 4: Detect resume scenario.**
After collecting SCREEN_LIST (screens with status `planned` or `failed`), read the full screens array from STATE.md. Check if any screens have status `composed`, `reviewed`, or `fixed`. If so:
- SKIPPED_SCREENS = names of screens NOT in SCREEN_LIST (because their status is `composed`, `reviewed`, or `fixed`)
- If SKIPPED_SCREENS is not empty, print:
  "Resuming batch -- skipping {SKIPPED_SCREENS.length} already-composed screen(s): {comma-separated names}"
  "Composing remaining {SCREEN_LIST.length} screen(s): {comma-separated names}"

**New step 5: Check for stale files from previous attempt.**
For each screen in SCREEN_LIST, check if `.planning/design/screens/{screen_name}-SUMMARY.md` exists on disk (use `test -f`). Collect any matches into STALE_SCREENS list.
- If STALE_SCREENS is not empty, print:
  "Note: {STALE_SCREENS.length} screen(s) have files from a previous attempt: {comma-separated names}. These will be overwritten."

**New step 6: Calculate waves.** (This is the OLD step 3 moved here -- waves should be calculated AFTER resume detection so the wave count reflects only the screens being composed, not skipped ones.)
- WAVE_COUNT = ceil(SCREEN_LIST.length / CONCURRENCY)
- WAVES = split SCREEN_LIST into chunks of CONCURRENCY size

Move the old step 3 content into the new step 6, and renumber old step 4 ("Display batch plan") to step 7.

The final Step 1b numbering should be:
1. Validate screen names (existing)
2. Check for empty list (existing)
3. (removed -- wave calculation moved to step 6)
4. Detect resume scenario (NEW)
5. Check for stale files (NEW)
6. Calculate waves (moved from old step 3)
7. Display batch plan (renumbered from old step 4)

IMPORTANT: Do NOT change the "Proceed to Step 3b" instruction at the end of Step 1b. It must still say to skip Steps 2, 2b, 2c, 2d, and 3.
  </action>
  <verify>
Read compose-screen.md Step 1b and confirm:
1. Steps are numbered 1 through 7
2. Step 4 contains "Resuming batch" resume detection logic
3. Step 5 contains stale file check with "files from a previous attempt"
4. Step 6 contains wave calculation (WAVE_COUNT, WAVES)
5. Step 7 contains "Display batch plan"
6. The "Proceed to Step 3b" instruction at the end is unchanged
  </verify>
  <done>Step 1b has 7 steps with resume detection at step 4, stale file warning at step 5, wave calculation at step 6, and batch plan display at step 7. No other steps or sections modified.</done>
</task>

</tasks>

<verification>
1. Read compose-screen.md from Step 1b through the "Proceed to Step 3b" line
2. Confirm resume detection logic references `composed`, `reviewed`, `fixed` statuses
3. Confirm stale file check uses `.planning/design/screens/{screen_name}-SUMMARY.md` path pattern
4. Confirm wave calculation (WAVE_COUNT, WAVES) is in step 6
5. Confirm no changes outside Step 1b
</verification>

<success_criteria>
- Step 1b has explicit resume detection that reports skipped screens by name
- Step 1b warns about stale files from interrupted waves without deleting them
- All existing Step 1b behavior (name validation, fuzzy matching, empty check) preserved
- Wave calculation happens after resume detection (correct ordering)
</success_criteria>

<output>
After completion, create `.planning/phases/32-reliability-enhancements/32-01-SUMMARY.md`
</output>

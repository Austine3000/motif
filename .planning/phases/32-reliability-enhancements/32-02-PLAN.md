---
phase: 32-reliability-enhancements
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - .claude/get-motif/workflows/compose-screen.md
autonomous: true

must_haves:
  truths:
    - "Foundational screens (layout, nav, home, landing) automatically compose in earlier waves before feature screens"
    - "Feature screen agents in wave 2+ receive foundation screen SUMMARY.md paths for cross-screen consistency"
    - "When no foundational screens are detected or all screens fit in one wave, behavior is identical to pre-Phase-32"
  artifacts:
    - path: ".claude/get-motif/workflows/compose-screen.md"
      provides: "Smart screen ordering in Step 3b and foundation summary injection in Step 3b.2"
      contains: "Smart ordering"
  key_links:
    - from: "compose-screen.md Step 3b pre-wave ordering"
      to: "SCREEN_LIST from Step 1b"
      via: "name-based classification into FOUNDATION_SCREENS and FEATURE_SCREENS"
      pattern: "FOUNDATION_PATTERNS.*layout.*nav"
    - from: "compose-screen.md Step 3b.2"
      to: ".planning/design/screens/{foundation_screen}-SUMMARY.md"
      via: "conditional context file injection for wave 2+ agents"
      pattern: "Foundation Screen Summaries"
---

<objective>
Add smart screen ordering to compose-screen.md Step 3b so foundational screens (layout, navigation, home) compose in wave 1 and feature screens compose in subsequent waves with access to foundation SUMMARY.md files.

Purpose: REL-03 requires that foundational screens compose first so feature screens can reference their established patterns. This improves cross-screen consistency by ensuring layout, navigation, and visual identity decisions are made before feature-specific screens begin.
Output: Updated compose-screen.md with pre-wave ordering logic and foundation summary injection
</objective>

<execution_context>
@/Users/austineogbuanya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austineogbuanya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-reliability-enhancements/32-RESEARCH.md
@.planning/phases/32-reliability-enhancements/32-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pre-wave smart screen ordering to Step 3b</name>
  <files>.claude/get-motif/workflows/compose-screen.md</files>
  <action>
In compose-screen.md Step 3b ("## Step 3b: Batch Wave Dispatch (BATCH MODE only)"), insert a new section AFTER the "Pre-wave context assembly" block and BEFORE the "For each wave" loop. Title it "**Pre-wave screen ordering (run ONCE before the wave loop):**"

Content of the new section:

1. Define classification patterns:
   - FOUNDATION_PATTERNS = ["layout", "nav", "navigation", "home", "landing", "shell", "frame"]
   - FOUNDATION_EXACT = ["header", "footer", "sidebar"]

2. Classify each screen in SCREEN_LIST:
   - name_lower = screen name lowercased
   - If name_lower exactly matches any entry in FOUNDATION_EXACT, OR any pattern in FOUNDATION_PATTERNS is a substring of name_lower: classify as FOUNDATIONAL
   - Otherwise: classify as FEATURE

3. Separate into two lists:
   - FOUNDATION_SCREENS = all screens classified as FOUNDATIONAL
   - FEATURE_SCREENS = all screens classified as FEATURE

4. Apply ordering only when it matters:
   - If FOUNDATION_SCREENS is not empty AND SCREEN_LIST.length > CONCURRENCY:
     - Print: "Smart ordering: {FOUNDATION_SCREENS.length} foundational screen(s) will compose first: {comma-separated names}"
     - ORDERED_LIST = FOUNDATION_SCREENS + FEATURE_SCREENS
   - Else:
     - ORDERED_LIST = SCREEN_LIST (unchanged)

5. Recalculate waves from ORDERED_LIST (overrides Step 1b step 6 calculation):
   - WAVE_COUNT = ceil(ORDERED_LIST.length / CONCURRENCY)
   - WAVES = split ORDERED_LIST into chunks of CONCURRENCY size

6. Track foundation wave boundary:
   - FOUNDATION_WAVE_COUNT = ceil(FOUNDATION_SCREENS.length / CONCURRENCY)
   - This tells us waves 1 through FOUNDATION_WAVE_COUNT contain foundational screens

IMPORTANT: The existing "For each wave" loop and all substeps (3b.1 through 3b.10) must use ORDERED_LIST / WAVES as recalculated here, not the original SCREEN_LIST from Step 1b. The "Initialize tracking lists" block (ALL_RESULTS, TOTAL_SUCCEEDED, TOTAL_FAILED) must remain BEFORE this new section.
  </action>
  <verify>
Read compose-screen.md Step 3b and confirm:
1. "Pre-wave screen ordering" section exists between context assembly and the wave loop
2. FOUNDATION_PATTERNS and FOUNDATION_EXACT are defined with all 10 patterns
3. Classification logic checks both substring match and exact match
4. Ordering only applies when FOUNDATION_SCREENS is non-empty AND SCREEN_LIST > CONCURRENCY
5. WAVES is recalculated from ORDERED_LIST
6. FOUNDATION_WAVE_COUNT is tracked
  </verify>
  <done>Step 3b has a pre-wave ordering section that classifies screens and reorders FOUNDATION_SCREENS before FEATURE_SCREENS. When no foundational screens exist or all screens fit in one wave, ordering is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add foundation summary injection to Step 3b.2</name>
  <files>.claude/get-motif/workflows/compose-screen.md</files>
  <action>
In compose-screen.md Step 3b.2 ("### 3b.2: Spawn parallel Task() agents"), add a conditional block AFTER the existing BATCH MODE INSTRUCTIONS template and BEFORE the "Spawn ALL agents for this wave in a single message" instruction. The block adds foundation screen summaries to the Task prompt for feature waves.

Add this logic:

```
**Foundation summary injection (wave 2+ only):**

If wave_index >= FOUNDATION_WAVE_COUNT AND FOUNDATION_SCREENS is not empty:
  Collect FOUNDATION_SUMMARIES = paths of SUMMARY.md files from foundational screens that were composed in earlier waves (check ALL_RESULTS for these screens with result PASSED or WARNED):
  - For each foundation screen name in FOUNDATION_SCREENS:
    - If the screen appears in ALL_RESULTS with result PASSED or WARNED:
      - Add `.planning/design/screens/{foundation_screen_name}-SUMMARY.md` to FOUNDATION_SUMMARIES
  - Cap FOUNDATION_SUMMARIES at 3 paths maximum (prioritize: layout-containing names first, then nav-containing, then others)

  If FOUNDATION_SUMMARIES is not empty, append this block to the Task prompt AFTER the BATCH MODE INSTRUCTIONS block:

  ## Foundation Screen Summaries (for cross-screen consistency)
  These foundational screens were composed in earlier waves. Read them for layout structure, navigation patterns, and visual consistency:
  {for each path in FOUNDATION_SUMMARIES:}
  - Read: `{path}`
  {endfor}
```

IMPORTANT: This block goes INSIDE the "For each screen in the current wave" loop but is conditional on wave_index. It does NOT execute for wave 1 (foundation waves). The existing BATCH MODE INSTRUCTIONS block must remain unchanged -- the foundation summary block is appended AFTER it, not merged into it.
  </action>
  <verify>
Read compose-screen.md Step 3b.2 and confirm:
1. Foundation summary injection block exists after BATCH MODE INSTRUCTIONS
2. Condition checks wave_index >= FOUNDATION_WAVE_COUNT AND FOUNDATION_SCREENS not empty
3. Summaries collected only from ALL_RESULTS with PASSED or WARNED status
4. Cap at 3 summaries with layout > nav > others priority
5. Block appended to Task prompt, not merged into BATCH MODE INSTRUCTIONS
6. Original BATCH MODE INSTRUCTIONS template unchanged
  </verify>
  <done>Step 3b.2 conditionally injects up to 3 foundation screen SUMMARY.md paths into feature wave Task prompts, giving feature screen agents knowledge of established layout and navigation patterns.</done>
</task>

</tasks>

<verification>
1. Read compose-screen.md Step 3b from start through 3b.2
2. Confirm pre-wave ordering section exists between context assembly and wave loop
3. Confirm foundation summary injection is conditional on wave_index >= FOUNDATION_WAVE_COUNT
4. Confirm no changes to Steps 3b.3 through 3b.10 (wave result collection, commits, state updates, review dispatch all unchanged)
5. Confirm BATCH MODE INSTRUCTIONS block in 3b.2 is byte-for-byte identical to pre-edit
</verification>

<success_criteria>
- Foundational screens (matching name patterns) are automatically placed in earlier waves
- Feature screens in wave 2+ receive foundation SUMMARY.md paths in their Task prompt
- When no foundational screens detected, wave ordering identical to Phase 29 behavior
- When all screens fit in one wave (count <= concurrency), no reordering applied
- Foundation summary injection capped at 3 to prevent context bloat
</success_criteria>

<output>
After completion, create `.planning/phases/32-reliability-enhancements/32-02-SUMMARY.md`
</output>

---
phase: 12-enforcement-and-validation
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - .claude/get-motif/agents/motif-design-reviewer.md
  - .claude/get-motif/hooks/motif-aria-check.js
  - .claude/get-motif/workflows/review.md
autonomous: false

must_haves:
  truths:
    - "The installed copy of motif-design-reviewer.md matches the runtimes/ source copy exactly (zero diff)"
    - "The installed copy of motif-aria-check.js matches the runtimes/ source copy exactly (zero diff)"
    - "The installed copy of review.md matches the core/ source copy exactly (zero diff)"
    - "End-to-end: the full pipeline is verified to produce screens with icon enforcement and zero icon-related gaps in the enforcement layer"
  artifacts:
    - path: ".claude/get-motif/agents/motif-design-reviewer.md"
      provides: "Installed reviewer with icon checks"
      contains: "Icon Catalog Compliance"
    - path: ".claude/get-motif/hooks/motif-aria-check.js"
      provides: "Installed aria-check hook with icon detection"
      contains: "findIconElements"
    - path: ".claude/get-motif/workflows/review.md"
      provides: "Installed review workflow with ICON-CATALOG.md context"
      contains: "ICON-CATALOG.md"
  key_links:
    - from: "runtimes/claude-code/agents/motif-design-reviewer.md"
      to: ".claude/get-motif/agents/motif-design-reviewer.md"
      via: "Direct file copy sync"
      pattern: "diff.*0"
    - from: "runtimes/claude-code/hooks/motif-aria-check.js"
      to: ".claude/get-motif/hooks/motif-aria-check.js"
      via: "Direct file copy sync"
      pattern: "diff.*0"
    - from: "core/workflows/review.md"
      to: ".claude/get-motif/workflows/review.md"
      via: "Direct file copy sync"
      pattern: "diff.*0"
---

<objective>
Sync Plan 12-01 source changes to installed copies and verify the complete icon enforcement pipeline end-to-end.

Purpose: Installed copies in .claude/get-motif/ are what agents actually read at runtime. Source changes in runtimes/ and core/ must be synced. End-to-end verification confirms the full pipeline (system generation -> composition -> review) works with icon enforcement.

Output: Synced installed files, end-to-end pipeline verification confirming icon enforcement is operational.
</objective>

<execution_context>
@/Users/austineogbuanya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austineogbuanya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-enforcement-and-validation/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync modified source files to installed copies</name>
  <files>.claude/get-motif/agents/motif-design-reviewer.md, .claude/get-motif/hooks/motif-aria-check.js, .claude/get-motif/workflows/review.md</files>
  <action>
Copy the 3 modified source files to their installed locations using direct `cp` (following the Phase 11 Plan 03 established pattern):

```bash
cp runtimes/claude-code/agents/motif-design-reviewer.md .claude/get-motif/agents/motif-design-reviewer.md
cp runtimes/claude-code/hooks/motif-aria-check.js .claude/get-motif/hooks/motif-aria-check.js
cp core/workflows/review.md .claude/get-motif/workflows/review.md
```

After copying, verify zero diff between each source and installed pair:

```bash
diff runtimes/claude-code/agents/motif-design-reviewer.md .claude/get-motif/agents/motif-design-reviewer.md
diff runtimes/claude-code/hooks/motif-aria-check.js .claude/get-motif/hooks/motif-aria-check.js
diff core/workflows/review.md .claude/get-motif/workflows/review.md
```

All three diffs must produce zero output (files are identical).
  </action>
  <verify>
1. `diff runtimes/claude-code/agents/motif-design-reviewer.md .claude/get-motif/agents/motif-design-reviewer.md` produces no output
2. `diff runtimes/claude-code/hooks/motif-aria-check.js .claude/get-motif/hooks/motif-aria-check.js` produces no output
3. `diff core/workflows/review.md .claude/get-motif/workflows/review.md` produces no output
4. `grep -c "Icon Catalog Compliance" .claude/get-motif/agents/motif-design-reviewer.md` returns at least 1
5. `grep -c "findIconElements" .claude/get-motif/hooks/motif-aria-check.js` returns at least 1
6. `grep -c "ICON-CATALOG.md" .claude/get-motif/workflows/review.md` returns at least 2
  </verify>
  <done>
All 3 installed copies match their source copies with zero diff. Icon enforcement checks are present in the installed reviewer, hook, and workflow files.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end pipeline verification</name>
  <files></files>
  <action>
Present the icon enforcement pipeline to the user for verification.

What was built: Complete icon enforcement pipeline across 3 files:
1. The reviewer agent now checks icon names against ICON-CATALOG.md (Lens 3) and flags cross-vertical icons (Lens 4)
2. The aria-check hook now detects icon elements for all 4 libraries and enforces aria-label on icon-only buttons/links and aria-hidden on decorative icons
3. The review workflow now passes ICON-CATALOG.md as context to the reviewer agent
4. All installed copies are synced and operational

How to verify:

1. Verify aria-check hook syntax is valid:
   `node -c .claude/get-motif/hooks/motif-aria-check.js && echo "PASS: valid syntax"`

2. Verify icon-only button detection works:
   `echo '{"tool_name":"Write","tool_input":{"file_path":"test.html","content":"<button><i class=\"ph ph-gear\"></i></button>"}}' | node .claude/get-motif/hooks/motif-aria-check.js`
   Expected: JSON output with `icon-button-missing-label` violation

3. Verify text+icon button does NOT trigger false positive:
   `echo '{"tool_name":"Write","tool_input":{"file_path":"test.html","content":"<button><i class=\"ph ph-gear\"></i> Settings</button>"}}' | node .claude/get-motif/hooks/motif-aria-check.js`
   Expected: No output (clean exit) -- the "Settings" text provides the accessible name

4. Verify Material Symbols (span-based) detection:
   `echo '{"tool_name":"Write","tool_input":{"file_path":"test.html","content":"<button><span class=\"material-symbols-outlined\">settings</span></button>"}}' | node .claude/get-motif/hooks/motif-aria-check.js`
   Expected: JSON output with `icon-button-missing-label` violation

5. Verify reviewer has icon checks:
   `grep "Icon Catalog Compliance" .claude/get-motif/agents/motif-design-reviewer.md && echo "PASS"`
   `grep "Icon Vertical Appropriateness" .claude/get-motif/agents/motif-design-reviewer.md && echo "PASS"`

6. Verify review workflow has ICON-CATALOG.md:
   `grep "ICON-CATALOG.md" .claude/get-motif/workflows/review.md && echo "PASS"`

7. Optional full pipeline test: Run /motif:system then /motif:compose then /motif:review on any vertical.

Resume signal: Type "approved" if all checks pass, or describe any issues found.
  </action>
  <verify>User confirms all automated verification commands pass and the icon enforcement pipeline is operational.</verify>
  <done>End-to-end icon enforcement pipeline verified: reviewer checks icon consistency and vertical appropriateness, aria-check hook detects icon elements across all 4 libraries, installed copies are synced.</done>
</task>

</tasks>

<verification>
1. All 3 installed copies match source with zero diff
2. aria-check hook passes syntax check
3. aria-check hook correctly detects icon-only buttons across all 4 library syntaxes
4. aria-check hook does NOT false-positive on text+icon buttons
5. Reviewer agent has both icon consistency (Lens 3) and vertical appropriateness (Lens 4) checks
6. Review workflow includes ICON-CATALOG.md in context
</verification>

<success_criteria>
- Phase 12 Success Criterion 1: Reviewer checks icon names against ICON-CATALOG.md -- VERIFIED via installed reviewer containing "Icon Catalog Compliance"
- Phase 12 Success Criterion 2: Reviewer checks icon vertical appropriateness -- VERIFIED via installed reviewer containing "Icon Vertical Appropriateness"
- Phase 12 Success Criterion 3: aria-check hook flags icon-only buttons missing aria-label and decorative icons missing aria-hidden -- VERIFIED via hook test commands
- Phase 12 Success Criterion 4: End-to-end pipeline produces screens with icon enforcement -- VERIFIED via checkpoint test
</success_criteria>

<output>
After completion, create `.planning/phases/12-enforcement-and-validation/12-02-SUMMARY.md`
</output>

---
phase: 31-auto-review-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/get-motif/workflows/compose-screen.md
autonomous: true

must_haves:
  truths:
    - "After batch compose completes, reviewer Task() agents are spawned automatically for all PASSED/WARNED screens"
    - "Review results are collected by grepping score and critical issues from REVIEW.md files — not reading full files"
    - "Review files are committed by the orchestrator, not individual reviewer agents"
    - "STATE.md is updated with reviewed status for each reviewed screen"
    - "A review summary table is printed showing per-screen score, critical count, and pass/fail status"
  artifacts:
    - path: ".claude/get-motif/workflows/compose-screen.md"
      provides: "Steps 3b.8c (auto-review dispatch) and 3b.8d (review result collection)"
      contains: "### 3b.8c"
  key_links:
    - from: "compose-screen.md step 3b.8c"
      to: "review.md agent_spawn template"
      via: "Task() spawn with BATCH AUTO-REVIEW INSTRUCTIONS prepended"
      pattern: "BATCH AUTO-REVIEW INSTRUCTIONS"
    - from: "compose-screen.md step 3b.8d"
      to: ".planning/design/reviews/*-REVIEW.md"
      via: "Grep for score and critical issues"
      pattern: "Score.*\\d+.*100"
---

<objective>
Add auto-review dispatch and result collection steps to the batch compose workflow in compose-screen.md.

Purpose: After batch compose completes, the orchestrator automatically spawns reviewer agents for all successfully composed screens, collects their results, commits review files, updates state, and prints a review summary — eliminating the need for the user to manually run `/motif:review all`.

Output: Steps 3b.8c and 3b.8d added to compose-screen.md between existing 3b.8b (batch manifest) and 3b.9 (auto-run offer).
</objective>

<execution_context>
@/Users/austineogbuanya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austineogbuanya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-auto-review-integration/31-RESEARCH.md
@.claude/get-motif/workflows/compose-screen.md
@.claude/get-motif/workflows/review.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Steps 3b.8c and 3b.8d to compose-screen.md</name>
  <files>.claude/get-motif/workflows/compose-screen.md</files>
  <action>
Read compose-screen.md fully. Insert two new steps between the existing "### 3b.8b: Write batch manifest" block (which ends with the manifest commit) and the existing "### 3b.9: Offer auto-run ONCE" block.

**Step 3b.8c: Auto-review dispatch**

Add the following step with this exact logic:

1. Collect REVIEWABLE_SCREENS — filter ALL_RESULTS for screens with status PASSED or WARNED.
2. If REVIEWABLE_SCREENS is empty, print "No screens passed composition — skipping auto-review." and skip to 3b.8e.
3. Announce: "Auto-reviewing {N} composed screens..."
4. Calculate REVIEW_WAVES = ceil(REVIEWABLE_SCREENS.length / CONCURRENCY), split into chunks.
5. For each review wave, spawn reviewer Task() agents using the SAME agent_spawn template from review.md Step 2 (the full block from `<agent_spawn id="review-{SCREEN_NAME}">` through `</agent_spawn>`), but with BATCH AUTO-REVIEW INSTRUCTIONS prepended to the Task prompt:

```
## BATCH AUTO-REVIEW INSTRUCTIONS
You are reviewing in auto-review mode (triggered automatically after batch compose).

CRITICAL DIFFERENCES from standalone /motif:review:
1. After writing {SCREEN_NAME}-REVIEW.md, do NOT run `git commit`. Leave the file on disk. The orchestrator commits.
2. Do NOT modify STATE.md. The orchestrator handles state updates.
3. Write REVIEW.md to `.planning/design/reviews/{SCREEN_NAME}-REVIEW.md` as normal.
```

Spawn ALL agents for each review wave in a single message with multiple Task() calls (same pattern as 3b.2).

6. Wait for all review agents in the wave to complete before proceeding to the next wave.

**Step 3b.8d: Review result collection**

Add the following step with this exact logic:

1. Initialize REVIEW_RESULTS = [].
2. For each screen in REVIEWABLE_SCREENS:
   a. Check if `.planning/design/reviews/{SCREEN_NAME}-REVIEW.md` exists.
   b. If exists: Use Grep to find the `## Score:` line and extract the numeric score. Use Grep to check the `## Critical Issues` section — count items that are NOT "None" or empty. Do NOT read the full REVIEW.md file (context bloat pitfall from research).
   c. Classify: REVIEW_PASSED if score >= 80 AND zero critical issues. REVIEW_FAILED otherwise.
   d. If REVIEW.md does not exist: status = REVIEW_SKIPPED, score = 0, critical = "unknown".
   e. Track: {name, score, critical_count, review_status}.
3. Commit all review files:
   ```bash
   git add .planning/design/reviews/*-REVIEW.md
   git commit -m "design(review): auto-review batch -- {N} screens reviewed"
   ```
4. Update STATE.md via batch-update-screens: set each reviewed screen to status `reviewed`.
   ```json
   {
     "updates": [{"name": "{screen}", "status": "reviewed"}, ...],
     "phase": "REVIEWING"
   }
   ```
   Run: `node .claude/get-motif/scripts/motif-state.js batch-update-screens '{payload}'`
5. Append a `## Review Results` section to BATCH-RESULT.md (read the file, append to the end):
   ```markdown
   ## Review Results

   **Reviewed:** {N} screens | **Passed:** {pass_count} | **Failed:** {fail_count}

   | Screen | Score | Critical | Status |
   |--------|-------|----------|--------|
   | {name} | {score}/100 | {critical_count} | PASS/FAIL |
   ```
   Then commit the updated manifest:
   ```bash
   git add .planning/design/BATCH-RESULT.md
   git commit -m "design(review): update batch manifest with review results"
   ```
6. Print review summary table to output (same format as the BATCH-RESULT.md table above).
7. Store REVIEW_ALL_PASSED = true if ALL screens have review_status REVIEW_PASSED.

Important implementation notes:
- The reviewer agent_spawn template should be referenced from review.md Step 2, not reinvented. Include the full template inline (context files, four lenses, output format, anti-slop checks).
- Use "review.md Step 2" as a reference comment so future readers know the source.
- Keep the same CONCURRENCY variable for review waves as compose waves (research recommendation).
  </action>
  <verify>
After editing, verify:
1. `grep -n "### 3b.8c" .claude/get-motif/workflows/compose-screen.md` returns a match
2. `grep -n "### 3b.8d" .claude/get-motif/workflows/compose-screen.md` returns a match
3. `grep -n "BATCH AUTO-REVIEW INSTRUCTIONS" .claude/get-motif/workflows/compose-screen.md` returns a match
4. `grep -n "batch-update-screens" .claude/get-motif/workflows/compose-screen.md` returns at least 2 matches (the existing 3b.6 call and the new 3b.8d call)
5. `grep -n "## Review Results" .claude/get-motif/workflows/compose-screen.md` returns a match (BATCH-RESULT.md update)
6. Step 3b.8c appears AFTER 3b.8b and BEFORE 3b.8d
7. Step 3b.8d appears AFTER 3b.8c and BEFORE 3b.8e or 3b.9
  </verify>
  <done>
compose-screen.md contains Steps 3b.8c (auto-review dispatch with reviewer Task() agents using review.md template plus batch instructions) and 3b.8d (result collection via Grep, commit, state update, BATCH-RESULT.md update, summary table). The step ordering is 3b.8b -> 3b.8c -> 3b.8d -> 3b.9.
  </done>
</task>

</tasks>

<verification>
- compose-screen.md contains steps 3b.8c and 3b.8d in correct order
- Reviewer agent spawn uses review.md Step 2 template with BATCH AUTO-REVIEW INSTRUCTIONS
- Result collection uses Grep (not full file reads) for score and critical issues
- Orchestrator commits review files (not individual agents)
- STATE.md updated via batch-update-screens with status "reviewed"
- BATCH-RESULT.md updated with review results section
</verification>

<success_criteria>
Steps 3b.8c and 3b.8d exist in compose-screen.md between 3b.8b and 3b.9, implementing auto-review dispatch and result collection with all the patterns specified in the research (batch instructions, Grep-based result extraction, orchestrator-owned commits, state update, manifest update).
</success_criteria>

<output>
After completion, create `.planning/phases/31-auto-review-integration/31-01-SUMMARY.md`
</output>

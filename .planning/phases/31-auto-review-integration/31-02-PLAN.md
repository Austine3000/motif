---
phase: 31-auto-review-integration
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - .claude/get-motif/workflows/compose-screen.md
autonomous: true

must_haves:
  truths:
    - "Auto-run is only offered if all reviews pass (score >= 80, zero critical) or user explicitly overrides"
    - "Failed review prints failing screens with scores and critical counts, suggests /motif:fix, and offers override"
    - "User override allows proceeding to auto-run even after review failure"
    - "User decline skips auto-run and goes to next-step guidance"
    - "Next-step guidance (3b.10) reflects review status — no longer suggests /motif:review all when review already happened"
  artifacts:
    - path: ".claude/get-motif/workflows/compose-screen.md"
      provides: "Step 3b.8e (review gate) and updated Steps 3b.9 and 3b.10"
      contains: "### 3b.8e"
  key_links:
    - from: "compose-screen.md step 3b.8e"
      to: "compose-screen.md step 3b.9"
      via: "REVIEW_ALL_PASSED boolean gates auto-run offer"
      pattern: "REVIEW_ALL_PASSED"
    - from: "compose-screen.md step 3b.8e"
      to: "compose-screen.md step 3b.10"
      via: "Skip path when user declines override"
      pattern: "skip.*3b\\.9.*3b\\.10"
    - from: "compose-screen.md step 3b.10"
      to: "/motif:fix suggestion"
      via: "Review-aware next step guidance"
      pattern: "motif:fix"
---

<objective>
Add the review gate step (3b.8e) and update the auto-run offer (3b.9) and next-step guidance (3b.10) to be review-aware.

Purpose: Auto-run is gated on review pass — a failed review blocks auto-run with a clear explanation and override path. Next-step guidance no longer redundantly suggests `/motif:review all` since review already happened automatically.

Output: Step 3b.8e added, Steps 3b.9 and 3b.10 updated in compose-screen.md.
</objective>

<execution_context>
@/Users/austineogbuanya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austineogbuanya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-auto-review-integration/31-RESEARCH.md
@.planning/phases/31-auto-review-integration/31-01-SUMMARY.md
@.claude/get-motif/workflows/compose-screen.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Step 3b.8e and update Steps 3b.9 and 3b.10</name>
  <files>.claude/get-motif/workflows/compose-screen.md</files>
  <action>
Read compose-screen.md fully. Insert Step 3b.8e between the new 3b.8d (from plan 01) and the existing 3b.9. Then update 3b.9 and 3b.10.

**Step 3b.8e: Review gate (auto-run decision)**

Add the following step:

1. Read REVIEW_ALL_PASSED (set by 3b.8d — true if all reviewed screens passed).

2. **If REVIEW_ALL_PASSED is true:**
   Print: "All screens passed review. Proceeding to auto-run."
   Continue to Step 3b.9.

3. **If REVIEW_ALL_PASSED is false:**
   List failing screens with their scores and critical issue counts from REVIEW_RESULTS.
   Print:
   ```
   Review found issues that should be fixed before running:
     - {screen_name}: {score}/100 ({critical_count} critical issues)
     - ...

   Recommended: Run `/motif:fix {space-separated failing screen names}` to address critical issues.

   Override: Would you like to launch the preview anyway? (yes/no)
   ```
   If user says yes: Continue to Step 3b.9.
   If user says no: Print "Skipping auto-run. Fix issues and re-compose, or run `/motif:review` after fixing." Skip 3b.9, go directly to 3b.10.

4. **If no screens were reviewed** (REVIEWABLE_SCREENS was empty in 3b.8c — all screens failed composition):
   Skip review gate entirely. 3b.9 proceeds as before (unchanged behavior for all-failed batches).

**Update Step 3b.9: Offer auto-run ONCE**

The existing 3b.9 content stays mostly the same (auto-run eligibility checks, launcher invocation). Add one line at the top:

"This step is reached only if the review gate (3b.8e) passed or the user overrode a failed review."

No other changes to 3b.9 logic — it already works correctly.

**Update Step 3b.10: Next step (review-aware)**

Replace the existing 3b.10 content with review-aware guidance:

```markdown
### 3b.10: Next step

Check STATE.md for remaining `planned` screens AND review status:

**If all screens are composed AND review passed (REVIEW_ALL_PASSED is true):**
  "All screens composed and reviewed. Your design is production-ready."
  If auto-run was launched: "Preview is running — check the browser."
  If auto-run was not launched or skipped: "Run the preview manually or proceed to development."

**If all screens are composed but review found issues (REVIEW_ALL_PASSED is false):**
  "All screens composed. Review found issues on {N} screens."
  List failing screens.
  "Run `/motif:fix {failing_screen_names}` to address critical issues, then `/motif:compose {failing_screen_names}` to re-compose."

**If some screens are `failed` (composition failures):**
  List them and suggest: "To retry failed screens, run `/motif:compose {failed_name_1} {failed_name_2}`"

**If some screens are still `planned` (were not in this batch):**
  List them and suggest composing them next.

If context > 50%, suggest `/clear` first.
```

This replaces the old guidance that said "Run `/motif:review all` to evaluate" — that is now redundant since auto-review already happened.
  </action>
  <verify>
After editing, verify:
1. `grep -n "### 3b.8e" .claude/get-motif/workflows/compose-screen.md` returns a match
2. `grep -n "REVIEW_ALL_PASSED" .claude/get-motif/workflows/compose-screen.md` returns multiple matches (set in 3b.8d, used in 3b.8e and 3b.10)
3. `grep -n "motif:fix" .claude/get-motif/workflows/compose-screen.md` returns a match in the 3b.8e and 3b.10 sections
4. `grep -n "review gate" .claude/get-motif/workflows/compose-screen.md` returns a match in 3b.9
5. The old 3b.10 text "Run \`/motif:review all\` to evaluate" is NO LONGER present in the 3b.10 section (may still exist in Step 6 for single-screen mode — that is correct)
6. Step ordering: 3b.8d -> 3b.8e -> 3b.9 -> 3b.10
  </verify>
  <done>
compose-screen.md contains Step 3b.8e (review gate with pass/fail/override logic), updated Step 3b.9 (notes review gate prerequisite), and updated Step 3b.10 (review-aware next-step guidance that no longer suggests manual /motif:review). Auto-run is gated on review pass or user override. Failed review suggests /motif:fix.
  </done>
</task>

</tasks>

<verification>
- Step 3b.8e exists between 3b.8d and 3b.9 with pass/fail/override logic
- 3b.9 notes it is only reached after review gate passes or override
- 3b.10 is review-aware and does not suggest /motif:review all
- Override path allows user to proceed to auto-run even after review failure
- Decline path skips auto-run and goes to 3b.10
- REVIEW_ALL_PASSED variable flows from 3b.8d through 3b.8e to 3b.10
</verification>

<success_criteria>
The complete batch flow is: compose -> summary -> manifest -> auto-review -> review collection -> review gate -> auto-run (if passed/overridden) -> next steps. Failed review blocks auto-run with override path. Next steps reflect review status.
</success_criteria>

<output>
After completion, create `.planning/phases/31-auto-review-integration/31-02-SUMMARY.md`
</output>

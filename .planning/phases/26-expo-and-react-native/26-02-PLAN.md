---
phase: 26-expo-and-react-native
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - .claude/get-motif/references/composer-rn.md
  - .claude/get-motif/references/css-to-rn.md
  - .claude/get-motif/references/framework-registry.json
autonomous: true
requirements:
  - COMP-03

must_haves:
  truths:
    - "React Native composition outputs View/Text/ScrollView/StyleSheet.create with no CSS, className, or HTML tags."
    - "RN composition uses tokens.native.ts for all visual values and flags unsupported CSS with inline TODOs."
  artifacts:
    - path: ".claude/get-motif/references/composer-rn.md"
      provides: "React Native composition overlay with file placement, token usage, and RN-specific constraints."
      contains: "React Native"
    - path: ".claude/get-motif/references/css-to-rn.md"
      provides: "CSS-to-RN property compatibility matrix for composer guidance."
      contains: "CSS-to-RN"
    - path: ".claude/get-motif/references/framework-registry.json"
      provides: "mobile-expo composition metadata (icon delivery, component format)."
      contains: "mobile-expo"
  key_links:
    - from: ".claude/get-motif/workflows/compose-screen.md"
      to: ".claude/get-motif/references/composer-rn.md"
      via: "Compose overlay resolution loads the RN overlay for mobile-expo."
      pattern: "composer-rn\\.md"
---

<objective>
Provide the React Native composition overlay and property-compatibility guidance so `/motif:compose` can output usable RN components with correct token usage.

Purpose: COMP-03 requires RN-native output (no HTML/CSS) and explicit handling of unsupported CSS.
Output: composer-rn.md overlay, css-to-rn.md matrix, and registry metadata alignment for RN composition.
</objective>

<execution_context>
@/Users/austineogbuanya/.codex/get-shit-done/workflows/execute-plan.md
@/Users/austineogbuanya/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-expo-and-react-native/26-RESEARCH.md
@.planning/phases/26-expo-and-react-native/26-VALIDATION.md
@.claude/get-motif/references/framework-registry.json
@.claude/get-motif/workflows/compose-screen.md
@.claude/get-motif/references/composer-nextjs.md
@.claude/get-motif/references/composer-vite.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" id="26-02-01" wave="2">
  <name>Task 1: Create the React Native composer overlay</name>
  <files>.claude/get-motif/references/composer-rn.md</files>
  <action>
Create `.claude/get-motif/references/composer-rn.md` with RN-specific composition rules:

- File output rules (screens vs components) for greenfield Expo projects.
- Import rules for RN primitives and tokens.native.ts.
- Style rules enforcing StyleSheet.create and token-only values.
- Element mapping table (div → View, p → Text, img → Image, button → Pressable, etc.).
- Anti-slop checklist (no HTML tags, no className, no CSS vars, no inline style objects, no rem/em units).
- Explicit instruction: if a design calls for unsupported CSS (grid, box-shadow, pseudo-elements, position:fixed), add inline `// TODO` comments instead of silently dropping.

Use the Next.js and Vite overlays as formatting reference, but tailor content to RN constraints.
  </action>
  <verify>
    <automated>
      <check command="rg -n \"React Native\|StyleSheet.create\|Pressable\|tokens.native\" .claude/get-motif/references/composer-rn.md">Confirms RN overlay includes core primitives, StyleSheet usage, and token import rules.</check>
    </automated>
  </verify>
  <done>`composer-rn.md` exists with RN-specific composition rules, file placement, and anti-slop constraints.</done>
</task>

<task type="auto" id="26-02-02" wave="2" depends_on="26-02-01">
  <name>Task 2: Add CSS-to-RN property matrix and align registry composition metadata</name>
  <files>.claude/get-motif/references/css-to-rn.md, .claude/get-motif/references/framework-registry.json, .claude/get-motif/references/composer-rn.md</files>
  <action>
Create `.claude/get-motif/references/css-to-rn.md` as a concise compatibility matrix covering:
- Layout (flex, position, gap, grid)
- Spacing (padding/margin)
- Typography (font size/weight/line-height)
- Borders/radius
- Shadows (iOS shadow props vs Android elevation)
- Unsupported features (pseudo-elements, position:fixed, CSS variables)

Update `composer-rn.md` to reference this matrix (or include the most critical rows inline) so the composer can flag unsupported properties with TODOs.

Update `framework-registry.json` `mobile-expo.composition` metadata if needed (for example icon delivery strategy like `lucide-react-native`). Keep it consistent with the RN overlay.
  </action>
  <verify>
    <automated>
      <check command="rg -n \"CSS-to-RN\|Unsupported\|grid\|position:fixed\" .claude/get-motif/references/css-to-rn.md">Confirms the matrix documents unsupported CSS and mapping guidance.</check>
      <check command="rg -n \"css-to-rn\|property matrix\|TODO\" .claude/get-motif/references/composer-rn.md">Confirms the RN overlay references the matrix or its key rules.</check>
    </automated>
  </verify>
  <done>RN overlay and registry metadata are aligned, and a CSS-to-RN matrix exists to prevent silent drops.</done>
</task>

</tasks>

<verification>
1. `composer-rn.md` exists and forbids HTML/CSS, requires RN primitives + StyleSheet + tokens.native.
2. CSS-to-RN property matrix exists and is referenced by the RN overlay.
3. Manual check: a composed RN screen uses View/Text/ScrollView, imports tokens.native, and includes TODOs for unsupported CSS.
</verification>

<success_criteria>
- COMP-03 is satisfiable: `/motif:compose` on a mobile-expo project can output React Native component files using RN primitives and StyleSheet.create.
- Unsupported CSS is explicitly flagged with inline TODOs instead of being dropped.
</success_criteria>

<output>
After completion, create `.planning/phases/26-expo-and-react-native/26-02-SUMMARY.md`
</output>

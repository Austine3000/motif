---
phase: 26-expo-and-react-native
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/get-motif/references/framework-registry.json
  - .claude/get-motif/workflows/generate-system.md
autonomous: true
requirements:
  - SCAF-04

must_haves:
  truths:
    - "Expo scaffolding is executed via create-expo-app with non-interactive flags and produces a runnable project."
    - "Mobile-expo projects receive tokens.native.ts inside the scaffolded project so RN composition can import it locally."
    - "Scaffolded Expo projects include a runnable entry (App.tsx) and can be started with `npx expo start --web`."
  artifacts:
    - path: ".claude/get-motif/references/framework-registry.json"
      provides: "Mobile-expo scaffold, runtime metadata, and materialization targets for tokens.native.ts."
      contains: "mobile-expo"
    - path: ".claude/get-motif/workflows/generate-system.md"
      provides: "Scaffold/materialization verification includes mobile-expo token placement expectations."
      contains: "mobile-expo"
  key_links:
    - from: ".claude/get-motif/workflows/generate-system.md"
      to: ".claude/get-motif/scripts/scaffold-project.js"
      via: "Scaffold/materialization is performed by the shared runner after token generation."
      pattern: "scaffold-project\\.js"
    - from: ".claude/get-motif/scripts/scaffold-project.js"
      to: ".claude/get-motif/references/framework-registry.json"
      via: "Scaffold runner reads mobile-expo contract for commands and materialization."
      pattern: "framework-registry"
---

<objective>
Deliver Expo scaffolding for greenfield mobile projects and ensure tokens.native.ts is materialized into the scaffolded project.

Purpose: SCAF-04 requires a first-class Expo scaffold that is runnable immediately and ready for RN composition.
Output: Updated registry contract, materialization path for tokens.native.ts, and generate-system verification notes.
</objective>

<execution_context>
@/Users/austineogbuanya/.codex/get-shit-done/workflows/execute-plan.md
@/Users/austineogbuanya/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-expo-and-react-native/26-RESEARCH.md
@.planning/phases/26-expo-and-react-native/26-VALIDATION.md
@.claude/get-motif/references/framework-registry.json
@.claude/get-motif/workflows/generate-system.md
@.claude/get-motif/scripts/scaffold-project.js
@CLAUDE.md
</context>

<tasks>

<task type="auto" id="26-01-01" wave="1">
  <name>Task 1: Extend the mobile-expo registry entry for scaffolding + token materialization</name>
  <files>.claude/get-motif/references/framework-registry.json</files>
  <action>
Update the `mobile-expo` entry to be on par with other platforms:

- Add a `scaffoldSequence` describing the intended steps.
- Align `devServer` to the runtime-launcher contract (use `mode`, `command`, `args`, `cwdPolicy`, `readyMatchers`, `urlPattern`, `defaultUrl`, `openStrategy`, `defaultPort`, `portFlag`).
- Add materialization targets so `tokens.native.ts` is copied into the scaffolded project (for example `theme/tokens.native.ts`), plus any minimal supporting templates (like a `theme/index.ts` re-export) and directory placeholders (`components/.gitkeep`, `screens/.gitkeep`) to make composition destinations explicit.
- Keep the entry declarative and consistent with the style used for `web-nextjs` and `web-vite`.

Do NOT change the scaffold command itself unless it is required for non-interactive usage. Preserve `create-expo-app@latest` with the current template unless a better option is explicitly justified by the roadmap.
  </action>
  <verify>
    <automated>
      <check command="node -e &quot;const r=require('./.claude/get-motif/references/framework-registry.json');const e=r['mobile-expo'];if(!e) throw new Error('missing mobile-expo');if(!e.scaffold || !e.scaffold.args) throw new Error('missing scaffold args');if(!e.devServer || !e.devServer.readyMatchers) throw new Error('missing devServer readyMatchers');if(!e.materialization || !e.materialization.designArtifacts) throw new Error('missing materialization');console.log('phase26 mobile-expo registry OK');&quot;">Confirms mobile-expo has scaffold args, runtime metadata, and token materialization declared.</check>
    </automated>
  </verify>
  <done>`mobile-expo` in the framework registry declares scaffold sequence, runtime metadata, and token materialization destinations needed for Expo scaffolding.</done>
</task>

<task type="auto" id="26-01-02" wave="1" depends_on="26-01-01">
  <name>Task 2: Document mobile-expo scaffold/materialization verification in generate-system</name>
  <files>.claude/get-motif/workflows/generate-system.md</files>
  <action>
Update the scaffold/materialization verification section to include mobile-expo expectations:
- After scaffold/materialization, confirm the tokens native artifact exists in the project (at the registry-defined destination).
- Mention that Expo scaffolding uses the shared scaffold runner and should be treated as greenfield unless a brownfield scan exists.

Keep the workflow structure unchanged; this is a documentation/verification addition only.
  </action>
  <verify>
    <automated>
      <check command="rg -n \"mobile-expo\" .claude/get-motif/workflows/generate-system.md">Confirms mobile-expo verification guidance exists in generate-system.</check>
    </automated>
  </verify>
  <done>`generate-system.md` explicitly calls out mobile-expo materialization verification for tokens.native.ts.</done>
</task>

</tasks>

<verification>
1. `framework-registry.json` exposes a complete mobile-expo scaffold + runtime + materialization contract.
2. `generate-system.md` documents how to verify Expo token placement after scaffolding.
3. Manual check: run the scaffold runner in a temp directory and confirm `package.json`, `App.tsx`, and the tokens.native destination exist.
</verification>

<success_criteria>
- SCAF-04 is satisfiable end-to-end: Expo scaffolding runs with non-interactive flags and produces a runnable project.
- The scaffolded Expo project receives a local `tokens.native.ts` that RN composition can import directly.
</success_criteria>

<output>
After completion, create `.planning/phases/26-expo-and-react-native/26-01-SUMMARY.md`
</output>

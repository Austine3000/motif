---
phase: 08-ci-and-publish
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified: []
autonomous: false
user_setup:
  - service: github
    why: "Repository hosting and CI/CD"
    env_vars: []
    dashboard_config:
      - task: "Verify GitHub repository exists and is public"
        location: "github.com/SailsLab/motif (or chosen org/repo)"
  - service: npm
    why: "Package publishing"
    env_vars: []
    dashboard_config:
      - task: "Configure trusted publisher for the package after first publish"
        location: "npmjs.com -> package settings -> Trusted Publishers -> GitHub Actions"

must_haves:
  truths:
    - "GitHub repository exists with remote origin configured"
    - "Code is pushed to GitHub (main branch + all history)"
    - "npm package is published and installable from the registry"
    - "npx <package>@latest installs Motif and completes the installation flow"
    - "Trusted publishing is configured for future automated releases"
  artifacts: []
  key_links:
    - from: "local git repo"
      to: "github.com remote"
      via: "git remote origin"
      pattern: "git remote -v"
    - from: ".github/workflows/publish.yml"
      to: "npmjs.com"
      via: "OIDC trusted publishing on tag push"
      pattern: "npm publish --provenance"
---

<objective>
GitHub repository setup, first npm publish, and end-to-end installation verification.

Purpose: With all files prepared in Plan 01, this plan connects the local repository to GitHub, performs the first npm publish, configures trusted publishing for future releases, and verifies that the published package installs correctly.

Output: Live GitHub repository, published npm package, configured trusted publishing, verified installation
</objective>

<execution_context>
@/Users/austineogbuanya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austineogbuanya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ci-and-publish/08-RESEARCH.md
@.planning/phases/08-ci-and-publish/08-01-SUMMARY.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub repository and push code</name>
  <files>package.json</files>
  <action>
1. **Remove tracked .DS_Store files** before pushing:
   - Run `git rm --cached .DS_Store core/.DS_Store runtimes/.DS_Store runtimes/claude-code/.DS_Store 2>/dev/null` to untrack any .DS_Store files
   - Commit: "chore: remove tracked .DS_Store files"

2. **Create GitHub repository** using the `gh` CLI:
   - Run `gh repo create SailsLab/motif --public --source=. --remote=origin --push`
   - If "SailsLab" org doesn't exist or user doesn't have access, this will fail -- handle gracefully by trying the user's personal account: `gh repo create motif --public --source=. --remote=origin --push`
   - If `gh` is not authenticated, this becomes an auth gate checkpoint (pause for user to run `gh auth login`)

3. **Verify remote is configured:**
   - Run `git remote -v` -- should show origin pointing to the GitHub repo
   - Run `git log --oneline -5` on the remote to confirm history was pushed

4. **Verify workflow file is on GitHub:**
   - Run `gh api repos/{owner}/{repo}/contents/.github/workflows/publish.yml --jq .name` to confirm the workflow file exists on remote

Note: The repository URL in package.json must match the actual GitHub URL. If the org/repo name differs from what was set in Plan 01, update package.json repository.url to match, commit, and push.
  </action>
  <verify>
- `git remote -v` shows origin pointing to a GitHub URL
- `gh repo view --json url --jq .url` returns the repository URL
- `gh api repos/{owner}/{repo}/contents/.github/workflows/publish.yml --jq .name` returns "publish.yml"
  </verify>
  <done>GitHub repository exists and is public, all code including .github/workflows/publish.yml is pushed, remote origin is configured.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: First npm publish and trusted publisher configuration</name>
  <files></files>
  <action>
The first publish requires human interaction because npm OIDC trusted publishing may need the package to exist before configuration, and the npm account/org must be set up if not already.

**Option A: First publish with granular token (recommended for first time)**

1. Log in to npmjs.com
2. If using a scoped name like `@sailslab/motif`:
   - Ensure the "sailslab" npm org exists (create at npmjs.com/org/create if not)
3. Create a granular access token:
   - npmjs.com -> Access Tokens -> Generate New Token -> Granular Access Token
   - Permissions: Read and Write
   - Select packages: Only select packages -> the package name
   - Expiration: 7 days (only needed for first publish)
4. From the project root, run:
   ```bash
   npm config set //registry.npmjs.org/:_authToken=YOUR_TOKEN
   npm publish --access public
   npm config delete //registry.npmjs.org/:_authToken
   ```
5. Verify the package exists: `npm view <package-name>`

**Option B: Try OIDC first (if npm supports pre-publish trusted publisher config)**

1. On npmjs.com, navigate to your profile -> Packages -> look for option to pre-register a package
2. If available, configure trusted publisher:
   - Organization/user: SailsLab (or your GitHub username)
   - Repository: motif
   - Workflow filename: publish.yml
3. Then tag and push to trigger the workflow:
   ```bash
   git tag v0.1.0
   git push origin v0.1.0
   ```
4. Check workflow status: `gh run list --workflow=publish.yml`

**After first publish (either option), configure trusted publishing:**

1. Go to npmjs.com -> package page -> Settings (or Access)
2. Under "Trusted Publishers", click "Add trusted publisher"
3. Select "GitHub Actions"
4. Fill in (CASE SENSITIVE):
   - Organization or user: `SailsLab` (or your GitHub username, must match exactly)
   - Repository: `motif` (must match exactly)
   - Workflow filename: `publish.yml` (must match exactly, include .yml extension)
5. Save

**Verify trusted publishing works by doing a patch release:**
```bash
npm version patch
git push origin main --follow-tags
```
Then check: `gh run list --workflow=publish.yml` and `npm view <package-name> versions`
  </action>
  <verify>
- `npm view <package-name>` returns package metadata (name, version, description)
- Trusted publisher is configured on npmjs.com for the package
  </verify>
  <done>npm package is published and live on the registry. Trusted publishing is configured for future automated releases via GitHub Actions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end installation verification</name>
  <files></files>
  <action>
Verify the complete CI/CD pipeline and end-to-end installation from the published npm package.
  </action>
  <verify>
1. **Verify the package is on npm:**
   ```bash
   npm view <package-name>
   ```
   Expected: Shows package metadata (name, version, description)

2. **Verify end-to-end installation in a fresh directory:**
   ```bash
   mkdir /tmp/motif-verify && cd /tmp/motif-verify
   git init && mkdir -p .claude
   npx <package-name>@latest
   ```
   Expected: Motif installer runs, detects Claude Code runtime, installs files to .claude/get-motif/ and .claude/commands/motif/

3. **Verify installed files exist:**
   ```bash
   ls .claude/get-motif/workflows/
   ls .claude/commands/motif/
   cat CLAUDE.md | grep "MOTIF-START"
   ```
   Expected: Workflows directory has .md files, commands directory has .md files, CLAUDE.md contains Motif snippet

4. **Verify CI workflow exists on GitHub:**
   ```bash
   gh run list --workflow=publish.yml
   ```
   Expected: At least one workflow run visible (from the publish)

5. **Clean up:**
   ```bash
   rm -rf /tmp/motif-verify
   ```
  </verify>
  <done>
  - npm package is published and `npm view` returns metadata
  - `npx <package>@latest` installs successfully in a fresh project with Claude Code directory
  - Installed files include workflows, commands, and CLAUDE.md Motif snippet
  - GitHub Actions workflow is visible and has run successfully
  - Phase 8 success criteria are fully met
  </done>
</task>

</tasks>

<verification>
- GitHub repository is public and contains all project code
- .github/workflows/publish.yml is present on GitHub
- npm package is published and `npm view <package-name>` returns metadata
- `npx <package-name>@latest` installs successfully in a fresh project
- Trusted publishing is configured on npmjs.com for the package
- Future releases can be triggered by `npm version patch && git push origin main --follow-tags`
</verification>

<success_criteria>
The package is live on npm, installable via npx, and future releases are automated through tag-triggered GitHub Actions with OIDC trusted publishing. Phase 8 success criteria are met:
1. GitHub Actions workflow triggers on git tag and publishes automatically
2. npx <package>@latest installs and completes the installation flow
</success_criteria>

<output>
After completion, create `.planning/phases/08-ci-and-publish/08-02-SUMMARY.md`
</output>

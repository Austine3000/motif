---
phase: 16-validation-and-hardening
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - core/workflows/compose-screen.md
  - .claude/get-motif/workflows/compose-screen.md
autonomous: true

must_haves:
  truths:
    - "Compose subagent stages all files before committing, runs validation, and only commits if validation passes"
    - "Compose subagent rolls back staging (git reset HEAD) on validation failure without deleting files"
    - "Compose subagent reports validation status in screen SUMMARY.md"
    - "Orchestrator warns user when PROJECT-SCAN.md scan date is not today before spawning composer"
    - "Greenfield compositions skip staleness check (no PROJECT-SCAN.md) and validator runs with only cycle detection"
  artifacts:
    - path: "core/workflows/compose-screen.md"
      provides: "Canonical compose workflow with validation gate and staleness check"
      contains: "compose-validator.js"
    - path: ".claude/get-motif/workflows/compose-screen.md"
      provides: "Live copy of compose workflow (byte-identical to canonical)"
      contains: "compose-validator.js"
  key_links:
    - from: "core/workflows/compose-screen.md (Step F)"
      to: "scripts/compose-validator.js"
      via: "Bash(node:*) call in subagent"
      pattern: "node scripts/compose-validator\\.js"
    - from: "core/workflows/compose-screen.md (Step 2b)"
      to: ".planning/design/PROJECT-SCAN.md"
      via: "Read Scanned date"
      pattern: "Scanned.*date"
    - from: "core/workflows/compose-screen.md (Step E)"
      to: "SUMMARY.md"
      via: "Validation section in summary template"
      pattern: "## Validation"
---

<objective>
Modify the compose-screen.md workflow to add a validate-then-commit-or-rollback pattern in Step F, a stale scan artifact check in Step 2, and a validation section in the summary template.

Purpose: Wires the validator script (from 16-01) into the actual compose pipeline so every screen composition is validated before commit, with automatic rollback on failure and staleness warnings for outdated scan data.
Output: Updated compose-screen.md (canonical + live copy, kept byte-identical)
</objective>

<execution_context>
@/Users/austineogbuanya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austineogbuanya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-validation-and-hardening/16-RESEARCH.md
@.planning/phases/16-validation-and-hardening/16-01-SUMMARY.md
@core/workflows/compose-screen.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add staleness check and validation gate to compose workflow</name>
  <files>core/workflows/compose-screen.md</files>
  <action>
Modify `core/workflows/compose-screen.md` with three changes. Read the current file first.

**Change 1: Add Step 2b — Stale Scan Artifact Check (orchestrator level)**

After the existing Step 2 "Assemble Context Profile" and before Step 3 "Spawn Composer Agent", insert a new step:

```markdown
## Step 2b: Scan Freshness Check

If BROWNFIELD is true (PROJECT-SCAN.md exists):
1. Read `.planning/design/PROJECT-SCAN.md` and find the `**Scanned:** YYYY-MM-DD` line
2. Compare the scan date to today's date
3. If the scan date is NOT today:
   - Warn the user: "Scan data is from {scan_date}. Your project may have changed since then."
   - Ask: "Continue with existing scan data, or re-scan first? (continue/rescan)"
   - If user says rescan: tell them to run `/motif:scan` first, then return to compose
   - If user says continue: proceed normally
4. If the scan date IS today: proceed without warning

If BROWNFIELD is false: skip this step entirely.
```

**Change 2: Replace Step F in the subagent prompt — Validate and Commit**

Find the current Step F section inside the `<agent_spawn>` block:
```markdown
### F. Commit
Commit all files: `design(compose): implement {SCREEN_NAME} screen`
```

Replace it with:
```markdown
### F. Validate and Commit

**F1. Stage all created files** (do NOT commit yet):
```bash
git add [every file you created — list them all explicitly]
```

**F2. Run post-decomposition validation:**
```bash
node scripts/compose-validator.js --screen {SCREEN_NAME} --files [file1] [file2] ...
```
Pass every file you created as --files arguments.

Read the JSON output from stdout.

**F3. Act on results:**

IF status is "pass" or "warn":
- Commit atomically: `git commit -m "design(compose): implement {SCREEN_NAME} screen"`
- If there were warnings, note them in SUMMARY.md under ## Validation

IF status is "fail":
- Roll back staging: `git reset HEAD [all files you staged in F1]`
- Do NOT delete the files — leave them on disk for user inspection
- Do NOT attempt to fix the errors yourself
- In SUMMARY.md, record the validation failure (see Step E)
- The orchestrator will report the failure to the user
```

**Change 3: Add Validation section to summary template in Step E**

In Step E (Create Summary), add a `## Validation` section at the end of the summary template, after `## Files Imported`:

```markdown
## Validation
- Status: PASSED/FAILED/WARN
- Import cycles: none / [list cycles]
- Naming conflicts: none / [list conflicts]
- Prop warnings: none / [list warnings]
```

Also add to the Self-Review Checklist (Step D) a new item:
```markdown
- [ ] All created files staged but NOT committed until validation passes
```

Do NOT change anything else in the workflow. Preserve all existing content exactly. The changes are surgical: insert Step 2b, replace Step F, extend Step E template, add one checklist item to Step D.
  </action>
  <verify>
```bash
grep -c "compose-validator.js" core/workflows/compose-screen.md
```
Expected: at least 1 match (in Step F)

```bash
grep -c "Step 2b" core/workflows/compose-screen.md
```
Expected: at least 1 match

```bash
grep -c "## Validation" core/workflows/compose-screen.md
```
Expected: at least 1 match (in summary template)

```bash
grep -c "git reset HEAD" core/workflows/compose-screen.md
```
Expected: at least 1 match (in rollback instruction)
  </verify>
  <done>
compose-screen.md contains: (1) Step 2b with scan freshness check that reads PROJECT-SCAN.md date and warns if not today, (2) Step F with stage-validate-commit-or-rollback pattern referencing compose-validator.js, (3) Validation section in Step E summary template, (4) self-review checklist item about staging before validation. All existing content is preserved unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sync live copy of compose-screen.md</name>
  <files>.claude/get-motif/workflows/compose-screen.md</files>
  <action>
Copy the updated `core/workflows/compose-screen.md` to `.claude/get-motif/workflows/compose-screen.md` so both copies are byte-identical. This is the same sync pattern used in Phase 15 (15-02, Task 2).

Read `core/workflows/compose-screen.md` and write its exact contents to `.claude/get-motif/workflows/compose-screen.md`.

Verify byte-identical with:
```bash
diff core/workflows/compose-screen.md .claude/get-motif/workflows/compose-screen.md
```
Expected: no output (files are identical).
  </action>
  <verify>
```bash
diff core/workflows/compose-screen.md .claude/get-motif/workflows/compose-screen.md
```
Expected: no output (byte-identical)

```bash
grep -c "compose-validator.js" .claude/get-motif/workflows/compose-screen.md
```
Expected: same count as canonical copy
  </verify>
  <done>
.claude/get-motif/workflows/compose-screen.md is byte-identical to core/workflows/compose-screen.md. Both contain the validation gate, staleness check, and updated summary template.
  </done>
</task>

</tasks>

<verification>
- Both compose-screen.md files contain "compose-validator.js" reference
- Both files contain "Step 2b" staleness check
- Both files contain "## Validation" in summary template
- Both files contain "git reset HEAD" rollback instruction
- Both files are byte-identical (diff produces no output)
- Existing workflow steps (1-4, A-E content besides additions) are unchanged
</verification>

<success_criteria>
The compose workflow now: (1) warns when scan data is stale before composition, (2) validates all decomposed output via compose-validator.js before committing, (3) commits atomically on pass/warn, (4) rolls back staging on failure without deleting files, and (5) reports validation status in screen summaries. Both canonical and live copies are in sync.
</success_criteria>

<output>
After completion, create `.planning/phases/16-validation-and-hardening/16-02-SUMMARY.md`
</output>

---
phase: 20-cli-commands-and-vertical-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/references/verticals/social.md
  - core/references/verticals/education.md
  - core/references/verticals/marketplace.md
  - core/references/verticals/devtools.md
  - bin/lib/manifest.js
  - bin/commands/status.js
  - bin/commands/list.js
autonomous: true

must_haves:
  truths:
    - "User can run motif status and see installed version, workflow phase, and screen count"
    - "User can run motif list and see all 8 available verticals with descriptions"
  artifacts:
    - path: "bin/lib/manifest.js"
      provides: "Shared manifest reading and version comparison utilities"
      exports: ["readManifest", "getPackageVersion", "compareVersions", "hashFile"]
    - path: "bin/commands/status.js"
      provides: "Status command showing version, phase, screens"
      exports: ["run"]
    - path: "bin/commands/list.js"
      provides: "List command showing available verticals"
      exports: ["run"]
    - path: "core/references/verticals/social.md"
      provides: "Social vertical reference in package source"
    - path: "core/references/verticals/education.md"
      provides: "Education vertical reference in package source"
    - path: "core/references/verticals/marketplace.md"
      provides: "Marketplace vertical reference in package source"
    - path: "core/references/verticals/devtools.md"
      provides: "DevTools vertical reference in package source"
  key_links:
    - from: "bin/commands/status.js"
      to: "bin/lib/manifest.js"
      via: "require readManifest, getPackageVersion"
      pattern: "require.*manifest"
    - from: "bin/commands/status.js"
      to: ".claude/get-motif/scripts/motif-state.js"
      via: "execSync shell-out for workflow state"
      pattern: "execSync.*motif-state"
    - from: "bin/commands/list.js"
      to: "core/references/verticals/"
      via: "readdirSync from pkgDir"
      pattern: "readdirSync.*verticalsDir"
---

<objective>
Sync missing vertical source files, create shared manifest utility, and implement the status and list CLI commands.

Purpose: Establishes the shared infrastructure (manifest.js) needed by all commands, resolves the vertical sync prerequisite for the list command, and delivers 2 of 4 new CLI subcommands.

Output: 4 synced vertical files in core/, shared manifest utility, and working status + list command modules.
</objective>

<execution_context>
@/Users/austineogbuanya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austineogbuanya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-cli-commands-and-vertical-discovery/20-RESEARCH.md
@bin/cli.js
@bin/commands/init.js
@bin/lib/find-root.js
@scripts/check-version.js
@.motif-manifest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync vertical source files and create shared manifest utility</name>
  <files>
    core/references/verticals/social.md
    core/references/verticals/education.md
    core/references/verticals/marketplace.md
    core/references/verticals/devtools.md
    bin/lib/manifest.js
  </files>
  <action>
    1. Copy the 4 new vertical files from .claude/get-motif/references/verticals/ to core/references/verticals/:
       - social.md, education.md, marketplace.md, devtools.md
       These are the authoritative versions created in Phase 18. Copy file contents exactly.

    2. Create bin/lib/manifest.js as a shared utility used by status, update, doctor commands. Export these functions:

       - readManifest(projectRoot): Read .motif-manifest.json from projectRoot, return parsed JSON or null on missing/invalid.
       - getPackageVersion(): Read version from the package's own package.json (use path.resolve(__dirname, '..', '..', 'package.json')).
       - compareVersions(a, b): Copy the proven pattern from scripts/check-version.js -- split on dots, compare numerically, return -1/0/1.
       - hashFile(filePath): SHA-256 hash of file contents using node:crypto createHash. This is already implemented in bin/commands/init.js -- extract the same logic.

    Use 'use strict' and require only node:fs, node:path, node:crypto. Zero external dependencies.
  </action>
  <verify>
    Run: ls core/references/verticals/ | wc -l (should show 8 files)
    Run: node -e "const m = require('./bin/lib/manifest.js'); console.log(typeof m.readManifest, typeof m.getPackageVersion, typeof m.compareVersions, typeof m.hashFile)"
    Expected output: function function function function
  </verify>
  <done>8 vertical files exist in core/references/verticals/. manifest.js exports all 4 functions and they load without error.</done>
</task>

<task type="auto">
  <name>Task 2: Implement motif status command</name>
  <files>bin/commands/status.js</files>
  <action>
    Create bin/commands/status.js following the established command module pattern (export run(args), use parseArgs, findProjectRoot).

    The status command must:
    1. Parse --help/-h flag. On help, print usage and exit.
    2. Find project root via findProjectRoot(process.cwd()). If not found, error "Not inside a project directory" and exit 1.
    3. Read manifest via readManifest(projectRoot). If null, error "No Motif installation found. Run 'motif init' to install." and exit 1.
    4. Print header: "Motif Status" (bold)
    5. Print version info from manifest: Version, Runtime, Installed date (always available).
    6. Print package version: compare manifest.version with getPackageVersion(). If package is newer, show "(update available: vX.Y.Z)" in yellow next to version line.
    7. Read design workflow state by shelling out to the motif-state.js script:
       - Construct path: path.join(projectRoot, '.claude', 'get-motif', 'scripts', 'motif-state.js')
       - If script exists, run: execSync(`node "${stateScript}" read`, { encoding: 'utf8', timeout: 5000 })
       - Parse JSON output. If no error field: show Phase and Screens (composed/total).
       - If script missing or error or no STATE.md: show "No active design project" for phase and "--" for screens. Do NOT crash.
    8. Use node:child_process execSync for the state read. Use styleText for formatting (bold headers, cyan values, yellow warnings, green for up-to-date).

    Follow the exact flag parsing pattern from init.js (parseArgs with strict: true).
  </action>
  <verify>
    Run: node -e "require('./bin/commands/status.js')" (should load without error)
    Run: node bin/cli.js status --help (should print help text and exit 0)
    Note: Full functional test requires being in a project with .motif-manifest.json, which the e2e tests in Plan 02 will cover.
  </verify>
  <done>status.js exports run(args), loads without error, prints help on --help, and shows version/phase/screens when run from a Motif-installed project.</done>
</task>

<task type="auto">
  <name>Task 3: Implement motif list command</name>
  <files>bin/commands/list.js</files>
  <action>
    Create bin/commands/list.js following the established command module pattern. This command does NOT require a project context -- it reads from the package source.

    The list command must:
    1. Parse --help/-h flag. On help, print usage and exit.
    2. Resolve the verticals directory: path.resolve(__dirname, '..', '..', 'core', 'references', 'verticals').
    3. If directory doesn't exist, error "Verticals directory not found" and exit 1.
    4. Read all .md files from the directory, sort alphabetically.
    5. For each file:
       - Extract vertical name from filename (strip .md extension).
       - Read file content.
       - Extract the core design principle: find the first text wrapped in ** ** (bold markdown). This is the short description.
       - Display as: "  {name padded to 14 chars}  {description}" with name in cyan using styleText.
    6. Print header: "Available Verticals" (bold) with blank line after.
    7. Print footer: "{count} verticals available. Use 'motif init' to start a project." with blank line before.

    No findProjectRoot needed. No manifest reading needed. Only requires node:fs, node:path, node:util (styleText).
  </action>
  <verify>
    Run: node bin/commands/list.js (should list 8 verticals with descriptions)
    Run: node bin/cli.js list --help (should print help text once registered in Plan 02)
    Direct test now: node -e "require('./bin/commands/list.js').run([])" (should print all 8 verticals)
  </verify>
  <done>list.js exports run(args), shows all 8 verticals (devtools, ecommerce, education, fintech, health, marketplace, saas, social) with descriptions extracted from each file.</done>
</task>

</tasks>

<verification>
- ls core/references/verticals/ shows 8 .md files
- node -e "require('./bin/lib/manifest.js')" loads without error
- node -e "require('./bin/commands/status.js').run(['--help'])" prints help
- node -e "require('./bin/commands/list.js').run([])" prints 8 verticals
</verification>

<success_criteria>
- 4 vertical source files synced to core/references/verticals/
- manifest.js exports readManifest, getPackageVersion, compareVersions, hashFile
- status.js shows version, phase, screens (graceful fallback when no design project)
- list.js shows all 8 verticals with extracted descriptions
</success_criteria>

<output>
After completion, create `.planning/phases/20-cli-commands-and-vertical-discovery/20-01-SUMMARY.md`
</output>

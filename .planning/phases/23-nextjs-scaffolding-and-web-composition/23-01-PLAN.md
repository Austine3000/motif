---
phase: 23-nextjs-scaffolding-and-web-composition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/motif/init.md
  - .claude/get-motif/references/framework-registry.json
autonomous: true

must_haves:
  truths:
    - "User answers what they are building in Round 1 and receives a framework recommendation with reasoning before the platform question in Round 4"
    - "Recommendation is based on project description keywords (dashboard->nextjs, landing page->static, mobile app->expo, SPA->vite)"
    - "User can accept the recommendation or override to any platform"
    - "After platform selection, if platform is web-nextjs, init instructions tell the user about scaffolding (or downstream workflow handles it)"
  artifacts:
    - path: ".claude/commands/motif/init.md"
      provides: "Framework recommendation logic between Round 1 and Round 4, scaffolding instructions in post-generation"
      contains: "Auto-recommendation rules"
    - path: ".claude/get-motif/references/framework-registry.json"
      provides: "Updated web-nextjs config with shadcn postInstall steps and core component list"
      contains: "shadcn"
  key_links:
    - from: ".claude/commands/motif/init.md"
      to: ".claude/get-motif/references/framework-registry.json"
      via: "Init reads registry for scaffold command and postInstall steps"
      pattern: "framework-registry"
---

<objective>
Add framework recommendation logic to /motif:init and update the framework registry with scaffolding post-install steps for shadcn/ui.

Purpose: Users should receive an intelligent framework suggestion based on what they describe building, rather than being asked a cold platform question. The registry must include shadcn/ui installation as a post-scaffold step so downstream workflows know to run it.

Output: Updated init.md with recommendation logic, updated framework-registry.json with shadcn postInstall config.
</objective>

<execution_context>
@/Users/austineogbuanya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austineogbuanya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-nextjs-scaffolding-and-web-composition/23-RESEARCH.md
@.claude/commands/motif/init.md
@.claude/get-motif/references/framework-registry.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add framework recommendation logic to init.md</name>
  <files>.claude/commands/motif/init.md</files>
  <action>
Add a new section between Round 1 and Round 4 in init.md called "Framework Recommendation" (placed after the Vertical Detection section, before Round 4).

The recommendation logic works as follows:
- After Round 1 answers and vertical detection, internally assess platform fit using keyword matching on the user's "what are you building" answer.
- Rules (from research):
  - "dashboard", "admin panel", "CRM", "analytics", "portfolio", "blog", "e-commerce store", "marketplace", "saas" -> web-nextjs (SSR benefits, SEO, App Router)
  - "landing page", "portfolio site", "brochure site", "one-page" -> web-static (simplest, fastest)
  - "single page app", "SPA", "tool", "internal tool", "calculator", "widget" -> web-vite (client-side rendering)
  - "mobile app", "iOS", "Android", "cross-platform mobile", "native app" -> mobile-expo (React Native)
  - Ambiguous or unclear -> web-nextjs (most versatile default)

- Present the recommendation at the START of Round 4, BEFORE asking the platform question:
  "Based on what you're building, I'd recommend **{label}** ({reasoning}). Want to go with that, or prefer something else?"
  Then show the platform options (a/b/c/d) so the user can accept or override.

- If the user accepts the recommendation, skip the platform question (already answered).
- If the user overrides, use their explicit choice.

Also update Round 4 to add a post-platform note: If platform is web-nextjs, web-vite, or mobile-expo, tell user: "After design system generation, I'll scaffold your project with the right tooling."

Do NOT change any other part of init.md. Preserve all existing content exactly.
  </action>
  <verify>Read init.md and confirm: (1) "Framework Recommendation" or "Auto-recommendation" section exists between Vertical Detection and Round 4, (2) keyword-to-platform mapping rules are present, (3) Round 4 platform question is preserved as override option, (4) all other init.md content is unchanged.</verify>
  <done>Init.md contains framework recommendation logic that maps project descriptions to platform suggestions, presents before the platform question, and allows override.</done>
</task>

<task type="auto">
  <name>Task 2: Update framework-registry.json with shadcn postInstall for web-nextjs</name>
  <files>.claude/get-motif/references/framework-registry.json</files>
  <action>
Update the web-nextjs entry in framework-registry.json:

1. Change `postInstall` from empty array to include shadcn setup:
   ```json
   "postInstall": [
     "npx shadcn@latest init -y -b neutral",
     "npx shadcn@latest add button card input badge -y"
   ]
   ```

2. Add a `shadcn` config block to the web-nextjs entry:
   ```json
   "shadcn": {
     "initCommand": "npx shadcn@latest init -y -b neutral",
     "coreComponents": ["button", "card", "input", "badge"],
     "addCommand": "npx shadcn@latest add {component} -y"
   }
   ```

3. Add `scaffoldSequence` field to document the correct order:
   ```json
   "scaffoldSequence": [
     "create-next-app (scaffold.command + scaffold.args)",
     "shadcn init (shadcn.initCommand)",
     "shadcn add core components (shadcn.coreComponents)",
     "generate globals.css with token bridge (tailwind-config-generator.js)"
   ]
   ```

Do NOT modify web-vite, web-static, or mobile-expo entries. Only update web-nextjs.

Per research: sequence matters -- create-next-app first, then shadcn init, then Motif globals.css generation last (shadcn init overwrites globals.css, so Motif must write last).
  </action>
  <verify>Read framework-registry.json and confirm: (1) web-nextjs.postInstall is non-empty with shadcn commands, (2) web-nextjs.shadcn block exists with initCommand and coreComponents, (3) web-nextjs.scaffoldSequence documents the correct order, (4) other platform entries are unchanged, (5) JSON is valid (node -e "JSON.parse(require('fs').readFileSync('.claude/get-motif/references/framework-registry.json','utf8'))").</verify>
  <done>framework-registry.json web-nextjs entry includes shadcn postInstall steps, core component list, and documented scaffold sequence.</done>
</task>

</tasks>

<verification>
1. Read init.md and grep for "recommendation" or "recommend" -- should find the new section
2. Read framework-registry.json and verify web-nextjs has shadcn config
3. Verify JSON validity: `node -e "JSON.parse(require('fs').readFileSync('.claude/get-motif/references/framework-registry.json','utf8')); console.log('valid')"`
4. Verify other platform entries unchanged by comparing key fields
</verification>

<success_criteria>
- init.md has framework recommendation logic that suggests platforms based on project description keywords
- framework-registry.json documents the full Next.js scaffolding sequence including shadcn/ui
- All changes are backward-compatible (no existing behavior broken)
</success_criteria>

<output>
After completion, create `.planning/phases/23-nextjs-scaffolding-and-web-composition/23-01-SUMMARY.md`
</output>
